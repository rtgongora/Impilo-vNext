# =============================================================================
# Impilo vNext — Runtime Phase (Wave 18)
# =============================================================================
#
# Runs all infrastructure + backend services + experience UI + edge enforcement.
# Requires: JARs built via docker-compose.build.yml (or local Maven build).
#
# Usage:
#   docker compose -f docker-compose.runtime.yml up -d
#   (or: ./scripts/dev-runtime.sh up)
#
# Env file: copy .env.example to .env and adjust as needed.
# =============================================================================

services:
  # ===========================================================================
  # INFRASTRUCTURE
  # ===========================================================================

  # ── PostgreSQL 16 ──────────────────────────────────────────────────
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-impilo}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-changeme}
    ports:
      - "5433:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data
      - ./scripts/seed/01-init-databases.sql:/docker-entrypoint-initdb.d/01-init-databases.sql
      - ./scripts/seed/02-seed-tuso.sql:/docker-entrypoint-initdb.d/02-seed-tuso.sql
      - ./scripts/seed/03-seed-vito.sql:/docker-entrypoint-initdb.d/03-seed-vito.sql
      - ./scripts/seed/04-seed-varapi.sql:/docker-entrypoint-initdb.d/04-seed-varapi.sql
      - ./scripts/seed/05-seed-tshepo.sql:/docker-entrypoint-initdb.d/05-seed-tshepo.sql
      - ./scripts/seed/06-seed-zibo.sql:/docker-entrypoint-initdb.d/06-seed-zibo.sql
      - ./scripts/seed/07-seed-pct.sql:/docker-entrypoint-initdb.d/07-seed-pct.sql
      - ./scripts/seed/08-seed-oros.sql:/docker-entrypoint-initdb.d/08-seed-oros.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-impilo}"]
      interval: 5s
      timeout: 3s
      retries: 10

  # ── PostgreSQL database bootstrap ─────────────────────────────────
  # The official postgres init scripts only run on an empty pg_data volume.
  # This idempotent step also repairs existing local volumes that were started
  # before all runtime databases existed.
  db-bootstrap:
    image: postgres:16-alpine
    environment:
      PGHOST: postgres
      PGPORT: 5432
      PGUSER: ${POSTGRES_USER:-impilo}
      PGPASSWORD: ${POSTGRES_PASSWORD:-changeme}
    depends_on:
      postgres:
        condition: service_healthy
    command:
      - sh
      - -ec
      - |
        databases="
        tshepo vito varapi tuso zibo msika ubomi pct oros pharmacy inpatient inventory mushex costing
        document_service notification jobs offline_sync integration_hub
        tshepo_authz tshepo_identity tshepo_consent mvumo tshepo_audit tshepo_keys tshepo_offline
        landela_adapter credential_verification share_slip card_print
        msika_flow product_registry inventory_elmis pharmacy_elmis
        butano butano_fhir fhir_gateway pacs
        keycloak experience_bff guidance clinical_knowledge impilo_learning
        reporting ndr impilo_ndr impilo_data_warehouse pipeline impilo_data_ingestion impilo_data_governance
        dags secharden surv camp impilo_audit_ledger
        impilo_forms impilo_search impilo_channels impilo_connector_fhir impilo_dispatch impilo_rules impilo_workflow impilo_schema_registry
        impilo_iot_ingestion impilo_asset_registry impilo_offline_edge
        impilo_coverage impilo_indawo costa_db mushex_db
        impilo_dev_portal impilo_support observability impilo_identity_assurance
        "
        for db in $${databases}; do
          if ! psql -d postgres -tAc "SELECT 1 FROM pg_database WHERE datname = '$${db}'" | grep -q 1; then
            createdb "$${db}"
            echo "created database $${db}"
          fi
        done
        psql -d tuso -c 'CREATE EXTENSION IF NOT EXISTS pg_trgm; CREATE EXTENSION IF NOT EXISTS btree_gin;'
        psql -d impilo_search -c 'CREATE EXTENSION IF NOT EXISTS pg_trgm; CREATE EXTENSION IF NOT EXISTS btree_gin;'
    restart: "no"

  # ── Redis 7 ────────────────────────────────────────────────────────
  redis:
    image: redis:7-alpine
    ports:
      - "6390:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  # ── Apache Kafka 3.7 (KRaft — no ZooKeeper) ───────────────────────
  kafka:
    image: apache/kafka:3.7.1
    environment:
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: broker,controller
      KAFKA_LISTENERS: PLAINTEXT://0.0.0.0:9092,CONTROLLER://0.0.0.0:9093
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@kafka:9093
      KAFKA_LOG_DIRS: /tmp/kraft-combined-logs
      CLUSTER_ID: "impilo-local-dev-cluster-01"
    ports:
      - "9092:9092"
    healthcheck:
      test: ["CMD-SHELL", "/opt/kafka/bin/kafka-broker-api-versions.sh --bootstrap-server localhost:9092 > /dev/null 2>&1"]
      interval: 10s
      timeout: 20s
      retries: 12
      start_period: 90s

  # ── Keycloak 25.x ─────────────────────────────────────────────────
  keycloak:
    image: quay.io/keycloak/keycloak:25.0
    command: start-dev --import-realm --health-enabled=true
    environment:
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: ${KEYCLOAK_ADMIN_PASSWORD:-admin}
      KC_DB: postgres
      KC_DB_URL: jdbc:postgresql://postgres:5432/keycloak
      KC_DB_USERNAME: ${POSTGRES_USER:-impilo}
      KC_DB_PASSWORD: ${POSTGRES_PASSWORD:-changeme}
      KC_HEALTH_ENABLED: "true"
    volumes:
      - ./tools/auth/impilo-realm.json:/opt/keycloak/data/import/impilo-realm.json:ro
    ports:
      - "8480:8080"
    depends_on:
      db-bootstrap:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "bash", "-c", "exec 3<>/dev/tcp/localhost/9000 && echo -e 'GET /health/ready HTTP/1.1\\r\\nHost: localhost\\r\\nConnection: close\\r\\n\\r\\n' >&3 && grep -q '\"status\":\"UP\"\\|200' <&3"]
      interval: 10s
      timeout: 5s
      retries: 15
      start_period: 60s

  # ── MinIO (S3-compatible) ──────────────────────────────────────────
  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ACCESS_KEY:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_SECRET_KEY:-minioadmin}
    ports:
      - "9000:9000"
      - "9003:9001"
    volumes:
      - minio_data:/data

  # ── HAPI FHIR R4 (BUTANO — Shared Health Record) ──────────────────
  hapi-fhir:
    image: hapiproject/hapi:v7.4.0
    environment:
      JAVA_TOOL_OPTIONS: -Xms256m -Xmx768m -XX:+UseG1GC
      spring.datasource.url: jdbc:postgresql://postgres:5432/butano
      spring.datasource.username: ${POSTGRES_USER:-impilo}
      spring.datasource.password: ${POSTGRES_PASSWORD:-changeme}
      spring.datasource.driverClassName: org.postgresql.Driver
      spring.jpa.properties.hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
      hapi.fhir.fhir_version: R4
    mem_limit: 1280m
    ports:
      - "18090:8080"
    depends_on:
      db-bootstrap:
        condition: service_completed_successfully
    healthcheck:
      disable: true

  # ===========================================================================
  # EDGE — Envoy + OPA
  # ===========================================================================

  # ── OPA (Open Policy Agent) ────────────────────────────────────────
  opa:
    image: openpolicyagent/opa:0.68.0-debug
    command:
      - "run"
      - "--server"
      - "--addr=0.0.0.0:8181"
      - "--set=decision_logs.console=true"
      - "/policies"
    volumes:
      - ./tools/ops/gateway/opa:/policies:ro
    ports:
      - "8181:8181"
    healthcheck:
      test: ["CMD", "/opa", "eval", "true"]
      interval: 5s
      timeout: 3s
      retries: 5

  # ── TSHEPO-AUTHZ — gRPC ext_authz (port 9090) ─────────────────────
  tshepo-authz:
    build:
      context: ./services/tshepo-authz-service
      dockerfile: Dockerfile.dev
    environment:
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      POSTGRES_USER: ${POSTGRES_USER:-impilo}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-changeme}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      KEYCLOAK_URL: http://keycloak:8080
      KEYCLOAK_REALM: ${KEYCLOAK_REALM:-impilo}
      SERVER_PORT: 8081
    mem_limit: 768m
    ports:
      - "19090:9090"
      - "18082:8081"
    depends_on:
      db-bootstrap:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
      kafka:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8081/actuator/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 15
      start_period: 60s

  # ── Envoy Gateway ─────────────────────────────────────────────────
  envoy:
    image: envoyproxy/envoy:v1.31-latest
    volumes:
      - ./infra/envoy/envoy-runtime.yaml:/etc/envoy/envoy.yaml:ro
    ports:
      - "10000:10000"
      - "9901:9901"
    depends_on:
      opa:
        condition: service_healthy
      tshepo-authz:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "bash", "-c", "exec 3<>/dev/tcp/localhost/9901 && echo -e 'GET /ready HTTP/1.1\\r\\nHost: localhost\\r\\nConnection: close\\r\\n\\r\\n' >&3 && grep -q 'LIVE' <&3"]
      interval: 5s
      timeout: 3s
      retries: 10

  # ===========================================================================
  # BACKEND SERVICES — Core Registry Spine
  # ===========================================================================

  # ── TSHEPO — Trust & Governance (port 8081) ────────────────────────
  tshepo:
    build:
      context: ./services/tshepo-service
      dockerfile: Dockerfile
    environment:
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      POSTGRES_USER: ${POSTGRES_USER:-impilo}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-changeme}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      KEYCLOAK_URL: http://keycloak:8080
      KEYCLOAK_REALM: ${KEYCLOAK_REALM:-impilo}
      SPRING_PROFILES_ACTIVE: default
      SERVER_PORT: 8081
      # Disable OAuth2 resource server for dev (no realm configured yet)
      SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_ISSUER_URI: ""
      SPRING_AUTOCONFIGURE_EXCLUDE: org.springframework.boot.autoconfigure.security.oauth2.resource.servlet.OAuth2ResourceServerAutoConfiguration
    mem_limit: 768m
    ports:
      - "18081:8081"
    depends_on:
      db-bootstrap:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
      kafka:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8081/actuator/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 15
      start_period: 45s

  # ── VITO — Client Registry (port 8082) ─────────────────────────────
  vito:
    build:
      context: ./services/vito-service
      dockerfile: Dockerfile
    environment:
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      POSTGRES_USER: ${POSTGRES_USER:-impilo}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-changeme}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      KEYCLOAK_URL: http://keycloak:8080
      KEYCLOAK_REALM: ${KEYCLOAK_REALM:-impilo}
      SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_ISSUER_URI: ""
      SPRING_AUTOCONFIGURE_EXCLUDE: org.springframework.boot.autoconfigure.security.oauth2.resource.servlet.OAuth2ResourceServerAutoConfiguration
      VITO_HMAC_PEPPER: impilo-dev-hmac-pepper-changeme-in-prod
    mem_limit: 768m
    ports:
      - "8092:8082"
    depends_on:
      db-bootstrap:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
      kafka:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8082/actuator/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 15
      start_period: 45s

  # ── VARAPI — Provider Registry (port 8083) ─────────────────────────
  varapi:
    build:
      context: ./services/varapi-service
      dockerfile: Dockerfile
    environment:
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      POSTGRES_USER: ${POSTGRES_USER:-impilo}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-changeme}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      KEYCLOAK_URL: http://keycloak:8080
      KEYCLOAK_REALM: ${KEYCLOAK_REALM:-impilo}
      SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_ISSUER_URI: ""
      SPRING_AUTOCONFIGURE_EXCLUDE: org.springframework.boot.autoconfigure.security.oauth2.resource.servlet.OAuth2ResourceServerAutoConfiguration
    mem_limit: 768m
    ports:
      - "8097:8083"
    depends_on:
      db-bootstrap:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
      kafka:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8083/actuator/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 15
      start_period: 45s

  # ── TUSO — Facility Registry (port 8084) ───────────────────────────
  tuso:
    build:
      context: ./services/tuso-service
      dockerfile: Dockerfile
    environment:
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      POSTGRES_USER: ${POSTGRES_USER:-impilo}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-changeme}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      KEYCLOAK_URL: http://keycloak:8080
      KEYCLOAK_REALM: ${KEYCLOAK_REALM:-impilo}
      SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_ISSUER_URI: ""
      SPRING_AUTOCONFIGURE_EXCLUDE: org.springframework.boot.autoconfigure.security.oauth2.resource.servlet.OAuth2ResourceServerAutoConfiguration
    mem_limit: 768m
    ports:
      - "8096:8084"
    depends_on:
      db-bootstrap:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
      kafka:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8084/actuator/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 15
      start_period: 45s

  # ── ZIBO — Terminology (port 8085) ─────────────────────────────────
  zibo:
    build:
      context: ./services/zibo-service
      dockerfile: Dockerfile
    environment:
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      POSTGRES_USER: ${POSTGRES_USER:-impilo}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-changeme}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      KEYCLOAK_URL: http://keycloak:8080
      KEYCLOAK_REALM: ${KEYCLOAK_REALM:-impilo}
      SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_ISSUER_URI: ""
      SPRING_AUTOCONFIGURE_EXCLUDE: org.springframework.boot.autoconfigure.security.oauth2.resource.servlet.OAuth2ResourceServerAutoConfiguration
    mem_limit: 768m
    ports:
      - "8085:8085"
    depends_on:
      db-bootstrap:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
      kafka:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8085/actuator/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 15
      start_period: 45s

  # ── PCT — Patient Care Tracker (port 8088) ─────────────────────────
  pct:
    build:
      context: ./services/pct-service
      dockerfile: Dockerfile
    environment:
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      POSTGRES_USER: ${POSTGRES_USER:-impilo}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-changeme}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      KEYCLOAK_URL: http://keycloak:8080
      KEYCLOAK_REALM: ${KEYCLOAK_REALM:-impilo}
      SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_ISSUER_URI: ""
      SPRING_AUTOCONFIGURE_EXCLUDE: org.springframework.boot.autoconfigure.security.oauth2.resource.servlet.OAuth2ResourceServerAutoConfiguration
    mem_limit: 768m
    ports:
      - "8088:8088"
    depends_on:
      db-bootstrap:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
      kafka:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8088/actuator/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 15
      start_period: 45s

  # ── OROS — Orders & Results (port 8089) ────────────────────────────
  oros:
    build:
      context: ./services/oros-service
      dockerfile: Dockerfile
    environment:
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      POSTGRES_USER: ${POSTGRES_USER:-impilo}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-changeme}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      KEYCLOAK_URL: http://keycloak:8080
      KEYCLOAK_REALM: ${KEYCLOAK_REALM:-impilo}
      SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_ISSUER_URI: ""
      SPRING_AUTOCONFIGURE_EXCLUDE: org.springframework.boot.autoconfigure.security.oauth2.resource.servlet.OAuth2ResourceServerAutoConfiguration
    mem_limit: 768m
    ports:
      - "18089:8089"
    depends_on:
      db-bootstrap:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
      kafka:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8089/actuator/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 15
      start_period: 45s

  # ── Guidance Service — Conversational & Knowledge (port 8260) ────────
  guidance:
    build:
      context: ./services/guidance-service
      dockerfile: Dockerfile
    environment:
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: guidance
      DB_USER: ${POSTGRES_USER:-impilo}
      DB_PASSWORD: ${POSTGRES_PASSWORD:-changeme}
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      SEARCH_SERVICE_URL: http://search:8230
    mem_limit: 768m
    ports:
      - "8260:8260"
    depends_on:
      db-bootstrap:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8260/actuator/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 15
      start_period: 30s

  # ── Clinical Knowledge Platform — EDLIZ-aligned (port 8270) ──────────
  clinical-knowledge-platform:
    build:
      context: ./services/clinical-knowledge-platform-service
      dockerfile: Dockerfile
    environment:
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: clinical_knowledge
      DB_USER: ${POSTGRES_USER:-impilo}
      DB_PASSWORD: ${POSTGRES_PASSWORD:-changeme}
      # PDF ingestion defaults (image copies PDF under /app/edliz — see service Dockerfile)
      CLINICAL_INGESTION_WORKING_DIR: /app
      EDLIZ_REFERENCE_PDF_PATH: edliz/EDLIZ-2025-final-for-circulation.pdf
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      CLINICAL_KAFKA_RELAY_ENABLED: "true"
    mem_limit: 768m
    ports:
      - "8270:8270"
    depends_on:
      db-bootstrap:
        condition: service_completed_successfully
      kafka:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8270/actuator/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 15
      start_period: 40s

  # ── Learning Service — Impilo Fundo backend (port 8235) ─────────────
  fundo:
    build:
      context: ./services/learning-service
      dockerfile: Dockerfile
    environment:
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      POSTGRES_USER: ${POSTGRES_USER:-impilo}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-changeme}
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/impilo_learning
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      KEYCLOAK_URL: http://keycloak:8080
      KEYCLOAK_REALM: ${KEYCLOAK_REALM:-impilo}
      SERVER_PORT: 8235
      LEARNING_SECURITY_OAUTH2_ENABLED: "false"
      LEARNING_DATABASE_AUTO_CREATE_ENABLED: ${LEARNING_DATABASE_AUTO_CREATE_ENABLED:-true}
      LEARNING_OUTBOX_PUBLISHER_ENABLED: ${LEARNING_OUTBOX_PUBLISHER_ENABLED:-false}
    mem_limit: 1024m
    ports:
      - "8235:8235"
    depends_on:
      db-bootstrap:
        condition: service_completed_successfully
      kafka:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8235/actuator/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 40s

  # ===========================================================================
  # EXPERIENCE LAYER
  # ===========================================================================

  # ── Experience BFF (port 8160) ─────────────────────────────────────
  experience-bff:
    build:
      context: ./services/experience-bff
      dockerfile: Dockerfile
    environment:
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: experience_bff
      DB_USER: ${POSTGRES_USER:-impilo}
      DB_PASSWORD: ${POSTGRES_PASSWORD:-changeme}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      SPRING_PROFILES_ACTIVE: default
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      KEYCLOAK_URL: http://keycloak:8080
      KEYCLOAK_REALM: ${KEYCLOAK_REALM:-impilo}
      # ── Backend service URLs ──────────────────────────────────────
      PCT_BASE_URL: http://pct:8088
      OROS_BASE_URL: http://oros:8089
      VITO_BASE_URL: http://vito:8082
      TUSO_BASE_URL: http://tuso:8084
      VARAPI_BASE_URL: http://varapi:8083
      BUTANO_BASE_URL: http://hapi-fhir:8090
      FHIR_BASE_URL: http://hapi-fhir:8090/fhir
      PHARMACY_BASE_URL: http://pharmacy:8096
      MSIKA_BASE_URL: http://msika:8086
      MSIKA_FLOW_BASE_URL: http://msika-flow:8100
      MUSHEX_BASE_URL: http://mushex:8102
      COSTA_BASE_URL: http://costa:8101
      COVERAGE_BASE_URL: http://coverage:8140
      INDAWO_BASE_URL: http://indawo:8150
      SURVEILLANCE_BASE_URL: http://surveillance:8180
      CAMPAIGNS_BASE_URL: http://campaigns:8190
      NOTIFICATION_BASE_URL: http://notification:8200
      DATA_GOVERNANCE_BASE_URL: http://data-governance:8220
      DOCUMENT_STORE_BASE_URL: http://document-store:8093
      LANDELA_BASE_URL: http://landela:8092
      ORTHANC_BASE_URL: http://orthanc:8042
      ORTHANC_DICOMWEB_URL: http://orthanc:8042/dicom-web
      GUIDANCE_BASE_URL: http://guidance:8260
      CLINICAL_KNOWLEDGE_PLATFORM_BASE_URL: http://clinical-knowledge-platform:8270
      LEARNING_SERVICE_BASE_URL: http://fundo:8235
    mem_limit: 768m
    ports:
      - "8160:8160"
    depends_on:
      db-bootstrap:
        condition: service_completed_successfully
      kafka:
        condition: service_healthy
      fundo:
        condition: service_healthy
      clinical-knowledge-platform:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8160/actuator/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 15
      start_period: 30s

  # ── One UI Shell — unified user-facing Experience (port 3000) ───────
  one-ui-shell:
    build:
      context: ./ui
      dockerfile: one-ui-shell/Dockerfile
      args:
        NEXT_PUBLIC_BFF_URL: http://localhost:8160
        NEXT_PUBLIC_API_GATEWAY_URL: http://localhost:10000
    environment:
      NEXT_PUBLIC_BFF_URL: http://localhost:8160
      NEXT_PUBLIC_API_GATEWAY_URL: http://localhost:10000
      API_GATEWAY_URL: http://envoy:10000
      BFF_URL: http://experience-bff:8160
      HOSTNAME: "0.0.0.0"
    ports:
      - "3000:3000"
    depends_on:
      experience-bff:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:3000 || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

volumes:
  pg_data:
  minio_data:
