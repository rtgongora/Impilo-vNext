apiVersion: v1
kind: Service
metadata:
  name: {{ include "tshepo-consent.fullname" . }}
  labels:
    {{- include "tshepo-consent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | default .Values.service.httpPort }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tshepo-consent.selectorLabels" . | nindent 4 }}
