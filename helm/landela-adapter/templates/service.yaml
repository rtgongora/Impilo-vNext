apiVersion: v1
kind: Service
metadata:
  name: {{ include "landela-adapter.fullname" . }}
  labels:
    {{- include "landela-adapter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | default .Values.service.httpPort }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "landela-adapter.selectorLabels" . | nindent 4 }}
