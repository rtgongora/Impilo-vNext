apiVersion: v1
kind: Service
metadata:
  name: {{ include "tshepo-audit.fullname" . }}
  labels:
    {{- include "tshepo-audit.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | default .Values.service.httpPort }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tshepo-audit.selectorLabels" . | nindent 4 }}
