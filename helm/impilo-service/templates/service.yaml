apiVersion: v1
kind: Service
metadata:
  name: {{ include "impilo-service.fullname" . }}
  labels:
    {{- include "impilo-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "impilo-service.selectorLabels" . | nindent 4 }}
