apiVersion: v1
kind: Service
metadata:
  name: {{ include "document-store.fullname" . }}
  labels:
    {{- include "document-store.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | default .Values.service.httpPort }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "document-store.selectorLabels" . | nindent 4 }}
