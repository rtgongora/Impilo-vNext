apiVersion: v1
kind: Service
metadata:
  name: {{ include "credential-verification.fullname" . }}
  labels:
    {{- include "credential-verification.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | default .Values.service.httpPort }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "credential-verification.selectorLabels" . | nindent 4 }}
