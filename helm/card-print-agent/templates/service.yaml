apiVersion: v1
kind: Service
metadata:
  name: {{ include "card-print-agent.fullname" . }}
  labels:
    {{- include "card-print-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | default .Values.service.httpPort }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "card-print-agent.selectorLabels" . | nindent 4 }}
