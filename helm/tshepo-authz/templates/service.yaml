apiVersion: v1
kind: Service
metadata:
  name: {{ include "tshepo-authz.fullname" . }}
  labels:
    {{- include "tshepo-authz.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | default .Values.service.httpPort }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tshepo-authz.selectorLabels" . | nindent 4 }}
