<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.6</version>
        <relativePath/>
    </parent>

    <groupId>zw.gov.mohcc.impilo</groupId>
    <artifactId>impilo-parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>Impilo vNext — Parent POM</name>
    <description>
        Shared dependency management and build configuration for all Impilo platform services.
    </description>

    <properties>
        <java.version>21</java.version>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- Dependency versions (single source of truth) -->
        <spring-boot.version>3.3.6</spring-boot.version>
        <spring-kafka.version>3.2.4</spring-kafka.version>
        <hapi-fhir.version>7.4.0</hapi-fhir.version>
        <flyway.version>10.17.3</flyway.version>
        <postgresql.version>42.7.10</postgresql.version>
        <redis.version>3.3.6</redis.version>
        <minio.version>8.6.0</minio.version>
        <bouncycastle.version>1.78.1</bouncycastle.version>
        <opentelemetry.version>1.41.0</opentelemetry.version>
        <mapstruct.version>1.6.2</mapstruct.version>
        <springdoc.version>2.6.0</springdoc.version>
        <nimbus-jose.version>9.40</nimbus-jose.version>
        <grpc.version>1.80.0</grpc.version>
        <grpc-spring-boot.version>3.1.0.RELEASE</grpc-spring-boot.version>
        <protobuf.version>3.25.5</protobuf.version>
        <!-- Biometric matching engine (matcher-engine core-infra component) -->
        <sourceafis.version>3.18.1</sourceafis.version>
        <onnxruntime.version>1.17.0</onnxruntime.version>
    </properties>

    <modules>
        <!-- Build order: libs first, then shared-core, then services -->
        <module>../libs/shared-kernel-java</module>
        <module>../libs/security-baseline</module>
        <module>../libs/paediatric-domain</module>
        <module>../libs/reproductive-domain</module>
        <module>shared-core</module>
        <module>tshepo-service</module>
        <module>vito-service</module>
        <module>varapi-service</module>
        <module>tuso-service</module>
        <module>zibo-service</module>
        <module>msika-service</module>
        <module>ubomi-service</module>
        <module>fhir-gateway-service</module>
        <module>pct-service</module>
        <module>scheduling-service</module>
        <module>referral-service</module>
        <module>simba-service</module>
        <module>oros-service</module>
        <module>pharmacy-service</module>
        <module>pharmacy-elmis-adapter</module>
        <module>inpatient-service</module>
        <module>community-service</module>
        <module>inventory-service</module>
        <module>inventory-elmis-adapter</module>
        <module>product-registry-service</module>
        <!-- Organization Registry — canonical organizations, authorized representatives, Channel-C claims -->
        <module>organization-registry-service</module>
        <module>mushex-service</module>
        <module>mushe-wallet-service</module>
        <module>costing-engine-service</module>
        <module>document-service</module>
        <module>pacs-adapter-service</module>
        <module>notification-service</module>
        <module>jobs-service</module>
        <module>integration-hub</module>
        <module>rules-service</module>
        <module>offline-sync-service</module>
        <module>card-print-agent</module>
        <!-- Landela-Integrated Credential & Document Suite -->
        <module>landela-adapter-service</module>
        <module>credential-verification-service</module>
        <module>share-slip-service</module>
        <!-- MSIKA Flow — Health Marketplace Orchestration -->
        <module>msika-flow-service</module>
        <!-- MSIKA Apps — Health OS Capability Marketplace (apps/plugins/extensions/connectors/adapters/packs/AI skills/devices) -->
        <module>msika-apps-service</module>
        <!-- BUTANO — Shared Health Record (HAPI FHIR JPA) -->
        <module>butano-service</module>
        <module>booking-service</module>
        <!-- Impilo Live — Live Events, Webinars & Broadcast -->
        <module>live-service</module>
        <!-- BUTANO FHIR — FHIR Resource Access & Orchestration -->
        <module>butano-fhir</module>
        <!-- Search Service — Enterprise Search -->
        <module>search-service</module>
        <!-- Learning — Fundo / CPD / helpdesk / workflow contextual layer -->
        <module>learning-service</module>
        <!-- MADI — Blood Donation, Blood Bank and Transfusion -->
        <module>madi-service</module>
        <!-- Rito — Quality, Safety & Client Voice -->
        <module>rito-quality-safety-service</module>
        <!-- Daidzai — Emergency, Disaster & Public-Health Response Command -->
        <module>daidzai-service</module>
        <!-- Participation — Citizen Get-Involved & Co-Design -->
        <module>participation-service</module>
        <module>telemonitoring-service</module>
        <!-- Forms Service — Clinical Content Platform -->
        <module>forms-service</module>
        <!-- Data Pipeline — v1.1-Native Ingestion & Watermarking -->
        <module>data-pipeline-service</module>
        <!-- NDR — National Data Repository (v1.1-Native Analytics Store) -->
        <module>national-data-repository-service</module>
        <!-- Reporting — Platform Analytics & Report Generation (v1.1-Native) -->
        <module>reporting-service</module>
        <!-- DAGS — Data Access Governance Service (v1.1-Native) -->
        <module>data-access-governance-service</module>
        <!-- Surveillance — Public Health Event Monitoring (v1.1-Native) -->
        <module>surveillance-service</module>
        <!-- Campaigns — Public Health Campaign Management (v1.1-Native) -->
        <module>campaigns-service</module>
        <!-- Patient Safety — Pharmacovigilance (ADR/AEFI), Safety Cases & MCAZ Workbench (v1.1-Native) -->
        <module>patient-safety-service</module>
        <!-- Identity Assurance — Device Attestation & Risk Scoring (v1.1-Native) -->
        <module>identity-assurance-service</module>
        <!-- Observability — Dashboard & Alert Rule Registry (v1.1-Native Stub) -->
        <module>observability-service</module>
        <!-- Security Hardening — Policy Pack & Compliance Scan Registry (v1.1-Native Stub) -->
        <module>security-hardening-service</module>
        <!-- Channels — Omnichannel Access Gateway (v1.1-Native) -->
        <module>channels-service</module>
        <!-- Coverage — Coverage & Eligibility Engine (v1.1-Native) -->
        <module>coverage-service</module>
        <!-- INDAWO — Location & Address Registry (v1.1-Native) -->
        <module>indawo-service</module>
        <!-- NDILA — Geospatial Intelligence, Routing, Location & Spatial Orchestration -->
        <module>ndila-service</module>
        <!-- Data Platform — Ring-2 v1.1-Native Services -->
        <module>data-ingestion-service</module>
        <module>data-governance-service</module>
        <module>khuluma-service</module>
        <module>ndr-service</module>
        <module>analytics-pipeline-service</module>
        <!-- Supply & IoT Platform — Asset Registry + Dispatch + IoT Ingestion -->
        <module>asset-registry-service</module>
        <module>dispatch-service</module>
        <module>iot-ingestion-service</module>
        <!-- Nhume — Dispatch, Delivery, Fleet Tracking & Last-Mile Logistics -->
        <module>nhume-service</module>
        <!-- Resilience & Operations Platform -->
        <module>support-service</module>
        <module>audit-ledger-service</module>
        <module>offline-edge-service</module>
        <!-- Data Warehouse — Gold Dataset Materializer (v1.1-Native) -->
        <module>data-warehouse-service</module>
        <!-- Integration Platform Expansion -->
        <module>workflow-service</module>
        <module>connector-fhir-adapter</module>
        <!-- TSHEPO decomposition (6 sub-services) -->
        <module>tshepo-authz-service</module>
        <module>tshepo-identity-service</module>
        <module>tshepo-consent-service</module>
        <!-- Mvumo — national digital consent orchestration (adaptive assurance, templates, remote sessions) -->
        <module>mvumo-service</module>
        <module>tshepo-audit-service</module>
        <module>tshepo-keys-service</module>
        <module>tshepo-offline-service</module>
        <!-- ABIS — biometric template custody & matching (trust plane, D6) -->
        <module>abis-service</module>
        <!-- TSHEPO shared libraries (under libs/) -->
        <module>../libs/tshepo-contracts</module>
        <module>../libs/tshepo-sdk</module>
        <!-- Tech Companion Suite (v1.1 enforcement primitives) -->
        <module>../libs/tech-companion</module>
        <module>../libs/federation-connector</module>
        <module>../libs/tech-companion-harness</module>
        <module>../libs/tech-companion-mock</module>
        <module>../libs/session-templates</module>
        <!-- Shared Kernel Java (v1.1 event envelope, audit ledger) -->
        <!-- duplicate module declaration removed -->
        <!-- Ops Instrumentation (structured logging, golden signals, health checks) -->
        <module>../libs/ops-instrumentation</module>
        <!-- Offline SDK (entitlement verifier + local queue format) -->
        <module>../libs/offline-sdk</module>
        <!-- TSHEPO trust-crypto primitive (B4): JWS verification + canonical trust error model -->
        <module>../libs/tshepo-trust-crypto</module>
        <!-- New approved libraries -->
        <module>../libs/supply-planning</module>
        <module>../libs/vault-kms</module>
        <module>../libs/chaos-testing</module>
        <!-- Contract Tests (schema compatibility + event envelope validator) -->
        <module>../libs/contract-tests</module>
        <!-- Developer & Governance Platform -->
        <module>developer-portal-service</module>
        <module>schema-registry-service</module>
        <!-- Citizen wellness + Health Connect domain (Experience BFF proxies) -->
        <module>wellness-service</module>
        <!-- Experience Platform BFF -->
        <module>experience-bff</module>
        <!-- RTC Gateway — provider-neutral media adapter for governed telemedicine -->
        <module>rtc-gateway-service</module>
        <!-- Workforce Governance — organisations, jurisdictions, assignments -->
        <module>workforce-governance-service</module>
        <!-- Vashandi — operational workforce SoR (roster, shift, attendance, leave, access risk) -->
        <module>vashandi-workforce-service</module>
        <!-- Clinical Knowledge & EDLIZ-aligned guidance platform -->
        <module>clinical-knowledge-platform-service</module>
        <module>matcher-engine</module>
        <module>guidance-service</module>
        <module>ai-model-registry-service</module>
        <module>llm-orchestration-service</module>
        <!-- ERP Plane — native general ledger, HR/payroll, procurement -->
        <module>general-ledger-service</module>
        <module>hr-payroll-service</module>
        <module>procurement-service</module>
    </modules>

    <dependencyManagement>
        <dependencies>
            <!-- Spring Boot BOM (imported from parent, listed here for clarity) -->

            <!-- Biometric matching engine deps (matcher-engine) -->
            <dependency>
                <groupId>com.machinezoo.sourceafis</groupId>
                <artifactId>sourceafis</artifactId>
                <version>${sourceafis.version}</version>
            </dependency>
            <dependency>
                <groupId>com.microsoft.onnxruntime</groupId>
                <artifactId>onnxruntime</artifactId>
                <version>${onnxruntime.version}</version>
            </dependency>

            <!-- Kafka -->
            <dependency>
                <groupId>org.springframework.kafka</groupId>
                <artifactId>spring-kafka</artifactId>
                <version>${spring-kafka.version}</version>
            </dependency>

            <!-- HAPI FHIR -->
            <dependency>
                <groupId>ca.uhn.hapi.fhir</groupId>
                <artifactId>hapi-fhir-base</artifactId>
                <version>${hapi-fhir.version}</version>
            </dependency>
            <dependency>
                <groupId>ca.uhn.hapi.fhir</groupId>
                <artifactId>hapi-fhir-structures-r4</artifactId>
                <version>${hapi-fhir.version}</version>
            </dependency>
            <dependency>
                <groupId>ca.uhn.hapi.fhir</groupId>
                <artifactId>hapi-fhir-client</artifactId>
                <version>${hapi-fhir.version}</version>
            </dependency>

            <!-- Database -->
            <dependency>
                <groupId>org.flywaydb</groupId>
                <artifactId>flyway-core</artifactId>
                <version>${flyway.version}</version>
            </dependency>
            <dependency>
                <groupId>org.flywaydb</groupId>
                <artifactId>flyway-database-postgresql</artifactId>
                <version>${flyway.version}</version>
            </dependency>
            <dependency>
                <groupId>org.postgresql</groupId>
                <artifactId>postgresql</artifactId>
                <version>${postgresql.version}</version>
            </dependency>

            <!-- MinIO -->
            <dependency>
                <groupId>io.minio</groupId>
                <artifactId>minio</artifactId>
                <version>${minio.version}</version>
            </dependency>

            <!-- OpenAPI docs -->
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
                <version>${springdoc.version}</version>
            </dependency>

            <!-- MapStruct -->
            <dependency>
                <groupId>org.mapstruct</groupId>
                <artifactId>mapstruct</artifactId>
                <version>${mapstruct.version}</version>
            </dependency>

            <!-- Cryptography (Argon2id via Bouncy Castle — required by Spring Security) -->
            <dependency>
                <groupId>org.bouncycastle</groupId>
                <artifactId>bcprov-jdk18on</artifactId>
                <version>${bouncycastle.version}</version>
            </dependency>

            <!-- Nimbus JOSE (JWT/JWS/JWK) -->
            <dependency>
                <groupId>com.nimbusds</groupId>
                <artifactId>nimbus-jose-jwt</artifactId>
                <version>${nimbus-jose.version}</version>
            </dependency>

            <!-- gRPC -->
            <dependency>
                <groupId>io.grpc</groupId>
                <artifactId>grpc-netty-shaded</artifactId>
                <version>${grpc.version}</version>
            </dependency>
            <dependency>
                <groupId>io.grpc</groupId>
                <artifactId>grpc-protobuf</artifactId>
                <version>${grpc.version}</version>
            </dependency>
            <dependency>
                <groupId>io.grpc</groupId>
                <artifactId>grpc-stub</artifactId>
                <version>${grpc.version}</version>
            </dependency>
            <dependency>
                <groupId>net.devh</groupId>
                <artifactId>grpc-server-spring-boot-starter</artifactId>
                <version>${grpc-spring-boot.version}</version>
            </dependency>
            <dependency>
                <groupId>com.google.protobuf</groupId>
                <artifactId>protobuf-java</artifactId>
                <version>${protobuf.version}</version>
            </dependency>

            <!-- TSHEPO shared libraries -->
            <dependency>
                <groupId>zw.gov.mohcc.impilo</groupId>
                <artifactId>tshepo-contracts</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>zw.gov.mohcc.impilo</groupId>
                <artifactId>tshepo-sdk</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- Observability -->
            <dependency>
                <groupId>io.opentelemetry</groupId>
                <artifactId>opentelemetry-api</artifactId>
                <version>${opentelemetry.version}</version>
            </dependency>

            <!-- Shared Kernel Java -->
            <dependency>
                <groupId>zw.gov.mohcc.impilo</groupId>
                <artifactId>shared-kernel-java</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- Paediatric domain: age routing + WHO growth standards -->
            <dependency>
                <groupId>zw.gov.mohcc.impilo</groupId>
                <artifactId>paediatric-domain</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- Reproductive domain: gestational dating, parity, contraceptive coverage -->
            <dependency>
                <groupId>zw.gov.mohcc.impilo</groupId>
                <artifactId>reproductive-domain</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- Offline SDK -->
            <dependency>
                <groupId>zw.gov.mohcc.impilo</groupId>
                <artifactId>offline-sdk</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- Contract Tests -->
            <dependency>
                <groupId>zw.gov.mohcc.impilo</groupId>
                <artifactId>contract-tests</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- Tech Companion Suite -->
            <dependency>
                <groupId>zw.gov.mohcc.impilo</groupId>
                <artifactId>tech-companion</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>zw.gov.mohcc.impilo</groupId>
                <artifactId>federation-connector</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>zw.gov.mohcc.impilo</groupId>
                <artifactId>tech-companion-harness</artifactId>
                <version>${project.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <configuration>
                        <source>${java.version}</source>
                        <target>${java.version}</target>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-clean-plugin</artifactId>
                    <configuration>
                        <failOnError>false</failOnError>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <!-- Byte Buddy (Mockito inline) may trail the host JDK; experimental mode avoids failures on newer JDKs (e.g. 25) until BOM catches up. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <argLine>-Dnet.bytebuddy.experimental=true -XX:+EnableDynamicAgentLoading</argLine>
                </configuration>
            </plugin>
            <!-- Stamp every jar with its source git commit (BOOT-INF/classes/git.properties).
                 Makes image/jar provenance a one-line read (Actuator /info, stale-jar audit)
                 instead of byte-diffing archaeology. Auto-discovers the repo .git; never fails
                 the build when .git is absent (e.g. jarless worktree / detached context). -->
            <plugin>
                <groupId>io.github.git-commit-id</groupId>
                <artifactId>git-commit-id-maven-plugin</artifactId>
                <version>9.0.1</version>
                <executions>
                    <execution>
                        <id>get-the-git-infos</id>
                        <goals><goal>revision</goal></goals>
                        <phase>initialize</phase>
                    </execution>
                </executions>
                <configuration>
                    <failOnNoGitDirectory>false</failOnNoGitDirectory>
                    <failOnUnableToExtractRepoInfo>false</failOnUnableToExtractRepoInfo>
                    <generateGitPropertiesFile>true</generateGitPropertiesFile>
                    <generateGitPropertiesFilename>${project.build.outputDirectory}/git.properties</generateGitPropertiesFilename>
                    <includeOnlyProperties>
                        <includeOnlyProperty>^git.commit.id.abbrev$</includeOnlyProperty>
                        <includeOnlyProperty>^git.commit.id.full$</includeOnlyProperty>
                        <includeOnlyProperty>^git.branch$</includeOnlyProperty>
                        <includeOnlyProperty>^git.dirty$</includeOnlyProperty>
                        <includeOnlyProperty>^git.build.time$</includeOnlyProperty>
                    </includeOnlyProperties>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
