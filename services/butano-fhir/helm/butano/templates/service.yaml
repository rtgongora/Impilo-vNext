apiVersion: v1
kind: Service
metadata:
  name: {{ include "butano.fullname" . }}
  labels:
    {{- include "butano.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | default .Values.service.httpPort }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "butano.selectorLabels" . | nindent 4 }}
