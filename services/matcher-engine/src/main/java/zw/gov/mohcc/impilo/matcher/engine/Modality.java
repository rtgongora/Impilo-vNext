package zw.gov.mohcc.impilo.matcher.engine;

/** Biometric modalities the engine can be asked to handle. Mirrors ABIS/VITO BiometricModality. */
public enum Modality {
    FINGERPRINT,
    FACE,
    IRIS
}
