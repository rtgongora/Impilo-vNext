apiVersion: v1
kind: Service
metadata:
  name: {{ include "ubomi.fullname" . }}
  labels:
    {{- include "ubomi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | default .Values.service.httpPort }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ubomi.selectorLabels" . | nindent 4 }}
