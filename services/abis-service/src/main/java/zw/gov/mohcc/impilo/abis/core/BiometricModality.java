package zw.gov.mohcc.impilo.abis.core;

/**
 * Biometric modalities per ITU-T X.1081. Thresholds are approved per modality and
 * versioned with the matching algorithm (Identity Contract §11).
 */
public enum BiometricModality {
    FINGERPRINT,
    IRIS,
    FACE
}
