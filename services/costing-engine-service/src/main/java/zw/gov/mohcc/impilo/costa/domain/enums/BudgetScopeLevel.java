package zw.gov.mohcc.impilo.costa.domain.enums;

/** Organisational scope a budget attaches to (via Tuso/Indawo hierarchy). */
public enum BudgetScopeLevel {
    FACILITY,
    DISTRICT,
    PROVINCE,
    NATIONAL
}
