package zw.gov.mohcc.impilo.costa.domain.enums;

/** Action recorded on the budget approval / transition log. */
public enum BudgetApprovalAction {
    SUBMIT,
    REVIEW,
    APPROVE,
    REJECT,
    RETURN,
    ACTIVATE,
    FREEZE,
    CLOSE,
    ARCHIVE
}
