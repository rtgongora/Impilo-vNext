package zw.gov.mohcc.impilo.costa.domain.enums;

/** The system that owns the underlying commitment record (COSTA holds only a reference). */
public enum CommitmentOwnerSystem {
    OROS,
    MSIKA,
    DURA,
    FUNDO,
    DAIDZAI,
    PCT,
    COSTA,
    MANUAL
}
