apiVersion: v1
kind: Service
metadata:
  name: {{ include "costing.fullname" . }}
  labels:
    {{- include "costing.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | default .Values.service.httpPort }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "costing.selectorLabels" . | nindent 4 }}
