apiVersion: v1
kind: Service
metadata:
  name: {{ include "product-registry.fullname" . }}
  labels:
    {{- include "product-registry.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | default .Values.service.httpPort }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "product-registry.selectorLabels" . | nindent 4 }}
