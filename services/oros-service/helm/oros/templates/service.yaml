apiVersion: v1
kind: Service
metadata:
  name: {{ include "oros.fullname" . }}
  labels:
    {{- include "oros.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | default .Values.service.httpPort }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "oros.selectorLabels" . | nindent 4 }}
