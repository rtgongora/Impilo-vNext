apiVersion: v1
kind: Service
metadata:
  name: {{ include "pacs-adapter.fullname" . }}
  labels:
    {{- include "pacs-adapter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | default .Values.service.httpPort }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pacs-adapter.selectorLabels" . | nindent 4 }}
