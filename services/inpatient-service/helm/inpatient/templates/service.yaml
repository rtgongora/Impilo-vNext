apiVersion: v1
kind: Service
metadata:
  name: {{ include "inpatient.fullname" . }}
  labels:
    {{- include "inpatient.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | default .Values.service.httpPort }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "inpatient.selectorLabels" . | nindent 4 }}
