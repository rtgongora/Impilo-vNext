apiVersion: v1
kind: Service
metadata:
  name: {{ include "pharmacy-elmis-adapter.fullname" . }}
  labels:
    {{- include "pharmacy-elmis-adapter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | default .Values.service.httpPort }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pharmacy-elmis-adapter.selectorLabels" . | nindent 4 }}
