apiVersion: v1
kind: Service
metadata:
  name: {{ include "msika.fullname" . }}
  labels:
    {{- include "msika.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | default .Values.service.httpPort }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "msika.selectorLabels" . | nindent 4 }}
