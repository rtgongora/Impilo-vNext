apiVersion: v1
kind: Service
metadata:
  name: {{ include "msika-flow.fullname" . }}
  labels:
    {{- include "msika-flow.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | default .Values.service.httpPort }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "msika-flow.selectorLabels" . | nindent 4 }}
