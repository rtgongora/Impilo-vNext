# Integration tests: datasource + OAuth2 exclusion come from @DynamicPropertySource / @SpringBootTest properties.
spring:
  flyway:
    enabled: true
impilo:
  security:
    disable-oauth-for-tests: true
