server:
  port: 8098
  servlet:
    context-path: /

spring:
  application:
    name: inventory-elmis-adapter
  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/inventory_elmis
    username: ${POSTGRES_USER:impilo}
    password: ${POSTGRES_PASSWORD:changeme}
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        default_schema: inventory_elmis
  flyway:
    enabled: true
    locations: classpath:db/migration
    schemas: inventory_elmis
    baseline-on-migrate: true
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
    consumer:
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      group-id: inventory-elmis-adapter
      auto-offset-reset: earliest
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ""

# --- Actuator & Metrics ---
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  metrics:
    tags:
      application: inventory-elmis-adapter

# --- OpenAPI / Swagger ---
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

# --- eLMIS Adapter-specific ---
elmis:
  connector:
    mode: REST
    external-url: ${ELMIS_URL:http://localhost:9080/api}
    timeout-seconds: 30
    retry-max-attempts: 3
    retry-backoff-ms: 1000
  mapping:
    facility-code-system: urn:zw:mohcc:facility
    item-code-system: urn:zw:mohcc:item
  kafka:
    stock-receipt-topic: inventory.stock.receipt
    stock-reconcile-topic: inventory.reconcile.result
    requisition-forward-topic: elmis.requisition.forwarded
