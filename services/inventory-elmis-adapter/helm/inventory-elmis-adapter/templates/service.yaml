apiVersion: v1
kind: Service
metadata:
  name: {{ include "inventory-elmis-adapter.fullname" . }}
  labels:
    {{- include "inventory-elmis-adapter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | default .Values.service.httpPort }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "inventory-elmis-adapter.selectorLabels" . | nindent 4 }}
