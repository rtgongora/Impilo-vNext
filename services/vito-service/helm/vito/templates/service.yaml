apiVersion: v1
kind: Service
metadata:
  name: {{ include "vito.fullname" . }}
  labels:
    {{- include "vito.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | default .Values.service.httpPort }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vito.selectorLabels" . | nindent 4 }}
