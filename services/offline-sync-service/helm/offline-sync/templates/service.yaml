apiVersion: v1
kind: Service
metadata:
  name: {{ include "offline-sync.fullname" . }}
  labels:
    {{- include "offline-sync.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | default .Values.service.httpPort }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "offline-sync.selectorLabels" . | nindent 4 }}
