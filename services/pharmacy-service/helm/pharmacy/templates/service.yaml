apiVersion: v1
kind: Service
metadata:
  name: {{ include "pharmacy.fullname" . }}
  labels:
    {{- include "pharmacy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | default .Values.service.httpPort }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pharmacy.selectorLabels" . | nindent 4 }}
