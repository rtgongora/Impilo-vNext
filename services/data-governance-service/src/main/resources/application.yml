server:
  port: ${SERVER_PORT:8220}

spring:
  application:
    name: data-governance-service
  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/impilo_data_governance
    username: ${POSTGRES_USER:impilo}
    password: ${POSTGRES_PASSWORD:impilo}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: ${KEYCLOAK_URL:http://localhost:8080}/realms/${KEYCLOAK_REALM:impilo}/protocol/openid-connect/certs
          issuer-uri: ${KEYCLOAK_URL:http://localhost:8080}/realms/${KEYCLOAK_REALM:impilo}

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
  endpoint:
    health:
      show-details: when-authorized
  metrics:
    tags:
      application: data-governance-service

impilo:
  companion:
    enabled: true
  ops:
    enabled: true
    outbox-table: dgv_event_outbox
  # De-identification pipeline (docs/data-platform/de-identification-pipeline.md).
  # Per-dataset HMAC secrets are resolved by reference (deid_dataset.secret_ref)
  # under impilo.deid.secrets.<secret-ref>. Production custody is tshepo-keys:
  # values are projected from managed secrets (IMPILO_DEID_SECRETS_* env vars);
  # key material is NEVER committed here and NEVER stored in the database.
  deid:
    secrets: {}
