apiVersion: v1
kind: Service
metadata:
  name: {{ include "tuso.fullname" . }}
  labels:
    {{- include "tuso.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | default .Values.service.httpPort }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tuso.selectorLabels" . | nindent 4 }}
