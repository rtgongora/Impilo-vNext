apiVersion: v1
kind: Service
metadata:
  name: {{ include "mushex.fullname" . }}
  labels:
    {{- include "mushex.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | default .Values.service.httpPort }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mushex.selectorLabels" . | nindent 4 }}
