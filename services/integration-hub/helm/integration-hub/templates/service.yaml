apiVersion: v1
kind: Service
metadata:
  name: {{ include "integration-hub.fullname" . }}
  labels:
    {{- include "integration-hub.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | default .Values.service.httpPort }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "integration-hub.selectorLabels" . | nindent 4 }}
