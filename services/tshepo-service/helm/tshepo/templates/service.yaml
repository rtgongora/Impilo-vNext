apiVersion: v1
kind: Service
metadata:
  name: {{ include "tshepo.fullname" . }}
  labels:
    {{- include "tshepo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | default .Values.service.httpPort }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tshepo.selectorLabels" . | nindent 4 }}
