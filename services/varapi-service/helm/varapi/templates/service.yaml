apiVersion: v1
kind: Service
metadata:
  name: {{ include "varapi.fullname" . }}
  labels:
    {{- include "varapi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port | default .Values.service.httpPort }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "varapi.selectorLabels" . | nindent 4 }}
