# Helm / preview deploy env — safe defaults only
IMPILO_ENV=preview
PREVIEW_PUBLIC_HOST=41.57.127.235
POSTGRES_PASSWORD=preview-change-me
POSTGRES_USER=impilo
POSTGRES_DB=impilo_preview
# Leave empty for same-origin (browser uses relative paths routed to the BFF by Traefik).
NEXT_PUBLIC_BFF_URL=
AUTH_FALLBACK_ENABLED=true
