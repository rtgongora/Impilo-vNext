# Services that are open regardless of any bypass flag — CP8 baseline, 2026-08-02.
#
# These declare NO bypass flag, so retiring the estate's 95 declared bypasses leaves every
# one of them untouched while the report reads 'bypasses retired'. Each needs a real
# securityFilterChain, not a flag flip. Shrinking this file IS the CP8 work.
#
# Enforced by scripts/guard/check-enforcement-posture.sh in BOTH directions.
#
# ⚠️ 2026-08-03: THIS FILE IS A SOURCE-LEVEL HYPOTHESIS LIST, NOT A MEASUREMENT.
# It is the output of audit-enforcement-posture.py, which reads services/*/src/main/java and
# never touches a running pod. Probing all 16 names live found FOUR FALSE POSITIVES —
# abis-service, indawo-service, ndila-service and wellness-service all return 401 to an
# unauthenticated business request — and one entry, shared-core, is a library that is not
# deployed and cannot be probed at all. abis-service matters most: CP8 headlines it as the
# biometric-identity case.
#
# Eleven names are confirmed open by probe. Do not quote any row of this file as an exposure
# without probing it first. Evidence and method:
#   docs/security/trust-audit/checkpoint-10/CP10_MEASURED_ENFORCEMENT_POSTURE.md
#
# The names are deliberately NOT edited: the guard diffs this file against the script's output,
# so removing a false positive here would turn the guard red without changing the estate. The
# fix is to correct the classifier, then this baseline. Until then, read it as "the script says
# these are open", not "these are open".

abis-service
analytics-pipeline-service
audit-ledger-service
connector-fhir-adapter
indawo-service
iot-ingestion-service
jobs-service
matcher-engine
ndila-service
nhume-service
offline-edge-service
offline-sync-service
pharmacy-elmis-adapter
shared-core
support-service
wellness-service
