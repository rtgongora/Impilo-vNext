# Impilo vNext Technical Standards

The Technical Standards Catalogue translates the controlling architecture into reusable engineering rules. Service-specific OpenAPI, AsyncAPI, JSON Schema, FHIR profiles, Rego policies, migrations, deployment profiles and runbooks remain the executable sources for their respective implementations.

Where a technical standard conflicts with the Target Architecture, the Target Architecture prevails and the standard must be corrected before implementation proceeds.
