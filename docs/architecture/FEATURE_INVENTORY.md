# Feature inventory

> High-level surfaces; see `docs/frontend/FRONTEND_IMPLEMENTATION_STATUS.md` for maturity.

- **registry plane** — routes under `/registry` and related BFF `/internal/v1/` namespaces
- **clinical plane** — routes under `/clinical` and related BFF `/internal/v1/` namespaces
- **enterprise plane** — routes under `/enterprise` and related BFF `/internal/v1/` namespaces
- **data plane** — routes under `/data` and related BFF `/internal/v1/` namespaces
- **trust plane** — routes under `/trust` and related BFF `/internal/v1/` namespaces
- **experience plane** — routes under `/experience` and related BFF `/internal/v1/` namespaces
