# Impilo vNext — Hybrid / Federated Target Architecture (pointer)

**This is a convenience pointer, not the architecture.**

- **Active version:** **v1.3.4**
- **Status:** Working draft — **NOT architecture-frozen**; freeze requires Product Owner sign-off
- **Complete document:** [`hybrid-federated-target-architecture-v1.3.4.md`](./hybrid-federated-target-architecture-v1.3.4.md)

The versioned file is the complete, self-contained, historically durable document. There is exactly **one** complete active copy, and it is the versioned file. When a new version is cut, the new versioned file becomes active, the previous one moves to [`archive/working-drafts/`](./archive/working-drafts/) with a supersession banner, and this pointer's version, status and path are updated in the same change.

**Superseded working drafts — none of them was ever frozen**, all in `archive/working-drafts/`: **v1.3.1** (directionally accepted, never approved) · **v1.3.2** (freeze review found stop conditions) · **v1.3.3** (freeze review refused freeze on two further stop conditions: the shared-workstation personal-domain block was enforced on the input rather than the outcome, and journey 12 cited a test proving something else). Earlier versions (v1.0 `97eab3ac8` · v1.1 `ae0c9a74c` · v1.2 `e84862787` · v1.3 `5bccc366e`) live in this file's git history, which carried the full document text through v1.3.2 (`984b2d498`).

Precedence, statement classification (`[D]`/`[T]`/`[O]`/`[L]`), the pre-freeze implementation gate and the journey assurance model (§38A) are stated in the versioned document and in [`ARCHITECTURE_PRECEDENCE.md`](./ARCHITECTURE_PRECEDENCE.md).
