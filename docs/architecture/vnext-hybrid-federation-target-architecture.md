# Impilo vNext — Hybrid / Federated Target Architecture (pointer)

**This is a convenience pointer, not the architecture.**

- **Active version:** **v1.3.5**
- **Status:** Working draft — **NOT architecture-frozen**; freeze requires Product Owner sign-off
- **Complete document:** [`hybrid-federated-target-architecture-v1.3.5.md`](./hybrid-federated-target-architecture-v1.3.5.md)

The versioned file is the complete, self-contained, historically durable document. There is exactly **one** complete active copy, and it is the versioned file. When a new version is cut, the new versioned file becomes active, the previous one moves to [`archive/working-drafts/`](./archive/working-drafts/) with a supersession banner, and this pointer's version, status and path are updated in the same change.

**Superseded working drafts — not one of them was ever frozen**, all in `archive/working-drafts/`: **v1.3.1** (directionally accepted, never approved) · **v1.3.2** · **v1.3.3** · **v1.3.4** (its review found that §38A and §38B, added to close the mis-citation defect class, had reproduced it internally). Earlier versions (v1.0 `97eab3ac8` · v1.1 `ae0c9a74c` · v1.2 `e84862787` · v1.3 `5bccc366e`) live in this file's git history, which carried the full document text through v1.3.2 (`984b2d498`).

Precedence, statement classification (`[D]`/`[T]`/`[O]`/`[L]`), the pre-freeze implementation gate, the journey assurance model (§38A) and the entailment register (§38C) are stated in the versioned document and in [`ARCHITECTURE_PRECEDENCE.md`](./ARCHITECTURE_PRECEDENCE.md).
