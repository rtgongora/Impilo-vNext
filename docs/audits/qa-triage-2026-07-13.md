# QA Testing Report Triage — 2026-07-13

**Source:** VNEXT QA testing report (16 issues, deployed preview). Each issue was traced through the actual code at HEAD (UI handler → BFF route → service) by three triage lanes. Classification: **CODE-BUG** (broken at HEAD), **DEPLOY-DRIFT** (code correct at HEAD; deployed build/environment is stale or mis-configured), **DATA-SEED** (code correct; preview lacks the data the flow needs), **DESIGN-GAP** (works as coded but the design is wrong/incomplete).

## R0 execution status (2026-07-13)

**10 of 16 code-fixed, committed and pushed** to `claude/staging-ux-orchestration-remediation-Yypyl`:

- **#1** Ndila map — BFF `NdilaController` rewrites tile templates to `/internal/v1/ndila/tiles/…` (`f3915c71b`)
- **#3 / #4 / #8** shell chrome — drawer dock inset, header ghost-click gating, tab-close restructure (`992d27704`)
- **#11** encounter index route + guarded builder (`d0298fad2`)
- **#12a / #12b** Ask Nompilo top-level `result`; discussion surfaces auth errors (`e3a61638b`)
- **#13 / #14** Madi BFF passes 4xx through honestly (409 duplicate message, 404→register empty state) + auth-guard/real-message on the register form (`ae966f04a`, tests `6c4568c13`)
- **#15** Madi drive envelope unwrap + `app/madi/error.tsx` boundary (`01bc38df0`)

All covered by tests (frontend vitest 0 tsc errors; BFF `MadiControllerTest` green including new 404/409 cases).

**6 deferred (not straightforward code fixes):**
- **#5** dock popover clipping — the nav's `backdrop-filter` makes it a containing block, so no CSS escape; needs the two `<details>` menus portaled out (dedicated refactor, contested chrome).
- **#2 / #9 / #10** — touch the parallel UX session's hot files (`app-registry`, `welcome`, `shared-ui` Stage); #9/#10 are code-correct at HEAD (deploy-drift). Coordinate with that lane / re-test after a clean deploy.
- **#6 / #7 / #16** — environment/seed (share-token + OTP fixture; Keycloak `impilo-backend` service account; vito internal access-mode header + seeded aliases). Folded into the R10/G35 pre-deploy checklist.

## Verdict summary

**8 of 16 are genuine code bugs at HEAD** (all S/S–M fixes): #3, #4, #5, #8, #11, #12a, #14, #15. The rest are deploy-drift/environment/data — consistent with the known estate-drift pattern (preview build older than HEAD; internal access-mode header not injected on some paths; unseeded fixtures).

| # | Issue | Root cause | Class | Fix size |
|---|---|---|---|---|
| 1 | Ndila oversight map not loading | Tile template advertised as `/api/v1/ndila/tiles/…` but browser can only reach `/internal/v1/ndila/tiles/…`; FE synthesizes v1.1 trust headers only for the `/internal/v1/ndila/tiles/` prefix; BFF forwards the service template verbatim (no rewrite). Labels additionally never render (style declares no `glyphs` endpoint but admin layers use `text-field`). Page itself wires `NdilaMap` identically to the working `SiteRegistryGeoMapPanel` | DEPLOY-DRIFT (primary) + latent CODE gaps | M |
| 2 | Pin default behavior/appearance | No default pins (`pinnedAppCodes: []`), bare "Pins" placeholder, "taskbar" vs "dock" copy divergence, redundant toggle logic (`ShellTaskbar.tsx`, `ShellStartMenu.tsx`, `useShellStore.ts`) | DESIGN-GAP | S–M |
| 3 | Bottom nav overlaps side panel | Dock `z-[10000]` paints over drawer `z-50`; drawer reserves no `--shell-taskbar-height` inset (NavRail already does — `NavRail.tsx:75`) | **CODE-BUG** | S |
| 4 | Top action buttons visible/misbehave with panel open | Header is `z-auto`, under the drawer's `z-40` overlay: buttons show through the 35% backdrop but clicks hit the overlay (closes drawer). `AppLayout` never reads `navDrawerOpen` | **CODE-BUG** | S |
| 5 | Unresponsive bottom-nav buttons | Dock `<nav>` is `overflow-x-auto`, which forces `overflow-y: auto` → the Context and Accessibility `<details>` popovers (`bottom-full`, i.e. upward) are clipped invisible. All other dock buttons verified functional | **CODE-BUG** | S |
| 6 | Claim Share Health Record | Chain UI→BFF→vito verified correct. Needs a valid seeded share token + OTP + step-up; nothing to claim on an unseeded preview; also depends on VITO reachability. DESIGN-GAP: no in-app path to obtain a token | DATA-SEED + DEPLOY-DRIFT | — (S seed fixture) |
| 7 | Personal profile creation | `POST /internal/v1/auth/register` verified correct; needs the Keycloak `impilo-backend` service account (manage-users) + reachable Keycloak, else 503. Secondary: 12-char password policy not pre-validated client-side | DEPLOY-DRIFT / env-config | — (config; S UX) |
| 8 | Tab close (✕) inaccessible | `role="button"` span nested **inside** the chip `<button>` (invalid interactive-in-interactive; unreliable event delivery) + ~<20px hover-gated hit target (`ShellTaskbar.tsx:401-419`) | **CODE-BUG** | S–M |
| 9 | Switch Profiles / Login / Sign Up non-functional | At HEAD the welcome + PublicShell auth controls are working `<Link>`s; literal "Switch Profiles" not found in chrome. Needs repro against the deployed build | DEPLOY-DRIFT / needs-repro | S once repro'd |
| 10 | SOS text contrast | `/emergency/**` pages use fixed AA-safe colors at HEAD. Only marginal combos are `ShellSosDialog` status banners (`bg-success-soft`+`text-primary-hover`, warning pair). Probable stale build; else retune the two token pairs | DEPLOY-DRIFT / token audit | S |
| 11 | /conditions → /encounter "Page Unavailable" | `app/ehr/[patientId]/encounter/` has **no index page** (only `[encounterId]/`), and builders emit the bare path when no encounter is active (`InpatientAdmissionHandoffBanner.tsx:56` — `encounterId ?? ""`) | **CODE-BUG** | S–M |
| 12a | Impilo Live "Ask Nompilo" | Response-shape mismatch: BFF `LiveComposerController` returns top-level `{result, fallbackUsed, …}` (always 200, even on LLM fallback); UI `lib/live.ts:678-681` reads `res.data` → `undefined` → renders nothing, and hard-codes `fallbackUsed:false` | **CODE-BUG** | S |
| 12b | Discussion "Send Message" | Send box renders only after a conversation exists (`EventDiscussionPanel`: 404 → only "Start discussion"); start/send require an authenticated comms actor (`requireCommsActor` → 401 silently swallowed) | DATA-SEED + DEPLOY-DRIFT (auth) | — (config; S UX to surface 401) |
| 13 | Madi donor registration | Happy-path chain verified correct. Likely: duplicate donor for the test CPID → `IllegalStateException` → 500→502 → generic "Registration failed"; or auth not hydrated → empty `personCpid` → button permanently disabled. All failures collapse to one opaque message | DATA-SEED / env + CODE hardening | S |
| 14 | Madi donor profile fails to load | **404-to-502 masking**: non-donor → madi 404 → `MadiServiceClient.getForEntity` throws → `MadiController.proxy` returns 502 `MADI_UNAVAILABLE` → page shows an error instead of the intended "Register as a donor" empty state (which exists at `profile/page.tsx:32-39`). The citizen controller (`CitizenMadiController.forwardGet`) already degrades gracefully — web donor pages use the staff route | **CODE-BUG** | S |
| 15 | Find-a-Drive error + app unresponsive | `madiApi.listDrives`/`drivesNearMe` do **not** unwrap the BFF `{data,meta}` envelope (unlike sibling calls); the page spreads the resulting object → `TypeError: object is not iterable` on first render; no `error.tsx` boundary under `app/madi` → subtree tears down ("unresponsive"). Same latent bug silently blanks the staff `/madi/drives` list | **CODE-BUG** | S |
| 16 | Identity Resolution + Alias Rotation | Chain + payloads verified correct end-to-end. Resolve → 404 anti-enumeration on unseeded aliases (by design); Rotate is INTERNAL-only → 403 when the internal access-mode header isn't injected (the known VITO estate-drift). Minor: rotate field expects a UUID but placeholder suggests `IMP-…`; 404 vs 403 not distinguished in panel errors | DATA-SEED (resolve) + DEPLOY-DRIFT (rotate) | — (config/seed; S UX) |

## Fix plan (Wave R0 — see remediation blueprint)

**R0-A Shell chrome (code):** #5 dock popover clipping (portal/fixed-layer the two menus) → #8 tab-close restructure (sibling buttons, ≥44px target) → #3 drawer bottom inset (`--shell-taskbar-height`, the NavRail pattern) → #4 hide/disable header actions on `navDrawerOpen` → #2 default pins + copy unification (coordinate: `app-registry.ts` is a UX-lane hot file).

**R0-B Citizen journeys (code):** #11 encounter index route (redirect to active encounter or `/encounters`) + guard builders → #12a `composerAssist` reads top-level `result`/`fallbackUsed` (or BFF wraps in the standard envelope) → #12b surface the 401/"start discussion first" states.

**R0-C Madi (code):** #15 unwrap envelopes + `Array.isArray` coercion + `app/madi/error.tsx` boundary → #14 treat upstream 404 as `data:null` 200 (mirror `CitizenMadiController.forwardGet`) and render error XOR empty-state → #13 duplicate→409/idempotent + surface BFF error message + auth-hydration guard. Consider consolidating web donor pages onto the citizen controller (fixes #14, de-risks #13/#15 in one move).

**R0-D Ndila map hardening (code, M):** #1 BFF `tileConfig` rewrites templates to `/internal/v1/ndila/tiles/…`; broaden the FE transformRequest header guard; add `glyphs` (or drop text layers).

**R0-E Environment/seed (deploy lane, not code):** #6 seeded share token + OTP fixture; #7 Keycloak `impilo-backend` service account; #12b comms-actor auth propagation on `/live/event/*`; #16 internal access-mode header on BFF→vito path + seeded aliases; #9/#10 re-test after a clean no-stale fullboot deploy (both look correct at HEAD).

**Cross-check with the functional-depth register:** #16's rotate-403 is the same estate-drift as G28's verified finding; #6's token-acquisition design gap and #12b's silent-401 UX are new small entries; none of the 16 contradicts the register's journey verdicts (the walk-in/referral green paths were not implicated).
