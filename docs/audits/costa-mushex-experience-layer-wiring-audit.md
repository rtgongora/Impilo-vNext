# COSTA / MusheX — Experience layer wiring audit

> **Controlling doctrine:** The follow-up gaps recorded in *Next Gaps / Follow-up Work* below are governed by [`docs/doctrine/mushex-gateway-neutrality.md`](../doctrine/mushex-gateway-neutrality.md) (MusheX dual-mode operating doctrine) and [`docs/doctrine/costa-mushex-billing-timing.md`](../doctrine/costa-mushex-billing-timing.md) (COSTA / MusheX costing, billing-timing, and settlement separation). When an action is proposed against any gap, the doctrine defines the *target* behaviour; this audit defines the *current state* and the *surface*. If those two ever conflict, doctrine wins and the discrepancy is itself a gap.

## Architecture

- **COSTA** (`costing-engine-service`, port **8101**): canonical costing, tariff intel (`/api/costa/**`), legacy bills (`/costa/v1/**`).
- **MusheX** (`mushex-service`): payments, intents, settlement — integrated via BFF and Kafka from bill lifecycle.
- **BFF:** `CostaIntelBffController` → `http://costa/api/costa/...` with v1.2 trust headers.

## Issue & resolution (tariff library)

| Item | Before | After |
|------|--------|--------|
| `/finance/tariffs` data source | Legacy `GET /costa/v1/tariffs` only | **Primary:** `GET .../costa-intel/tariff-lists` → `costa_tariff_lists` |
| Seeded lists visible | Often **no** (empty `tariff` table) | **Yes** — grouped Default / WHO / International / Uploads / Retired |
| Reference warnings | N/A | Banner + per-row for reference-only; ZW PoC disclaimer |
| Billing / MusheX for refs | Server already blocks | UI explains; `assertCanBill` unchanged |

## Endpoints (operator checklist)

| Operation | COSTA | BFF |
|-----------|--------|-----|
| List libraries | `GET /api/costa/tariff-libraries` | `GET /internal/v1/finance/costa-intel/tariff-libraries` |
| List tariff lists | `GET /api/costa/tariff-lists` | `GET /internal/v1/finance/costa-intel/tariff-lists` |
| Cost estimate | `POST /api/costa/cost-estimate` | `POST /internal/v1/finance/costa-intel/cost-estimate` |
| Charge sheet | `POST /api/costa/charge-sheets` | same via proxy path |
| Invoice from estimate | `POST /api/costa/invoices/from-cost-estimate` | same |
| MusheX handoff | `POST /api/costa/payment-handoff` | same |

## Gaps (next)

1. **Full upload UI** (CSV/XLSX/JSON) in Experience — today: COSTA API + **COSTA** finance page partial probes.
2. **Charge sheet** end-to-end from encounter CTA (not only COSTA page).
3. **Patient summary** line for open balance / MusheX status — BFF DTO extension.

## Next Gaps / Follow-up Work

Added from the Stage 0 UX / orchestration audit (May 2026). Each item is **identified only** here; no implementation is committed by this section unless explicitly noted. Stage 1 of the audit follow-up was limited to documentation and the regenerated `/internal/v1` index. Stage 2 added bidirectional doctrine ↔ audit traceability, sidecar `DEPRECATED.md` markers (partial G-6), the wallet local-fallback documentation principle (G-5 doctrine-only), and lightweight references to this doctrine from the service catalogue. **Stage 3.1** implemented the wallet local-fallback configuration gate end-to-end for the two upstream-aware `WalletController` paths. **Stage 3.1.1** extended that same gate to the three previously synthesise-only read endpoints (`/me/balance`, `/me/transactions`, `/me/funding-sources`), closing G-5.1. **Stage 3.2** added the canonical read-only COSTA hub page at `/finance/costa` in `ui/one-ui-shell`, closing G-1. **Stage 3.3** added the canonical read-only MusheX platform admin page at `/finance/mushex-platform`, closing G-2. **Stage 3.4B** re-routed the mobile citizen wallet onto the canonical `/internal/v1/wallet/**` plane and removed three dead `/internal/v1/mobile/citizen/finance/**` calls, closing G-3.

The doctrine that *governs* every gap below is [`docs/doctrine/mushex-gateway-neutrality.md`](../doctrine/mushex-gateway-neutrality.md). Gap IDs (G-1 … G-6) are cited bidirectionally by that doctrine.

| # | Gap | Where it shows up | Doctrine reference | Class |
|---|-----|--------------------|---------------------|-------|
| G-1 | **Canonical `/finance/costa` page added to `one-ui-shell`.** **Implemented in Stage 3.2.** New page at [`ui/one-ui-shell/src/app/finance/costa/page.tsx`](../../ui/one-ui-shell/src/app/finance/costa/page.tsx) — a read-only COSTA hub that orients the user across tariff libraries, cost estimates, charge sheets (cost events), service-access decisions, MusheX invoice/payment handoff, and related finance surfaces. Reuses standard `AppLayout` / `PageShell` / `WorkflowHeader`; preserves encounter / patient / source handoff query parameters across all linked surfaces. Live signals (operational/reference tariff count, captured cost events count when scoped to an encounter/CPID, service-access decisions count when scoped to an encounter) come from existing BFF routes via `apiClient` so v1.2 trust headers are enforced: `/internal/v1/finance/costa-intel/tariff-lists`, `/internal/v1/finance/costa-intel/cost-events`, `/internal/v1/finance/service-access-decisions`. No write actions, no fabricated data — each card surfaces an honest "could not load" message when the upstream is unavailable. New hook file [`ui/one-ui-shell/src/hooks/queries/useCostaIntel.ts`](../../ui/one-ui-shell/src/hooks/queries/useCostaIntel.ts) (two thin GET hooks; the tariff-lists query key is shared with `/finance/tariffs` so the cache is reused). Existing `useServiceAccessDecisionsList` is reused as-is. The `cmd-finance-costa` start-menu command already pointed at `/finance/costa` so no app-registry change is required. Test coverage: [`ui/one-ui-shell/src/app/finance/costa/page.test.tsx`](../../ui/one-ui-shell/src/app/finance/costa/page.test.tsx) — 5 tests covering render, live counts, encounter handoff, standard shell layout, and the graceful-unavailable / no-fabrication path. | `ui/one-ui-shell/src/app/finance/costa/page.tsx`, `ui/one-ui-shell/src/hooks/queries/useCostaIntel.ts`, `ui/one-ui-shell/src/app/finance/costa/page.test.tsx` | [`docs/doctrine/health-os-doctrine.md`](../doctrine/health-os-doctrine.md); [`docs/doctrine/mushex-gateway-neutrality.md`](../doctrine/mushex-gateway-neutrality.md) (finance/MusheX handoff hub) | Implemented (Stage 3.2) |
| G-2 | **Canonical MusheX platform admin page added to `one-ui-shell`.** **Implemented in Stage 3.3.** New page at [`ui/one-ui-shell/src/app/finance/mushex-platform/page.tsx`](../../ui/one-ui-shell/src/app/finance/mushex-platform/page.tsx) — a read-only Mode A admin hub that surfaces the four GET routes on `FinanceMushexPlatformController`: `/internal/v1/finance/mushex-platform/wallets`, `/remittance-transfers`, `/card-profiles`, and `/reversals`. Live row counts are derived from a single shared envelope-extraction helper (`extractCount` in [`ui/one-ui-shell/src/hooks/queries/useMushexPlatformAdmin.ts`](../../ui/one-ui-shell/src/hooks/queries/useMushexPlatformAdmin.ts)) which tolerates top-level arrays, `{ data: [] }`, `{ items: [] }`, `{ content: [] }`, and `{ totalElements }` envelopes; unrecognised shapes render an honest "shape not recognised" state rather than a fabricated count. Hooks: `useMushexPlatformWallets`, `useMushexPlatformRemittanceTransfers`, `useMushexPlatformCardProfiles`, `useMushexPlatformReversals` (TanStack Query wrappers over `apiClient`, so v1.2 trust headers are enforced). Uses standard `AppLayout` / `PageShell` / `WorkflowHeader`; no new shell, no new sidebar, no new auth model. Reachable via the new start-menu command `cmd-finance-mushex-platform` (FINANCE role) registered in [`ui/one-ui-shell/src/lib/shell/app-registry.ts`](../../ui/one-ui-shell/src/lib/shell/app-registry.ts), and cross-linked from `/finance/costa` ("Invoice & payment handoff (MusheX)" section). Includes a "Platform routing & gateway readiness" doctrine card that describes Mode A / Mode B without inventing endpoints, and a "Related MusheX finance surfaces" tile grid linking to `/finance/costa`, `/finance/payer-ops`, `/finance/payer-claims`, `/finance/settlements`, `/finance/reconciliation`, `/finance/refunds`, `/finance/ledger`, and `/wallet`. **No write actions** are exposed (no create-wallet, no credit/debit, no create-remittance, no create-card, no create-reversal); those POST routes remain backend-only until a later stage. Tests: [`ui/one-ui-shell/src/app/finance/mushex-platform/page.test.tsx`](../../ui/one-ui-shell/src/app/finance/mushex-platform/page.test.tsx) (7 tests covering header / sections, derived counts across all four envelope shapes including empty arrays, standard `AppLayout` / `PageShell` / `WorkflowHeader` usage, explicit BFF route documentation, related-surface link set without sidecar URLs, no write-action buttons, and a "graceful unavailable states" suite that re-mocks the hooks to errors and asserts each card surfaces its own honest "Could not load … from MusheX" message). Doctrine: [`docs/doctrine/mushex-gateway-neutrality.md`](../doctrine/mushex-gateway-neutrality.md) (Mode A admin surface, now traceable to `/finance/mushex-platform`). | `ui/one-ui-shell` (canonical) — backend `FinanceMushexPlatformController` reused as-is | [`docs/doctrine/mushex-gateway-neutrality.md`](../doctrine/mushex-gateway-neutrality.md) | Implemented (Stage 3.3); write actions intentionally deferred |
| G-3 | **Mobile citizen wallet now routes through the canonical Mushe Wallet plane.** **Implemented in Stage 3.4B.** [`apps/mobile/citizen-app/src/services/walletService.ts`](../../apps/mobile/citizen-app/src/services/walletService.ts) was rewritten to call `GET /internal/v1/wallet/me` and `GET /internal/v1/wallet/me/transactions` (the canonical `WalletController` plane that proxies to Mushe Wallet); the `patientId` query parameter is gone and the wallet owner is implied by the `x-actor-id` trust header that the mobile API client already injects from the Keycloak session. The wallet payload adapter tolerates the three documented success shapes — upstream Mushe flat (`{ id, balance, currency, status }`), BFF local-fallback JSON:API (`{ id, type, attributes: { … } }`), and a defensive extra `data` wrapper — and the transactions adapter handles both a flat array (BFF local fallback) and the Spring Data Page envelope (upstream Mushe, `{ content: […] }`). [`apps/mobile/citizen-app/src/services/financeService.ts`](../../apps/mobile/citizen-app/src/services/financeService.ts) (which was previously hitting three **dead** `/internal/v1/mobile/citizen/finance/**` routes — every call was a 404 at runtime; see the Stage 3.4A audit) was re-targeted onto the same canonical plane: `fetchBalance` → `/me/balance`, `fetchTransactions` → `/me/transactions`. `fetchPendingCharges` is intentionally retained as a stable export that resolves to `[]` until a COSTA-backed pending-charges route is wired (no fabricated data). [`WalletSection`](../../apps/mobile/citizen-app/src/screens/personal/WalletSection.tsx) and [`FinanceSection`](../../apps/mobile/citizen-app/src/screens/personal/FinanceSection.tsx) now branch on the stable `WALLET_UPSTREAM_UNAVAILABLE` error code to show an honest "Wallet temporarily unavailable" `ErrorState` (with correlation id and a Retry action) instead of silently rendering `balance = 0` / a generic error. The mobile API client error helper [`ApiError.fromResponse`](../../apps/mobile/packages/mobile-api-client/src/errors.ts) was extended in the same stage to recognise the BFF v1.2 nested error envelope (`{ error: { code, message } }`) so the stable `WALLET_UPSTREAM_UNAVAILABLE` code reaches the UI instead of being collapsed to a generic `HTTP_503`; the legacy flat `{ errorCode, errorMessage }` shape is still accepted for back-compat. Tests: 10 new service-level tests in [`walletService.test.ts`](../../apps/mobile/citizen-app/src/__tests__/personal/walletService.test.ts) (route paths, no `patientId`, all three success-shape adapters, no fabricated balance on empty data, `WALLET_UPSTREAM_UNAVAILABLE` propagation through both endpoints); 6 new error-parsing tests in the existing [`mobile-api-client/test/errors.test.ts`](../../apps/mobile/packages/mobile-api-client/test/errors.test.ts) covering the BFF v1.2 envelope, the legacy flat shape, missing/empty bodies, malformed nested errors, and a real `WALLET_UPSTREAM_UNAVAILABLE` payload; and refreshed smoke tests in [`WalletSection.test.tsx`](../../apps/mobile/citizen-app/src/__tests__/personal/WalletSection.test.tsx) and [`FinanceSection.test.tsx`](../../apps/mobile/citizen-app/src/__tests__/personal/FinanceSection.test.tsx) that read the on-disk service source and assert no `apiClient.<verb>` call references the legacy mobile/citizen wallet or finance route families. The legacy `WellnessServiceProxyController` whitelist entry for `/internal/v1/mobile/citizen/wallet/**` and the legacy `CitizenMyLifeController.@GetMapping("/wallet")` endpoints in `wellness-service` were **not** removed in this stage — both remain in place so any older mobile build that ships before the rollout continues to function. Retiring them is a follow-up item below and should be done only once mobile telemetry confirms no client still hits them. | `apps/mobile/citizen-app/src/services/walletService.ts`, `apps/mobile/citizen-app/src/services/financeService.ts`, `apps/mobile/citizen-app/src/screens/personal/WalletSection.tsx`, `apps/mobile/citizen-app/src/screens/personal/FinanceSection.tsx`, `apps/mobile/packages/mobile-api-client/src/errors.ts` | [`docs/doctrine/mushex-gateway-neutrality.md`](../doctrine/mushex-gateway-neutrality.md) (wallet is part of the MusheX plane, not wellness; *Wallet local-fallback principle* — stable `WALLET_UPSTREAM_UNAVAILABLE` code) | Implemented (Stage 3.4B); legacy wellness-proxy / wellness-service wallet routes retained behind a follow-up retirement |
| G-4 | **Per-intent rail-selection / fallback doctrine implemented end-to-end.** **Implemented in Stage 3.5.** Design doc: [`docs/design/g4-rail-selection-policy.md`](../design/g4-rail-selection-policy.md) (11-section technical design covering vocabulary, configuration, domain model, deterministic algorithm, persistence strategy, dev-vs-prod safety posture, idempotency semantics, stable error codes, backward compatibility, OpenAPI delta, and test matrix). Domain additions: [`RailSelectionReason`](../../services/mushex-service/src/main/java/zw/gov/mohcc/impilo/mushex/domain/enums/RailSelectionReason.java) enum, [`RailSelectionRequest`](../../services/mushex-service/src/main/java/zw/gov/mohcc/impilo/mushex/service/rail/RailSelectionRequest.java) / [`RailSelectionResult`](../../services/mushex-service/src/main/java/zw/gov/mohcc/impilo/mushex/service/rail/RailSelectionResult.java) records, [`RailSelectionPolicy`](../../services/mushex-service/src/main/java/zw/gov/mohcc/impilo/mushex/service/rail/RailSelectionPolicy.java) SPI, [`DefaultRailSelectionPolicy`](../../services/mushex-service/src/main/java/zw/gov/mohcc/impilo/mushex/service/rail/DefaultRailSelectionPolicy.java) deterministic implementation, and a new [`MushexProperties.RailSelection`](../../services/mushex-service/src/main/java/zw/gov/mohcc/impilo/mushex/config/MushexProperties.java) config class with conservative production defaults (`enabled=true`, `defaultRail=SANDBOX`, `allowDirectGateway=false`, `allowSandboxFallback=true`). `AdapterRegistry` gained a non-throwing `has(AdapterType)` query so the policy can probe registration without exception flow. Wire contract additions: [`CreateIntentRequest`](../../services/mushex-service/src/main/java/zw/gov/mohcc/impilo/mushex/api/dto/CreateIntentRequest.java) gained optional `preferredRailAdapter` (enum), `allowFallback` (boolean), `directGatewayAllowed` (boolean) at the tail, with a backward-compatible 7-arg secondary constructor so existing positional construction in `PaymentIntentIntegrationTest` continues to compile and old JSON bodies that omit the keys continue to deserialize. `PaymentIntentController.createIntent` now builds a `RailSelectionRequest` only when at least one of the three new fields is non-null (otherwise passes `null` so service-side defaulting applies) and has a controller-local `@ExceptionHandler(RailSelectionException.class)` that maps `RAIL_ADAPTER_UNKNOWN` / `RAIL_ADAPTER_UNAVAILABLE` to HTTP 400 using the standard `ApiResponse.error(...)` envelope. Persistence: the policy is invoked inside [`PaymentIntentService.createIntent(..., RailSelectionRequest)`](../../services/mushex-service/src/main/java/zw/gov/mohcc/impilo/mushex/service/PaymentIntentService.java) (a new 8-arg overload; the legacy 7-arg method now delegates to it), the result is merged into the intent's `metadata` JSONB column under the reserved `rail_selection` key (with `selection_version=1` so future evolutions are detectable), and the `INTENT_CREATED` outbox event payload gained three keys — `effectiveRail`, `preferredRail`, `railSelectionReason` — that downstream consumers must tolerate per the platform event-versioning rule. **No schema migration.** Idempotency-replay returns the existing intent unchanged: a second `createIntent` call with the same `idempotencyKey` does not re-run selection and does not republish an event, regardless of any rail-selection inputs supplied on the second call. Impilo-simulation metadata (`impilo_simulation=true`) forces SANDBOX regardless of caller preference, preserving the existing simulation safety guarantee. Test coverage: [`DefaultRailSelectionPolicyTest`](../../services/mushex-service/src/test/java/zw/gov/mohcc/impilo/mushex/rail/DefaultRailSelectionPolicyTest.java) (13 pure-policy tests covering explicit preferred, fallback allowed, fallback disallowed by caller, fallback disallowed by deployment, unknown rail, no preference, direct-gateway requested-and-permitted, direct-gateway demoted by deployment, direct-gateway with no external rail, safety-switch off, Impilo simulation flag, case-insensitive rail name, empty-request factory); [`PaymentIntentServiceTest`](../../services/mushex-service/src/test/java/zw/gov/mohcc/impilo/mushex/PaymentIntentServiceTest.java) (8 new integration-style cases covering metadata merge + outbox keys on explicit preferred, fallback to SANDBOX in metadata, controller-level rejection on `allowFallback=false`, unknown-rail rejection without side effects, legacy 7-arg overload defaulting, Impilo-simulation forcing SANDBOX, idempotency replay does not re-run selection, outbox-key stability without caller preference); [`PaymentIntentIntegrationTest`](../../services/mushex-service/src/test/java/zw/gov/mohcc/impilo/mushex/PaymentIntentIntegrationTest.java) (3 new MockMvc cases covering happy preferred-rail end-to-end, the `RAIL_ADAPTER_UNKNOWN` HTTP 400 envelope, and backward compatibility of the legacy 7-arg JSON body). Full mushex-service test suite: **138 / 138 tests pass.** OpenAPI: [`contracts/openapi/mushex.openapi.yaml`](../../contracts/openapi/mushex.openapi.yaml) `CreateIntentRequest` gained the three optional properties with their enum / boolean shapes, and a new `RailSelection` schema documents the metadata block that downstream consumers can expect to see on the intent's `metadata` JSON and on the `INTENT_CREATED` outbox event. | `services/mushex-service` (new `service/rail/*` package, extended `PaymentIntentService` / `PaymentIntentController` / `CreateIntentRequest` / `MushexProperties` / `AdapterRegistry`, new `RailSelectionReason` enum) | [`docs/doctrine/mushex-gateway-neutrality.md`](../doctrine/mushex-gateway-neutrality.md) (Mode A ↔ Mode B switch); [`docs/design/g4-rail-selection-policy.md`](../design/g4-rail-selection-policy.md) | Implemented (Stage 3.5); no schema change; conservative production defaults preserved |
| G-5 | **Local wallet fallback in BFF is now configuration-gated.** **Implemented in Stage 3.1.** Property: `impilo.wallet.allow-local-fallback` (default `false`), bound by [`BffWalletProperties`](../../services/experience-bff/src/main/java/zw/gov/mohcc/impilo/experience/config/BffWalletProperties.java) and consumed by [`WalletController`](../../services/experience-bff/src/main/java/zw/gov/mohcc/impilo/experience/controller/WalletController.java). Behaviour: upstream success → unchanged; upstream failure + flag `false` → HTTP 503 with stable code `WALLET_UPSTREAM_UNAVAILABLE` and `meta` block (no synthetic wallet data); upstream failure + flag `true` → existing in-memory fallback plus `WARN` "WALLET FALLBACK ACTIVATED" audit log. Test coverage: [`WalletControllerTest`](../../services/experience-bff/src/test/java/zw/gov/mohcc/impilo/experience/controller/WalletControllerTest.java) (Stage 3.1 tests for `GET /me`, `POST /pay`, non-wallet methods, and validation paths; Stage 3.1.1 added 9 further tests for `/me/balance`, `/me/transactions`, `/me/funding-sources` — 19 tests in total). Stage 3.1.1 extended the same gate to the read endpoints (see G-5.1); the wallet fallback is now consistent across every `WalletController` code path that has a real upstream call available. | `services/experience-bff/.../WalletController.java`, `services/experience-bff/.../BffWalletProperties.java`, `application.yml`, `application-test.yml` | [`docs/doctrine/mushex-gateway-neutrality.md`](../doctrine/mushex-gateway-neutrality.md) (*Wallet local-fallback principle*) | Implemented (Stage 3.1) |
| G-5.1 | **`GET /me/balance`, `GET /me/transactions`, `GET /me/funding-sources` are now upstream-first and share the Stage 3.1 gate.** **Implemented in Stage 3.1.1.** Each of the three endpoints now resolves the wallet via `MusheWalletServiceClient.getWalletByOwner("PERSON", actorId)` (reusing a single shared `WalletController.lookupWalletByOwner` helper that also serves `/me`) and then calls the matching upstream operation: `getBalance(walletId)`, `getTransactions(walletId, 0, 50)`, `listFundingSources(walletId)`. Behaviour: upstream success → `JsonNode` forwarded unchanged inside `{ data, meta }`; upstream failure + flag `false` → HTTP 503 + `WALLET_UPSTREAM_UNAVAILABLE`; upstream failure + flag `true` → pre-Stage-3.1.1 in-memory shape + `WARN` "WALLET FALLBACK ACTIVATED" audit log. No public endpoint paths changed; the page/size used for `getTransactions` is an internal default (`0`/`50`) and is not exposed as a query parameter (no public contract change). **Remaining caveat:** local fallback and upstream success shapes are not byte-identical for these three endpoints (upstream is a `JsonNode`; fallback is the legacy in-memory Map / List). This asymmetry exists by design — dev/test consumers continue to receive the legacy shape they relied on — and tracking a future shape-normalisation refinement, if needed, would belong to a Stage 3.1.2 follow-up. | `services/experience-bff/.../WalletController.java` (endpoints `/me/balance`, `/me/transactions`, `/me/funding-sources`), `services/experience-bff/.../WalletControllerTest.java` | [`docs/doctrine/mushex-gateway-neutrality.md`](../doctrine/mushex-gateway-neutrality.md) (*Wallet local-fallback principle*, items 2 and 4) | Implemented (Stage 3.1.1) |
| G-6 | **Sidecar MusheX UIs need `DEPRECATED.md` markers.** Legacy `ui/mushex-finance-console`, `ui/mushex-ops-console`, `ui/mushex-payer-portal` are superseded by `one-ui-shell` finance / wallet pages. **Stage 2 status:** `DEPRECATED.md` markers were added to each of the three sidecar folders, naming the canonical replacement surfaces in `one-ui-shell` and the relevant BFF route families; the folders themselves are retained for reference/parity and have not been deleted, moved, or renamed. Net-new feature work in those sidecars remains disallowed by doctrine. Retirement (folder removal) is deferred to a later stage. | `ui/mushex-finance-console`, `ui/mushex-ops-console`, `ui/mushex-payer-portal` | [`docs/doctrine/health-os-doctrine.md`](../doctrine/health-os-doctrine.md) (one unified experience layer); [`docs/doctrine/mushex-gateway-neutrality.md`](../doctrine/mushex-gateway-neutrality.md) (canonical MusheX surfaces) | Fragmented / duplicated — markers in place, retirement pending |
| G-13 | **Phase 8 audit — observability / audit / reconciliation / operations — plus Slice 8A outbox event catalogue.** **Implemented in Phase 8 (first slice).** Audit document at [`phase-8-observability-audit-reconciliation-ops-audit.md`](./phase-8-observability-audit-reconciliation-ops-audit.md) maps the four surface families, names the gaps, and queues operational follow-ons (8B–8F). The first concrete deliverable, [`mushex-costa-outbox-event-catalogue.md`](./mushex-costa-outbox-event-catalogue.md), records — in one place — every Kafka outbox event type emitted by `mushex-service` and `costing-engine-service` (20 + 15 = 35 distinct types across two services), the Kafka topic each lands on per the two `OutboxPublisher.routeTopic(...)` switches, the originating service, the aggregate type, and (where unambiguous) the known payload-shape keys. The catalogue is generated by reading `routeTopic(...)` and the `publishEvent(...)` call-sites directly, so it cannot lie about which events the code actually emits. Five MusheX event types that currently fall through to the default `mushex.events` topic (`CLAIM_DISPUTED`, `SETTLEMENT_COMPUTED`, `REMITTANCE_REVOKED`, `LEDGER_ENTRY_POSTED`, `RECON_MATCHED`) are explicitly flagged for Slice 8B (convention test). The catalogue records, alongside intent-created payloads, the Phase 5 G-4 `rail_selection` keys (`preferred_rail_adapter`, `effective_rail`, `allow_fallback`, `direct_gateway_allowed`, `rail_selection_reason`). No code, no schema, no BFF, no UI change in this batch — pure documentation infrastructure. | `docs/audits/` (2 new files); audit register row | [`docs/audits/phase-8-observability-audit-reconciliation-ops-audit.md`](./phase-8-observability-audit-reconciliation-ops-audit.md); [`docs/audits/mushex-costa-outbox-event-catalogue.md`](./mushex-costa-outbox-event-catalogue.md); [`docs/doctrine/mushex-gateway-neutrality.md`](../doctrine/mushex-gateway-neutrality.md) | Implemented (Phase 8, Slice 8A); 8B (convention test), 8C (ops runbook), 8D (audit aggregate-filter), 8E (triple-source reconciliation), 8F (consumer map) queued |
| G-12 | **Retirement-readiness ledger + telemetry-signals catalogue.** **Implemented in Phase 7 (first slice).** Central ledger lands at [`docs/retirement/retirement-readiness-ledger.md`](../retirement/retirement-readiness-ledger.md) — seven entries (`RR-01`..`RR-07`) covering every outstanding deprecation in the repository: three MusheX sidecars (`ui/mushex-finance-console`, `ui/mushex-ops-console`, `ui/mushex-payer-portal`), two legacy web shells (`ui/experience`, `ui/ehr`), the legacy mobile-citizen wallet routes (G-3 retirement tail), and the `costa-console` catalog row. Each entry carries a stable id, a canonical replacement, observable retirement criteria, current evidence, and current blockers. A four-state status taxonomy (`flagged` → `awaiting-evidence` → `evidence-clean` → `retired`) defines the promotion path with required artefacts at each step. Companion [`docs/retirement/telemetry-signals.md`](../retirement/telemetry-signals.md) names the five telemetry signals (`SIDECAR_UI`, `LEGACY_BFF_ROUTE`, `LEGACY_WEB_SHELL`, `CANONICAL_PARITY`, `CI_BUILD_REMOVED`) each ledger criterion depends on, their aggregation windows, their thresholds, their false-positive risks, and a per-entry signal map. Each of the five existing `DEPRECATED.md` files ([`ui/mushex-finance-console`](../../ui/mushex-finance-console/DEPRECATED.md), [`ui/mushex-ops-console`](../../ui/mushex-ops-console/DEPRECATED.md), [`ui/mushex-payer-portal`](../../ui/mushex-payer-portal/DEPRECATED.md), [`ui/experience`](../../ui/experience/DEPRECATED.md), [`ui/ehr`](../../ui/ehr/DEPRECATED.md)) is cross-linked to its ledger entry and to the telemetry-signals doc. The ledger explicitly lists the next operational slices (7B–7F) that wire dashboards / BFF inbound counters / CI guards — each independently scoped, each low-risk, each out of scope for this audit-and-ledger batch. No code, no schema, no BFF, no UI change in this batch — pure documentation infrastructure. | `docs/retirement/` (2 new files); 5 `DEPRECATED.md` cross-links | [`docs/retirement/retirement-readiness-ledger.md`](../retirement/retirement-readiness-ledger.md); [`docs/retirement/telemetry-signals.md`](../retirement/telemetry-signals.md); [`docs/doctrine/mushex-gateway-neutrality.md`](../doctrine/mushex-gateway-neutrality.md) | Implemented (Phase 7, ledger established); Slices 7B–7F (dashboards, BFF inbound counters, CI guards, fresh parity audits) queued |
| G-11 | **Phase 6 — Claims / Coverage / Remittances / Subsidies surface coherence.** **Implemented for Slices 6A–6D; Slice 6E deferred.** The prior audit+hooks slice remains in place (`useCoverageContributionsList`, `useCoveragePreauthsList`, `useCoverageUtilizationList`, `useCoverageAppealsList` in [`useCoverage.ts`](../../ui/one-ui-shell/src/hooks/queries/useCoverage.ts), with `coverageListRows` envelope tolerance). **Slice 6B** now wires those hooks into the existing `/coverage` tabs: preauth, contributions, appeals, and intelligence render read-only table rows over the existing BFF routes (`/internal/v1/coverage/preauths`, `/contributions`, `/appeals`, `/utilization`) with explicit loading/error/empty states and no fabricated metrics. **Slice 6C** applies a claims coherence pass: `/finance/claims` and `/finance/claims/[id]` now include a direct handoff to `/finance/payer-claims`, and `/finance/payer-claims/[claimId]` gates submit/dispute behind explicit operator confirmations while keeping the same existing endpoints (`POST /internal/v1/finance/payer-claims/{id}/submit`, `POST /.../dispute`). **Slice 6D** lands a canonical read-only remittances hub at [`/finance/remittances`](../../ui/one-ui-shell/src/app/finance/remittances/page.tsx) over `GET /internal/v1/coverage/remittances`, linked from `/finance` and registered in central route metadata/parity (`routes.ts`, `route-parity-check.mjs`). **Slice 6E** remains deferred: subsidies still have no first-class backend representation in this layer and need product + API definition before safe UI exposure. | `ui/one-ui-shell` (`/coverage` tab wiring + tests, remittances page + test, claims coherence updates, route registry/parity updates) | [`phase-6-claims-coverage-remittance-subsidy-audit.md`](./phase-6-claims-coverage-remittance-subsidy-audit.md); [`docs/doctrine/mushex-gateway-neutrality.md`](../doctrine/mushex-gateway-neutrality.md); [`docs/architecture/experience-bff-downstream-route-map.md`](../architecture/experience-bff-downstream-route-map.md) | Phase 6 implemented (Slices 6A–6D); Slice 6E subsidies deferred pending product/backend decision |
| G-10 | **COSTA workflow maturity — encounter timeline plus controlled actions.** **Implemented in Phase 5 (complete).** The timeline page [`/finance/costa/encounter/[encounterId]`](../../ui/one-ui-shell/src/app/finance/costa/encounter/%5BencounterId%5D/page.tsx) merges encounter-scoped service-access decisions, cost events, invoice lifecycle rows, MusheX source-list intents, and intent-filtered settlement headers. Additive read endpoints landed across both services: COSTA `GET /costa/v1/finance/lifecycle/invoices?encounter_id=...` (BFF passthrough `GET /internal/v1/finance/billing-workspace/lifecycle/invoices?encounterId=...`), MusheX `GET /mushex/v1/payment-intents?source_type=...&source_ids=...` (BFF passthrough `GET /internal/v1/finance/payer-ops/payment-intents?sourceType=...&sourceIds=...`), and MusheX `GET /mushex/v1/settlements?intent_ids=...` (BFF passthrough `GET /internal/v1/finance/settlements?intentIds=...`). Phase 5 controlled writes are now surfaced on [`/finance/costa`](../../ui/one-ui-shell/src/app/finance/costa/page.tsx) using existing endpoints with explicit operator confirmation + required reason capture: `POST /internal/v1/finance/service-access-decisions` and `POST /internal/v1/finance/costa-intel/invoices/from-cost-estimate`. | `services/costing-engine-service` (encounter invoice lifecycle read), `services/mushex-service` (source-list intents + intent-filter settlements), `services/experience-bff` (additive payer-ops/settlement passthroughs), `ui/one-ui-shell` (timeline fan-in + controlled actions + tests) | [`docs/doctrine/mushex-gateway-neutrality.md`](../doctrine/mushex-gateway-neutrality.md); [`docs/design/phase-5-costa-encounter-timeline.md`](../design/phase-5-costa-encounter-timeline.md) | Implemented (Phase 5 complete) |
| G-9 | **MusheX platform read-to-detail — full read-only detail implemented.** **Implemented in Phase 4 (first slice + follow-on).** The original wallet drill-down at [`/finance/mushex-platform/wallets/[walletId]`](../../ui/one-ui-shell/src/app/finance/mushex-platform/wallets/%5BwalletId%5D/page.tsx) remains in place and now consumes a new per-id identity endpoint `GET /internal/v1/finance/mushex-platform/wallets/{walletId}` in addition to transactions/card links. The follow-on slice adds additive per-id read routes on MusheX + BFF for the three previously deferred entity families — remittance transfers (`GET .../remittance-transfers/{transferId}`), card profiles (`GET .../card-profiles/{cardProfileId}`), and reversals (`GET .../reversals/{reversalId}`) — and three new canonical read-only pages in `one-ui-shell`: [`/finance/mushex-platform/remittance/[transferId]`](../../ui/one-ui-shell/src/app/finance/mushex-platform/remittance/%5BtransferId%5D/page.tsx), [`/finance/mushex-platform/cards/[cardId]`](../../ui/one-ui-shell/src/app/finance/mushex-platform/cards/%5BcardId%5D/page.tsx), and [`/finance/mushex-platform/reversals/[reversalId]`](../../ui/one-ui-shell/src/app/finance/mushex-platform/reversals/%5BreversalId%5D/page.tsx). The hub page now exposes four read-only "Open by ID" forms (wallet, remittance, card, reversal). No write actions were added. `useMushexPlatformAdmin.ts` gains additive query hooks for all four per-id endpoints plus `extractRecord(...)`; existing `extractRows(...)` remains unchanged. Route registry adds three new finance routes; `EXPECTED_ROUTE_COUNT` is increased accordingly and route-parity list updated. Safety remains unchanged: no `post/put/patch/delete` calls on the new pages, no schema migration, no trust-header/auth bypass, and no fabricated data (empty/unavailable states are explicit). | `services/mushex-service` (new per-id GETs for wallets/remittance/card/reversal + `PlatformResourceNotFoundException` 404 mapping), `services/experience-bff` (matching transparent per-id passthroughs), `ui/one-ui-shell` (3 new detail pages + 4 per-id hooks + hub lookup forms + route registry/parity updates) | [`docs/doctrine/mushex-gateway-neutrality.md`](../doctrine/mushex-gateway-neutrality.md); [`docs/design/phase-4-mushex-platform-detail.md`](../design/phase-4-mushex-platform-detail.md) | Implemented (Phase 4 full read-to-detail); write actions remain explicitly deferred behind safety design |
| G-8 | **Attempt-time rail enforcement — implemented in full, plus follow-on batch.** **Phase 3 + follow-on status:** every risk and remaining backlog item from the initial Phase 3 batch is resolved. New runtime guard: `PaymentRailAdapter.liveCapable()` (defaults to `false`; all four adapters today are stubs) is now the third leg of the safety gate — an operator who accidentally flips both `mushex.adapters.<rail>.{enabled,credentials-configured}` on still cannot trigger a real provider call until the adapter is upgraded with a real client. New controlled-reselect endpoint `POST /mushex/v1/payment-intents/{id}/attempts/reselect` re-runs `RailSelectionPolicy` and preserves the previous decision on `metadata.rail_selection.history[]`; emits a new `ATTEMPT_RESELECTED` outbox event. New read endpoint `GET /mushex/v1/payment-intents/{id}/attempts` returns the chronological attempt history. Class-level `@ExceptionHandler(IntentNotFoundException)` retrofits `GET /payment-intents/{id}` (previously returned 500 on missing id) to a clean `404 INTENT_NOT_FOUND`. New BFF passthroughs on [`PayerOpsController`](../../services/experience-bff/src/main/java/zw/gov/mohcc/impilo/experience/controller/PayerOpsController.java) for `attempts/reselect` and the attempts-list endpoint, and a minimal sandbox-safe UI trigger surface lives on [`/finance/payer-ops`](../../ui/one-ui-shell/src/app/finance/payer-ops/page.tsx) (initiate-attempt button with confirmation modal, reselect-rail button, attempts-history panel) backed by new TanStack hooks `usePayerOpsInitiateAttempt`, `usePayerOpsReselect`, and `usePayerOpsAttempts`. **Original Phase 3 implementation:** the previously-deferred attempt-creation flow now exists end-to-end in `services/mushex-service` and is wired through `experience-bff`. New service [`PaymentAttemptService`](../../services/mushex-service/src/main/java/zw/gov/mohcc/impilo/mushex/service/PaymentAttemptService.java) creates attempts on `POST /mushex/v1/payment-intents/{id}/attempts` (new endpoint on [`PaymentIntentController`](../../services/mushex-service/src/main/java/zw/gov/mohcc/impilo/mushex/api/PaymentIntentController.java)); new pure helper [`RailEnforcement`](../../services/mushex-service/src/main/java/zw/gov/mohcc/impilo/mushex/service/rail/RailEnforcement.java) reads `payment_intents.metadata.rail_selection.effective_rail`, resolves it to an `AdapterType`, throws `RAIL_ADAPTER_UNAVAILABLE_AT_ATTEMPT` (422) when the adapter is unregistered, defaults legacy intents (no metadata) to `mushex.rail-selection.default-rail` (SANDBOX by default), and forces SANDBOX when `metadata.impilo_simulation=true` (`SIMULATION_LOCK`). A conservative **safety gate** prevents `PaymentRailAdapter.initiatePayment(...)` from being called for `MOBILE_MONEY` / `BANK_TRANSFER` / `CARD_GATEWAY` unless **both** `mushex.adapters.<rail>.enabled` and `credentials-configured` are true — both default to `false` so production deployments never call a real rail by accident; when blocked, the attempt is persisted with `status=INITIATED` and `enforcement_metadata.safety_gate=BLOCKED_PRE_LIVE` (no adapter call, no fake adapterRef). Idempotency: optional `idempotencyKey` from JSON body or `X-Idempotency-Key` header — replays return the existing attempt with no second adapter call and no second outbox event; enforced at the DB level via a partial unique index on `(intent_id, idempotency_key)`. Schema migration [`V006__payment_attempt_idempotency_and_enforcement.sql`](../../services/mushex-service/src/main/resources/db/migration/V006__payment_attempt_idempotency_and_enforcement.sql) adds two additive nullable columns to `mushex_payment_attempts` (`idempotency_key VARCHAR(120)` + `enforcement_metadata JSONB`); existing rows remain valid. Outbox: two new event types `ATTEMPT_INITIATED` and `ATTEMPT_FAILED_PRE_INITIATION` route to dedicated topics `mushex.payment.attempt.initiated` / `mushex.payment.attempt.failed` via [`OutboxPublisher.routeTopic`](../../services/mushex-service/src/main/java/zw/gov/mohcc/impilo/mushex/kafka/OutboxPublisher.java); payloads carry `intentId`, `attemptId`, `effectiveRail`, `preferredRail`, `enforcementReason`, `safetyGate`, `legacyIntent`, `idempotencyKey`. BFF: one new passthrough `POST /internal/v1/finance/payer-ops/payment-intents/{intentId}/attempts` on [`PayerOpsController`](../../services/experience-bff/src/main/java/zw/gov/mohcc/impilo/experience/controller/PayerOpsController.java), forwarding body and the `X-Idempotency-Key` header. **No UI surface change** — the existing `/finance/payer-ops` UX continues to be the entry point; a dedicated trigger UI is queued behind a separate UX design. Backward compatibility: `CreateIntentRequest`, webhook controller, `PaymentRailAdapter` SPI and `AdapterRegistry` are unchanged. The full implementation design lives at [`docs/design/phase-3-attempt-time-rail-enforcement-implementation.md`](../design/phase-3-attempt-time-rail-enforcement-implementation.md); the original audit-only doc [`docs/design/phase-3-attempt-time-rail-enforcement.md`](../design/phase-3-attempt-time-rail-enforcement.md) is preserved as historical context. Tests cover every test-matrix row: 15 in [`RailEnforcementTest`](../../services/mushex-service/src/test/java/zw/gov/mohcc/impilo/mushex/rail/RailEnforcementTest.java), 11 in [`PaymentAttemptServiceTest`](../../services/mushex-service/src/test/java/zw/gov/mohcc/impilo/mushex/PaymentAttemptServiceTest.java), 10 in [`PaymentAttemptIntegrationTest`](../../services/mushex-service/src/test/java/zw/gov/mohcc/impilo/mushex/PaymentAttemptIntegrationTest.java). | `services/mushex-service` (new `service/PaymentAttemptService`, `service/rail/RailEnforcement*`, `api/dto/CreateAttemptRequest`, V006 migration, additive entity/repo fields, new outbox routes); `services/experience-bff` (one new BFF passthrough on `PayerOpsController`, one new client method on `MushexServiceClient`) | [`docs/design/phase-3-attempt-time-rail-enforcement-implementation.md`](../design/phase-3-attempt-time-rail-enforcement-implementation.md); [`docs/doctrine/mushex-gateway-neutrality.md`](../doctrine/mushex-gateway-neutrality.md); [`docs/audits/mushex-costa-outbox-event-catalogue.md`](./mushex-costa-outbox-event-catalogue.md) | Implemented (Phase 3, full slice); UI trigger surface and controlled-retry (reselection) endpoint remain explicitly out of scope and queued behind separate UX/design |
| G-7 | **Adapter readiness model — read-only operator visibility.** **Implemented in Phase 2.** Design: [`docs/design/phase-2-adapter-readiness.md`](../design/phase-2-adapter-readiness.md). New deterministic readiness service [`AdapterReadinessService`](../../services/mushex-service/src/main/java/zw/gov/mohcc/impilo/mushex/service/rail/AdapterReadinessService.java) computes a state per `AdapterType` (`NOT_REGISTERED` / `DISABLED` / `READY_SANDBOX` / `CREDENTIALS_MISSING` / `READY_LIVE`; `DEGRADED` reserved) from `AdapterRegistry` presence and `MushexProperties` config only — **never reads payment-provider credentials, never opens a connection to a provider, never moves money**. New domain enum [`AdapterReadinessStatus`](../../services/mushex-service/src/main/java/zw/gov/mohcc/impilo/mushex/domain/enums/AdapterReadinessStatus.java) and read-only record [`AdapterReadiness`](../../services/mushex-service/src/main/java/zw/gov/mohcc/impilo/mushex/service/rail/AdapterReadiness.java). Configuration: three new conservative nested classes under `MushexProperties.Adapters` (`mobileMoney`, `bankTransfer`, `cardGateway`) carry only `enabled` and `credentialsConfigured` booleans (operator self-attestation; not credentials). Production defaults: every real-money rail is `enabled=false` / `credentialsConfigured=false`, so the default posture is zero `liveCapable` rails. New backend endpoint [`AdapterReadinessController`](../../services/mushex-service/src/main/java/zw/gov/mohcc/impilo/mushex/api/AdapterReadinessController.java) at `GET /mushex/v1/platform/adapter-readiness` returning `ApiResponse<List<AdapterReadiness>>`. BFF passthrough: extended [`MushexServiceClient.platformAdapterReadiness`](../../services/experience-bff/src/main/java/zw/gov/mohcc/impilo/experience/client/MushexServiceClient.java) and a new `GET /internal/v1/finance/mushex-platform/adapter-readiness` on [`FinanceMushexPlatformController`](../../services/experience-bff/src/main/java/zw/gov/mohcc/impilo/experience/controller/FinanceMushexPlatformController.java) (mirrors the seven existing passthroughs; same `FinancePlaneAuthorizationService` gate). UX: new TanStack Query hook [`useMushexPlatformAdapterReadiness`](../../ui/one-ui-shell/src/hooks/queries/useMushexPlatformAdmin.ts) and a read-only table on [`/finance/mushex-platform`](../../ui/one-ui-shell/src/app/finance/mushex-platform/page.tsx) that replaces the previous static "Platform routing & gateway readiness" copy. No write buttons, no fabricated rows. The `PaymentRailAdapter` SPI is unchanged; the four existing adapters are not edited. Test coverage: [`AdapterReadinessServiceTest`](../../services/mushex-service/src/test/java/zw/gov/mohcc/impilo/mushex/rail/AdapterReadinessServiceTest.java) — 9 unit tests covering every state branch (registered/not registered, sandbox enabled/disabled, real-money default → disabled, enabled-without-creds → credentials-missing, enabled-with-creds → live, default-posture snapshot, no-exception over all enabled × credsConfigured combinations). Full `services/mushex-service` test suite: **147 / 147 tests pass.** | `services/mushex-service` (new `domain/enums/AdapterReadinessStatus`, `service/rail/AdapterReadiness*`, `api/AdapterReadinessController`, extended `MushexProperties`); `services/experience-bff` (one new passthrough); `ui/one-ui-shell` (one new hook, one new page panel) | [`docs/doctrine/mushex-gateway-neutrality.md`](../doctrine/mushex-gateway-neutrality.md) (gateway-neutral by design, gateway-capable by default); [`docs/design/phase-2-adapter-readiness.md`](../design/phase-2-adapter-readiness.md) | Implemented (Phase 2); credentials never read, no live probing, conservative production defaults |

These gaps are tracked here so subsequent stages can address them without re-deriving the underlying analysis. Stages 2+ must each cite the relevant gap ID(s) above. Stage 2 closed G-6 to the *marker* level only and progressed G-5 to the *documented-principle* level only. Stage 3.1 closed G-5 to the *implemented* level for the two upstream-aware wallet paths and opened the narrower G-5.1 to track the three remaining synthesise-without-upstream endpoints. **Stage 3.1.1** then closed G-5.1 by adding upstream-first behaviour and the same gate to all three read endpoints, so the wallet local-fallback contract is now uniform across `WalletController`. **Stage 3.2** closed G-1 by adding the canonical `/finance/costa` page in `one-ui-shell`; the `cmd-finance-costa` start-menu command and the `/finance/tariffs` "COSTA tools" action now resolve to a real page instead of returning 404. **Stage 3.3** closed G-2 by adding the canonical `/finance/mushex-platform` page in `one-ui-shell` (read-only Mode A admin hub over `FinanceMushexPlatformController`); the new `cmd-finance-mushex-platform` start-menu command and the cross-link from `/finance/costa` both resolve to the new page. **Stage 3.4B** closed G-3 by re-routing the mobile citizen wallet onto the canonical `/internal/v1/wallet/**` plane, retiring three dead `/internal/v1/mobile/citizen/finance/**` calls, and teaching the mobile API client to preserve the BFF's stable `WALLET_UPSTREAM_UNAVAILABLE` error code; the legacy `WellnessServiceProxyController` whitelist for `/internal/v1/mobile/citizen/wallet/**` and the legacy `CitizenMyLifeController` wallet endpoints in `wellness-service` were intentionally left in place behind a follow-up retirement so older mobile builds keep working until rollout telemetry confirms no client is still on them. **Stage 3.5** closed G-4 by introducing the `RailSelectionPolicy` SPI, a deterministic default implementation, and three optional `CreateIntentRequest` fields (`preferredRailAdapter`, `allowFallback`, `directGatewayAllowed`), with conservative production defaults (`defaultRail=SANDBOX`, `allowDirectGateway=false`) and no database schema change — the rail-selection result is persisted on the existing `payment_intents.metadata` JSONB column under a reserved `rail_selection` key and surfaced on the `INTENT_CREATED` outbox event via three new payload keys. Remaining open gaps after Stage 3.5 are the *retirement* (folder-removal) tail of **G-6** and the *legacy-route retirement* tail of **G-3** (removing the wellness-proxy whitelist entry and the `wellness-service` wallet endpoints once mobile clients have updated). Both are deliberately deferred until telemetry/compatibility evidence supports them.

## Related

- `services/costing-engine-service/.../V007__costa_tariff_intel_vertical_slice.sql`
- `V010__costa_tariff_operational_upload_and_retired_seed.sql`
- `shared-ui/lib/finance/tariff-library-groups.ts` — UI grouping
- [`docs/doctrine/mushex-gateway-neutrality.md`](../doctrine/mushex-gateway-neutrality.md) — MusheX dual-mode operating doctrine (orchestration vs. direct/default gateway).
- [`docs/architecture/experience-bff-internal-routes.md`](../architecture/experience-bff-internal-routes.md) — Generated `/internal/v1` controller index (regenerate via `scripts/bff-routes/list-bff-internal-routes.mjs`).
