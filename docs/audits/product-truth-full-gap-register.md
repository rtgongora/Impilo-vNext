# Product Truth — Full Gap Register

> Master tracker for the full gap-discovery pass (2026-06-23). The automated
> product-truth **gate** enforces only the regex-detectable subset (`gapBaseline`
> in `reports/product/product-truth-baseline.json`, currently 6). This register
> tracks the **complete** set, including semantic security/trust/clinical/legal
> defects that compile, persist, and return success — which no scanner can detect.
>
> **Methodology:** four-way structured read-only sweep (backend a–l, backend m–z,
> experience-bff + mobile, one-ui-shell) + widened deterministic detectors. Each row
> has a stable ID, file:line, category, severity, a **decision**
> (fix / honest-degrade / defer-with-marker / reject) and the **roadmap block**
> that closes it (see the remediation roadmap plan).
>
> **Honest counts:** deterministic gate = **6**. Full register = **~59 findings (G001–G055)**:
> ~15 blocker/critical, ~14 high, ~20 medium, ~9 low. The gap between 6 and ~58 is
> the finding — the scanner is honest about its *method* but incomplete in *coverage*.

## CRITICAL / BLOCKER — security · trust · clinical · legal

| ID | Finding | file:line | Cat | Decision | Block |
|----|---------|-----------|-----|----------|-------|
| G001 | ~~Step-up accepts **any non-blank code**~~ **DEFECT FIXED; UNIT-PROVEN (B1, `276da9db`) + RUNTIME-PROVEN for the supervisor-approval path (fix-d):** mode-dispatched verification, no accept-any path; `StepUpServiceTest` 14/14, **and** `StepUpVerificationIT` drives issue→approve→verify, fail-closed-without-approval, lockout, and replay end-to-end against a **real Postgres** (full context boot + all 13 migrations). The runtime proof additionally surfaced and fixed [[G060]] (migration collision) and [[G061]] (lockout/audit rollback). **Maturity: REAL_PROVEN for SUPERVISOR_APPROVAL; the other modes remain unit-only/seams** — TOTP fails closed until an enrolment-secret store exists; SMS-OTP (ties G033) + biometric are `NOT_LIVE_CAPABLE` adapters; no controller endpoint yet (see [[G058]]). Feature still **not operationally complete**. | `tshepo-authz-service/.../stepup/*`, `StepUpService.java`, `StepUpVerificationIT.java` | unimpl/crypto | defect fixed; supervisor path runtime-proven; seams open | B1 |
| G002 | ~~Offline capability token **JWS signature never verified**~~ **DEFECT FIXED + UNIT-PROVEN (B2, `fefe6422`):** real Ed25519 JWS verification (alg-allowlist + kid + signature + exp/nbf) before any claim is trusted; `CapabilityTokenJwsVerifierTest` 6/6 + service 16/16. **NOT runtime-proven** (JWKS + repo mocked; GoldenContractIT needs Postgres). **Residuals:** verification fetches JWKS **online per verify** → **not yet truly offline** (needs cached/distributed JWKS); issuer/audience/scope claim checks **not** done (only alg/kid/sig/exp/nbf + DB tenant); no integration proof. Maturity: **unit-proven, not REAL_PROVEN**. | `tshepo-offline-service/.../CapabilityTokenJwsVerifier.java`, `CapabilityTokenService.java` | crypto | defect fixed; offline+claims open | B2 |
| G003 | ~~DAGS permit-token HMAC key defaulted to literal **"change-me-in-prod"** — forgeable if env unset~~ **FIXED + RUNTIME-PROVEN (B3).** Fails closed on a blank/default key; **v2 token binds the full access context** — tenant, requester (SHA-256), requestId, resourceType, resourceId, purpose, issued/exp, unique nonce — under a full-length HMAC-SHA256. Requester is server-side (`approve()` reads the persisted entity). Verify path + replay + structured audit added (see [[G056]]). Proven by `EnforcementServiceTest` (13 cases, real HMAC, every reject reason) + `PermitEnforcementRuntimeProofIT` (real Postgres). | `data-access-governance-service/.../EnforcementService.java` | hardcoded/crypto | fixed; runtime-proven | B3 |
| G056 | ~~**DAGS permit token signature is never verified by any consumer**~~ **FIXED + RUNTIME-PROVEN (B3).** `EnforcementService.verifyAndConsume(token, context)` checks the signature in **constant time**, expiry, and every bound claim against the enforcing caller's context, then **consumes the nonce (single-use replay protection)** via a new `permit_replay` table; a specific `Reason` is returned for each failure. New `PermitEnforcementController` (`POST /internal/v1/permits/enforce`) is the consumer — tenant from server-side `TrustContext`, outcome audited. The signature is no longer decorative: forged/tampered/replayed tokens are rejected. Proven end-to-end against real Postgres (`PermitEnforcementRuntimeProofIT`: verify→replay-reject, tamper-reject). | `data-access-governance-service/.../core/EnforcementService.java`, `.../api/PermitEnforcementController.java` | unverified-signature | fixed; runtime-proven | B3 |
| G004 | ~~Identity-assurance **risk assessment is a client-trusted pass-through**~~ **FIXED + UNIT-PROVEN (C1).** The verdict (score/level/recommendation) is computed server-side by the new `RiskEngine` from the reported signals; the request no longer accepts a verdict. `RiskEngineTest` (8, real logic) + service capture tests. | `identity-assurance-service/.../core/RiskEngine.java`, `RiskAssessmentService.java` | unimpl | fixed | C1 |
| G005 | ~~Null defaults fabricate a verdict: omitted fields → score 0 / LOW / **ALLOW**~~ **FIXED (C1).** No client verdict to default; an empty signal set is a *computed* LOW/ALLOW (correct), not a fabricated one — and no signal can be overridden to ALLOW by the caller. | `identity-assurance-service/.../core/RiskEngine.java` | hardcoded | fixed | C1 |
| G006 | ~~Identity-assurance **attestation client-trusted** — caller self-asserts `VERIFIED, confidence=1.0`~~ **FIXED + UNIT-PROVEN (C1).** `recordAttestation` no longer accepts outcome/confidence; a claim with evidence is captured **PENDING** (awaiting a real verifier), no evidence is **FAILED** — never client-asserted VERIFIED. confidence is 0 until a verifier sets it (fail-closed seam). | `identity-assurance-service/.../core/AttestationService.java` | unimpl | fixed | C1 |
| G007 | ~~FHIR gateway **never forwards** — returns SUCCESS, no HTTP call to BUTANO/HAPI; payload dropped~~ **FIXED + PROVEN (F1).** New `FhirForwarder` actually POSTs/PUTs the payload to the route's target endpoint (over the trust-header-forwarding RestTemplate); `GatewayForwardService` records `SUCCESS` only on a real 2xx, else `FORWARD_FAILED` (fail-closed on non-2xx/transport error). Proven by `FhirForwarderTest` (3, MockRestServiceServer: 2xx delivered, 4xx/5xx not) + `GatewayForwardServiceTest` (2: SUCCESS only when delivered, FORWARD_FAILED otherwise — no fabricated SUCCESS). | `fhir-gateway-service/.../core/FhirForwarder.java`, `GatewayForwardService.java` | write-no-persist | fixed; proven | F1 |
| G008 | ~~Payment `verifyWebhook` returns true for **any non-blank signature** (no HMAC)~~ **FIXED + PROVEN (E1).** The three real rail adapters (MobileMoney, BankTransfer, CardGateway) now delegate to the existing `WebhookSecurityService` (real HMAC-SHA256, constant-time, configured secret) instead of the `signature != null && !isBlank` stub. SandboxMockAdapter stays (labelled mock). Proven by `AdapterWebhookVerificationTest` (4, real HMAC): forged/blank/tampered rejected, valid accepted, across all 3 adapters. | `mushex-service/.../adapter/{MobileMoney,BankTransfer,CardGateway}Adapter.java` | crypto | fixed; proven | E1 |
| G009 | ~~BFF IdentityAssurance GET `/status` **fully fabricated** (no client injected)~~ **FIXED (C2a).** Now a thin proxy over identity-assurance-service via the new `IdentityAssuranceServiceClient`; returns the real per-actor level + AssurancePolicy-derived permissions/pathways (no hardcoded LOA2). | `experience-bff/.../IdentityAssuranceController.java`, `.../client/IdentityAssuranceServiceClient.java` | hardcoded | fixed | C2a |
| G010 | ~~BFF IdentityAssurance POST `/upgrade/request` mints fake id, **persists nothing**~~ **FIXED (C2a).** Proxies the real upgrade-request workflow (persisted PENDING + reviewed approve/reject in identity-assurance-service, runtime-proven). | `experience-bff/.../IdentityAssuranceController.java` | write-no-persist | fixed | C2a |
| G011 | ~~BFF TempIdReview queue **fabricated**~~ **RESOLVED — DEAD DUPLICATE REMOVED (C2b).** The fabricated `TempIdReviewController` (`/internal/v1/admin/temp-id-review`, hardcoded `TMP-2026-*`) was a redundant duplicate: the **real** provisional-ID review is already wired end-to-end — VITO owns provisional IDs (`ProvisionalIdEntity`, `IdentityStatus.PROVISIONAL`) → `VitoClientRegistryBffController` `/internal/v1/vito/client-registry` (list `?status=PROVISIONAL` = the queue) → UI hooks `useVitoClientRegistry`/`useIdentityOperations`. No UI/service called the fabrication. Deleted it (+ its SecurityConfig entry) rather than build a second proxy (guardrail: no duplicate SoR functionality). | `experience-bff/.../controller/TempIdReviewController.java` (removed); real: `.../VitoClientRegistryBffController.java` | hardcoded → removed | resolved (removed) | C2b |
| G012 | ~~BFF TempIdReview approve/reject **persist nothing**, no VITO promotion~~ **RESOLVED — DEAD DUPLICATE REMOVED (C2b).** Same disposition as G011: the real approve/reject is VITO `POST /v1/client-registry/clients/{healthId}/verification-reviews` (proxied by `VitoClientRegistryBffController.createVerificationReview`), which persists and drives the real promotion. The fabricated persist-nothing endpoint was removed. | `experience-bff/.../controller/TempIdReviewController.java` (removed) | write-no-persist → removed | resolved (removed) | C2b |
| G013 | ~~Multi-tenant breach — inpatient clinical reads/writes hardcode `DEFAULT_TENANT`/`DEFAULT_FACILITY`~~ **FIXED + RUNTIME-PROVEN (D1).** All inpatient reads/writes now use the request's trust-context tenant/facility (`currentTenant()`/`currentFacility()` via `TrustContextHolder`, populated by a newly-registered `TrustContextFilter`) across InpatientClinicalService / ProcedureEpisodeService / WardRoundService. Proven by `InpatientTenantIsolationIT` — a care plan created under tenant A is invisible to tenant B (same patientId). Full inpatient suite 8/0 unaffected. | `inpatient-service/.../core/*Service.java`, `config/SecurityConfig.java` | hardcoded | fixed; runtime-proven | D1 |
| G014 | ~~Multi-tenant leak — dispatch snapshot JPQL has **no tenant filter**~~ **FIXED + PROVEN (D2).** `findSnapshotAsOf` now filters `WHERE j.tenantId = :tenantId AND j.createdAt <= :asOf`; `DispatchJobService.getSnapshot` + `SnapshotController` thread the request tenant (`UUID.fromString(ctx.tenantId())`). Proven by `DispatchSnapshotTenantIsolationTest` (JPA slice, real H2): tenant B's snapshot excludes tenant A's jobs. (NB: the pre-existing `DispatchApiMockMvcTest` can't boot here — its SecurityConfig needs a JwtDecoder even with disable-oauth-for-tests=true; unrelated latent issue, noted not fixed.) | `dispatch-service/.../repository/DispatchJobRepository.java`, `core/DispatchJobService.java`, `api/SnapshotController.java` | hardcoded | fixed; proven | D2 |
| G015 | ~~BFF GDPR account-deletion **persists nothing**; status always null~~ **FIXED + PROVEN (G1).** Root cause was deeper than "controller forgot to persist": the experience-bff has **no datasource** (composition layer; the V39 `db/migration` tables it referenced are dead — Flyway/JDBC are not even on its classpath), so it *could* not persist. Built the real owner in **data-governance-service** (Privacy-by-Architecture §6, which already owns retention schedules): new `dgv_data_subject_request` table (V005) + entity/repo + `DataSubjectRequestService` with a real lifecycle (PENDING→CANCELLED, idempotent submit, single in-flight per subject) + transactional-outbox events (`…data-subject-request.submitted.v1` / `…cancelled.v1`) that downstream consumers act on for actual erasure (Keycloak/VITO/consent). New `PrivacyRightsController` (`POST/GET/POST-cancel /internal/v1/governance/data-subject-requests`). `AccountDeletionController` now proxies it via `DataGovernanceServiceClient` — **status reflects real persisted state** (null only when never submitted), cancel returns **404 when nothing is pending** (no fabricated success). Proven by `PrivacyRightsApiMockMvcTest` (4: submit-persists+emits, idempotent-no-dup-event, status-reflects-state+404, cancel-lifecycle+404). | `data-governance-service/.../api/PrivacyRightsController.java`, `core/DataSubjectRequestService.java`, `domain/DataSubjectRequestEntity.java`, `db/migration/V005__data_subject_requests.sql`; BFF `controller/AccountDeletionController.java`, `client/DataGovernanceServiceClient.java` | write-no-persist | fixed; proven | G1 |

## HIGH

| ID | Finding | file:line | Cat | Decision | Block |
|----|---------|-----------|-----|----------|-------|
| G016 | ~~Refund large-amount **step-up not enforced** (logs, proceeds to save + ledger)~~ **FIXED + PROVEN (E2).** A refund exceeding the configured threshold now **blocks** (throws `401 STEP_UP_REQUIRED`) BEFORE any refund record or ledger entry is written, unless a step-up token (`X-Step-Up-Token`) is present — mirroring VITO's `StepUpAspect`; INTERNAL service-to-service calls bypass. Proven by `RefundServiceTest` (4): large+no-token blocked and writes nothing, large+token proceeds, internal bypasses, small unaffected. | `mushex-service/.../service/RefundService.java` | write-no-enforce | fixed; proven | E2 |
| G017 | ~~Provider biometric match = exact SHA-256 equality; confidence hardcoded; "(placeholder matcher)"~~ **FIXED + PROVEN (H3).** Removed the SHA-256 template-hash equality that reported `MATCH`/confidence 1.0. Matching now goes through a swappable `BiometricMatcher` seam; the default `FailClosedBiometricMatcher` **never fabricates a match** — it returns `UNAVAILABLE`/0.0 so `verify()` denies (non-MATCH) and stays auditable until a genuine matcher is registered (`@Primary` override). Decision summary records the honest reason. Proven by `FailClosedBiometricMatcherTest` (never MATCH, 0 confidence, not live-capable); full varapi suite 111/0. | `varapi-service/.../core/biometric/BiometricMatcher.java`, `FailClosedBiometricMatcher.java`, `ProviderBiometricService.java` | crypto | fixed; proven | H3 |
| G018 | ~~License certificate download throws `UnsupportedOperationException` on a live route → 500~~ **FIXED + PROVEN (H3).** New `LicenseCertificatePdfGenerator` (Apache PDFBox, self-contained — no external reporting service) renders a real printable A4 certificate from the authoritative licence record (practitioner, provider ID, licence type/number, council, status, validity, conditions, issuer); WinAnsi-safe text sanitisation. `LicenseService.downloadCertificate` returns the PDF bytes; the live `GET /v1/portal/certificates/{id}/download` route now succeeds. Proven by `LicenseCertificatePdfGeneratorTest` (real %PDF header; tolerates missing optional fields). | `varapi-service/.../core/LicenseCertificatePdfGenerator.java`, `LicenseService.java` | unimpl | fixed; proven | H3 |
| G019 | ~~Document signature provider defaults to **NOOP** — random sig, status SIGNED, no crypto~~ **FIXED + PROVEN (F2).** `NoopSignatureProvider.sign` now **fails closed** (throws "refusing to fabricate a signature") instead of returning a random ref + SIGNED status; `SignatureService` records the throw as a **FAILED** job, so unsigned documents are no longer reported signed. Also closes the router's NOOP fallback (unknown provider type now fails closed too). A real provider (EXTERNAL_STUB) must be configured to sign. Proven by `NoopProvidersFailClosedTest`. | `document-service/.../signature/NoopSignatureProvider.java` | placeholder | fixed; proven | F2 |
| G020 | ~~Document OCR provider defaults to **NOOP** — literal "OCR placeholder", job COMPLETED~~ **FIXED + PROVEN (F2).** `NoopOcrProvider.extract` now **fails closed** (throws) instead of returning the placeholder text; `OcrService` records a FAILED job. Real OCR requires a configured provider. Proven by `NoopProvidersFailClosedTest`. | `document-service/.../ocr/NoopOcrProvider.java` | placeholder | fixed; proven | F2 |
| G021 | ~~hr-payroll mutating routes have **no method-level authz**~~ **FIXED + PROVEN (H2).** Every mutating route (`createEmployee/Contract/LeaveType/LeaveRequest/Balance/Attendance/PayrollRun`, `calculate`, `pay`, `createDeduction`) now calls `requireHrWriteAuthority()` → throws Spring `AccessDeniedException` (403) unless the `TrustContext` is INTERNAL **and** actorType ∈ {OPERATOR, SYSTEM}. Clinicians (PROVIDER), citizens (CITIZEN) and EXTERNAL callers can no longer write payroll/leave/attendance. Reads unchanged (tenant-scoped). Proven by `InternalHrApiContractSurfaceTest` (OPERATOR allowed; CITIZEN, PROVIDER, EXTERNAL each denied + no repo interaction). | `hr-payroll-service/.../api/InternalHrApi.java` | hardcoded | fixed; proven | H2 |
| G022 | ~~Leave-request write doesn't decrement balance~~ **FIXED + PROVEN (H2).** New `LeaveService.requestLeave` **reserves** balance against the matching `LeaveBalanceEntity` (used += days, available -= days, fiscal-year derived from start date), **rejects** when no balance/entitlement exists (IllegalState) or available < days (IllegalArgument "Insufficient…"), computes inclusive day-count when omitted, and emits a `leave_request:reserved` outbox event. Controller routes the POST through it. Proven by `HrPayrollDomainLogicTest` (decrements 10→7 on 3 days; rejects 5-on-2). **SoR move (P2.1):** leave is a **Vashandi** workforce concern (per the registry) — the reserve-on-request logic is now ported into Vashandi (`LeaveAvailabilityService` + `vsh_leave_balance`, proven by `LeaveBalanceServiceTest`), enforced when a balance is configured. **DONE (P3.1/P3.3):** the ERP-HR UI reads leave from Vashandi (BFF `ErpHrBffController` → `VashandiServiceClient` by provider-id; proven by `ErpHrBffControllerTest` + the ERP-HR RTL `page.test.tsx`), and hr-payroll's `LeaveService`/`LeaveRequestEntity`/`LeaveBalanceEntity`(+repos)+endpoints were **removed** (leave *types* stay as reference config). | `vashandi-workforce-service/.../core/LeaveAvailabilityService.java`; BFF/UI repoint | write-no-persist | fixed; moved to Vashandi | H2/P2.1/P3 |
| G023 | ~~inventory-elmis-adapter is a **CRUD shell** — no eLMIS client/scheduler ever completes~~ **FIXED + PROVEN (H4).** `triggerSync` no longer parks a record at IN_PROGRESS forever: it now drives the sync to a **terminal state** via the new `ElmisSyncConnector`. When no real endpoint is configured (the default `localhost:9080` placeholder) it **fails closed** — status FAILED, errorMessage `NOT_LIVE: eLMIS endpoint not configured…` (no fabricated success); against a configured endpoint it POSTs the sync payload and completes COMPLETED with the returned `recordsSynced`, or FAILED with the transport error. New `RestTemplate` bean (`ElmisClientConfig`, configurable timeout). Proven by `ElmisSyncTest` (4): not-configured→FAILED/NOT_LIVE (no HTTP), configured→COMPLETED(42), endpoint-error→FAILED, triggerSync never leaves IN_PROGRESS. | `inventory-elmis-adapter/.../core/ElmisSyncConnector.java`, `SyncResult.java`, `SyncStateService.java`, `config/ElmisClientConfig.java` | unimpl | fixed; proven | H4 |
| G024 | ~~BFF privacy-prefs PUT **echoes body, no persistence** (claims `privacy_display_preference` V39)~~ **FIXED + PROVEN (G2).** The V39 claim was itself fiction — that table lives in the BFF's **dead** `db/migration` store (the BFF has no datasource), so GET returned hardcoded defaults and PUT echoed the body. Built the real owner in **data-governance-service** (§6): new `dgv_privacy_display_preference` table (V006, unique per tenant+user) + entity/repo + `PrivacyPreferenceService` (find + **upsert** with safe-value clamping of level/auto-lock + `…privacy-preference.updated.v1` outbox event). Endpoints added to `PrivacyRightsController` (`GET/PUT /internal/v1/governance/privacy-preferences`). `PrivacyPreferencesController` now proxies via `DataGovernanceServiceClient` — **PUT persists**, GET reads the real value back, and an unset preference returns honest defaults flagged `persisted=false` (UI shape preserved + additive flag). Proven by `PrivacyPreferenceApiMockMvcTest` (2: PUT-persists→GET-reads-back+event, upsert-overwrites+clamps-invalid). | `data-governance-service/.../core/PrivacyPreferenceService.java`, `domain/PrivacyDisplayPreferenceEntity.java`, `api/PrivacyRightsController.java`, `db/migration/V006__privacy_display_preferences.sql`; BFF `controller/PrivacyPreferencesController.java`, `client/DataGovernanceServiceClient.java` | echo | fixed; proven | G2 |
| G025 | ~~Clinical drug-interaction checker is a **hardcoded finite set** — can return false "no interactions"~~ **FIXED (F3, honesty).** The empty-result render no longer shows a reassuring green "No known interactions found" (which implied clinical safety from a 7-pair local set). It now shows an amber caution: "No match in this quick-reference set — a limited local reference, NOT a complete interaction check; an absent result does not mean safe; verify against a full service before prescribing." (A real terminology/knowledge-backed checker is a larger follow-on; this removes the dangerous false-negative.) `tsc` clean. | `ui/one-ui-shell/.../clinical/MedscapeTools.tsx` | component-fixture | fixed (no false negative) | F3 |
| G026 | ~~BillingPanel financial fixtures; only Claims tab live, charges/invoices/payments/revenue have no live path~~ **WIRING (P1.3).** **Overview revenue-trend + payer-mix now LIVE** — COSTA's `revenue-summary` already aggregates `byPayerType` + `monthlyTrend` (proxied by `FinancialReportsBffController`); added `useFinanceRevenueSummary` + wired the overview (real payer % + monthly trend, relabelled honestly), demo only when no revenue recorded. **(My earlier "no reporting/analytics endpoint" claim was FALSE — it existed.)** Claims tab live (coverage). **Charges/invoices/payments now LIVE too (P1.3b)** — new facility-scoped COSTA queries (`BillHeaderRepository.findByTenantIdAndFacilityIdAndStatusIn`; `Invoice`/`Payment` join the bill since they carry no facilityId) + `FacilityBillingController` (`/internal/v1/finance/facility/{unbilled-charges,invoices,payments}`) + `FinanceFacilityBffController` proxy + 3 hooks; render real bill/invoice/payment fields, demo + notice when none. **All 5 BillingPanel sections live.** COSTA suite 37/0; BFF compile + `tsc` clean. | `costing-engine-service/.../api/FacilityBillingController.java` + repo queries; BFF `FinanceFacilityBffController.java`; `ui/.../workspace-ops/BillingPanel.tsx`, `hooks/queries/useFinanceBillingWorkspace.ts` | component-fixture | all 5 sections wired | P1.3 |
| G027 | ~~HRShiftsPanel `ACTIVE_SHIFTS/LEAVE_REQUESTS/PENDING_HANDOVERS` fixtures, no live path~~ **WIRING (P1.2 + H1; leave pending P3).** **Handovers now LIVE** — the inpatient-service SBAR `ShiftHandoverEntity` + `GET /internal/v1/handover` already existed; added a BFF GET (`InpatientController.listHandovers` + `InpatientServiceClient.listHandovers`) + `useShiftHandovers(facilityId)` hook; renders real outgoing-staff/status/submitted-at, demo + notice when none. **(My earlier "no handover endpoint exists" was FALSE.)** **Active-shifts** LIVE from the roster feed (coverage% honestly omitted). Staff Roster live. **Leave** stays demo pending the vashandi consolidation (P3) — leave is a duplicate SoR (vashandi vs hr-payroll), being reconciled rather than wired to the duplicate. `tsc`/BFF compile clean. | `ui/.../workspace-ops/HRShiftsPanel.tsx`, `hooks/queries/useInpatient.ts`, BFF `InpatientController.java`, `client/InpatientServiceClient.java` | component-fixture | handovers+active-shifts wired; leave→P3 | P1.2/H1 |
| G028 | ~~StockManagementPanel `PURCHASE_ORDERS/PENDING_RECEIPTS/TRANSFERS` fixtures drive a KPI~~ **WIRED (P1.1 + H1).** All three tabs are now LIVE. **Purchase orders** read the real `procurement-service` (fully built: PO lifecycle/GRN/3-way match) via the pre-existing `useProcPurchaseOrders()` → `ErpProcurementBffController` — render real poNumber/status/total/supplier-ref/date; demo + notice only when none raised. **(My earlier "procurement module is not yet implemented" notice was FALSE — the service existed. Caught by the SoR-First protocol.)** **Receiving/Transfers** derive from the real stock ledger (`useInventoryLedger`: `RECEIPT` / `TRANSFER_IN/OUT` events). Inventory tab live. `tsc` clean. | `ui/.../workspace-ops/StockManagementPanel.tsx` | component-fixture | wired (procurement + ledger) | P1.1/H1 |
| G029 | ~~AIDiagnosticAssistant **silently falls back to fabricated** differentials (orphaned/unmounted today)~~ **RESOLVED — REBUILT REAL (F3 → post-F3 build).** The orphaned fabricated-differentials component was first deleted, then (per PO direction) **rebuilt as a real, governed decision-support tool** at `ui/.../clinical/AIDiagnosticAssistant.tsx`, backed end-to-end by the real `useAskEdlizClinical()` → BFF `/internal/v1/clinical/assistant/ask` → `ClinicalAssistantService` (EDLIZ source retrieval + deterministic rules engine + auditable `RecommendationTraceEntity`). Renders page-referenced citations, rules warnings, the honest `support_mode` (incl. `INSUFFICIENT_EVIDENCE` shown as "not a clinical answer"), the backend disclaimer, and the trace id. **Never fabricates** — error path surfaces unavailability; INSUFFICIENT_EVIDENCE renders honestly with no invented citations/recommendation. Mounted in the Clinical Tools CDS tab. **Governed loop completed:** a clinician can record an auditable **override** of a recommendation against its trace via real BFF `/internal/v1/clinical/audit/overrides` → CKP `OverrideRecordEntity` + outbox event. 4 RTL tests (grounded answer, insufficient-evidence honesty, override, fail-honest) green; `tsc` clean. **Intelligence upgrade (W2):** the assistant now reasons via a real grounded LLM (Claude through the existing `llm-orchestration-service` `/internal/v1/llm/structured`) over the retrieved EDLIZ sections + medicine guidance + deterministic rule alerts + patient context — `ClinicalReasoner` seam, `GroundingValidator` (citation-closure/ratio/empty-retrieval guards), honesty gate on real-vs-stub provider, and **fail-closed to the deterministic answer** on any disabled/unavailable/ungrounded/hallucinated result. Deterministic safety alerts stay authoritative (LLM can never suppress them); response contract preserved (additive `reasoning_provenance`/`model`/`grounding`/`differential_considerations`); `llm_reasoning` audit block on every trace. **Retrieval (W1):** swappable `GuidanceRetriever` seam — lexical now, semantic/hybrid via search-service wired + fail-closed (needs an EDLIZ index back-fill to activate). 14 CKP unit tests (reasoner/grounding/client) + 3 retriever tests green. **Note:** closing G029 surfaced a much larger ambition — a real end-to-end interpretation-grade CDS engine (patient-appropriate reference ranges, lab/vital interpretation, derived calculators). That is a **separate product initiative (Track B)** spun off into [`docs/roadmaps/cds-engine-program.md`](../roadmaps/cds-engine-program.md); it is **deferred until this gap register (Track A: Waves G/H/I) reaches zero** (PO decision 2026-06-24). G029/G029b themselves are closed. **Track B update (2026-06-25):** Phase 0.2 + **Phase 1 KEYSTONE landed** on `intake/wave-b-tshepo-gdhcn-trust-primitives` — governed, cited, fail-closed, auditable context-aware interpretation of vitals + labs end-to-end (CKP `clinical/interpretation/*` RangeResolver + InterpretationEngine; Zibo `OBSERVATION_DEFINITION` + provider; AKI/hyperK/critical-lab/pregnancy rules; `POST /interpretation/evaluate` + BFF proxy; cited DRAFT vitals seed + single-source generator + drift guard; both UI surfaces render interpreted flags). `NO_REFERENCE_RANGE` never NORMAL; UCUM-safe or `UNIT_MISMATCH`; each interpretation records the selected range id+version. See [`cds-engine-phase1-completion.md`](../roadmaps/cds-engine-phase1-completion.md). | `ui/.../clinical/AIDiagnosticAssistant.tsx`, CKP `reasoning/*`, `llm/*`, `assistant/retrieval/*`, `interpretation/*` (+ `useClinicalCds.ts`, BFF clinical controller/client, `app/clinical-tools/page.tsx`) | component → real LLM-backed | REAL_PROVEN (unit) | post-F3 / W1-W2 / TrackB-P1 |
| G029b | ~~**ActiveCDSBanner fabricated live "AI Diagnostic Engine (Live)" insights**~~ — `generateContextualGuidance()` returned hardcoded sepsis/renal alerts and `fetchAIGuidance()` did `setTimeout(1500)` then injected invented vitals (K+ 5.8, creatinine 2.1) labelled "(Live)". Mounted in `ClinicalToolbar` → shown to users. **RESOLVED — REAL END-TO-END (post-F3 build).** All fabrication removed AND wired to a real feed: in the EHR shell, `EHRLayout` assembles the patient's **own** records — active conditions (`useConditions`), active prescriptions (`usePrescriptions`), latest vitals (`useVitals`), documented allergies (`useAllergies`) — into a rules-engine context and POSTs to new BFF `/internal/v1/clinical/rules/evaluate` → CKP `ClinicalRulesEngine`; matching deterministic alerts map into the banner. The rules engine was **extended with real vitals (HTN-crisis/tachy/brady/fever), drug–allergy interaction, severe-allergy flag, asthma+beta-blocker contraindication, and chronic-disease monitoring rules** (`ClinicalEvaluationContext` gains `Vitals`+`Allergy`), making the CDS-tab catalogue truthful (Hypoxemia honestly marked dormant — no SpO₂ capture yet). Empty when nothing triggers — never fabricated. Backend: 15 rules-engine unit tests (7 new) green. UI: 2 hook tests (real context assembly, disabled-without-patient) green. **Intelligence upgrade (W3):** added a real, grounded, fail-closed **AI insight** that summarises/prioritises the patient's *deterministic* alerts via the governed LLM (`/internal/v1/clinical/cds/summary`, code-subset guardrail, audited); rendered as a single `info`-severity, AI-attributed banner item that never outranks or gates a real alert, and is absent (not faked) when the LLM is unavailable. `CdsInsightService` x3 + `useCdsInsight` x3 tests green. | `ui/.../clinical/ActiveCDSBanner.tsx`, `EHRLayout.tsx`, `useClinicalCds.ts`, CKP `cds/CdsInsightService`, `ClinicalRulesEngine`, BFF `ClinicalKnowledgeController` | live-fabrication → real LLM-backed | REAL_PROVEN (unit) | post-F3 / W3 |

## MEDIUM

| ID | Finding | file:line | Cat | Decision | Block |
|----|---------|-----------|-----|----------|-------|
| G030 | ~~Wallet card crypto key = **SHA-256 of a key-reference string** (deterministic)~~ **FIXED + PROVEN (E3).** The per-record AES-256 key is now `HMAC-SHA256(masterKey, keyRef)` where `masterKey` is a configured secret (`wallet.card.encryption-master-key`, env, fail-closed on blank/<32-char) — so the key is NOT recoverable from the stored `encryption_key_ref` alone. NB: tshepo-keys is signing-only and `vault-kms` is a stub, so there is no platform data-key API to "fetch from" today; the configured KEK is the honest fix and a future KMS/tshepo-keys data-key API can replace it. Proven by `CardHealthDataCryptoTest` (4): the old SHA-256(keyRef) key fails to decrypt, HMAC-derived round-trip works, constructor fails closed. | `mushe-wallet-service/.../card/CardHealthDataService.java` | crypto | fixed; proven | E3 |
| G031 | ~~Wallet encounter event summary written as literal `"{}"`~~ **FIXED (I).** The queued critical-summary placeholder is now an explicit `{"status":"PENDING_SYNC"}` instead of `"{}"` — a reader can't mistake "not yet synced from BUTANO" for "no critical health data" (no allergies/conditions) on a health card. The real summary is still filled at card-sync. compiles. | ~~`mushe-wallet-service/.../WalletEventConsumer.java:241`~~ | placeholder | fix | I |
| G032 | ~~SMART card public key stored as `"DEV-PLACEHOLDER-"+uuid`~~ → **CLOSED (batch F)**. Fabrication removed; the key is now provisioned at the real point in the lifecycle and activation fails closed without it: (1) V030 migration drops `NOT NULL` on `public_key` + entity nullable; (2) `requestCard` stores the supplied key or `null` (no placeholder); (3) `markPrinted(cardId, publicKey)` provisions the secure-element key at print; (4) `activate()` **fails closed** — rejects a missing or legacy `DEV-PLACEHOLDER-` key; (5) `CardController` print endpoint accepts the key (`CardPrintRequest`); BFF print route already forwards a JSON body. 5 unit tests (no-fabrication, provision-at-print, fail-closed on null/placeholder, success on real key). **Follow-up:** card-print-agent must supply the generated public key at print, else activation correctly fails closed. mvn compile + card/issuance/qr tests green. | `vito-service/.../core/card/CardLifecycleService.java`; `.../api/CardController.java`; `db/migration/V030__*.sql` | resolved | **built (batch F)** | **DONE** |
| G033 | ~~SMS/EMAIL default to log-only stub providers — notifications silently dropped out-of-box~~ **FIXED (I, honesty).** The stub providers no longer *silently* drop: `SmsStubProvider`/`EmailStubProvider.send` now log a **WARN** ("NOT DELIVERED — stub provider; set notification.{sms,email}.provider=http/smtp") instead of a quiet INFO, so undelivered OTPs/alerts are loud + auditable. The real http/smtp providers still win when configured (resolve order unchanged). (Full delivery still needs a real gateway configured — external dep.) compiles. | ~~`notification-service/.../{SmsStubProvider,EmailStubProvider}.java`~~ — `ProviderRegistry.java:40`, `SmsStubProvider.java:15` | placeholder | fix (config default) | I |
| G034 | ~~Pickup slip returns hardcoded JSON instead of a PDF~~ → **CLOSED**. `GET /v1/orders/{id}/pickup/slip.pdf` now streams a real `application/pdf` (new `PickupSlipPdfService`, PDFBox + ZXing, mirroring share-slip/vito slip pattern): order ref, token id, expiry, instructions, and a scannable QR when the caller passes the client-held `token` (validated against the stored hash via `PickupTokenService.resolveSlipContext`, never persisted/logged; 404 no-active-token, 400 mismatch). 6 unit tests (PDF bytes + context validation). **Note (reachability):** this is the order-native slip and currently has no BFF/UI consumer — the *live* UI pickup-slip path is VITO's already-real `/v1/slips/pickup.pdf` (generic delegated slip), so the two are complementary, not duplicates. | `msika-flow-service/.../core/PickupSlipPdfService.java`; `.../api/controller/PickupController.java` | resolved | **built** | **DONE** |
| G035 | ~~BFF StructuredHistory fabricates clinical history ("Dr. Stub") for golden patient~~ **FIXED (I).** The demo histories are already gated to a single golden demo patient (real patients get empty), but the clinician-impersonating names `"Dr. Stub"` / `"OT Stub"` are relabelled `"Sample data (demo)"` so demo rows can never read as a real clinician. compiles. | ~~`experience-bff/.../StructuredHistoryController.java:286` | hardcoded | honest-degrade/fix | I |
| G036 | ~~BFF Inventory `/requisitions` demo fallback with **no** dev-mode flag~~ **FIXED (I).** The demo-requisitions fallback is now gated behind `impilo.bff.inventory.demo-fallback` (**default false** = production-safe); when off, an unavailable upstream returns an empty list, never fixtures. Mirrors the wallet/citizen-longtail flag pattern. compiles. | ~~`experience-bff/.../InventoryController.java:141`~~ | hardcoded | fix (gate behind flag) | I |
| G037 | ~~Fabricated provenance — literal `completedBy`/`recordedBy` pollute clinical audit~~ **FIXED (I).** The hardcoded provenance literals (`"preop-submit"`, `"anaesthesia-preop"`, `"mvumo"`) are replaced by the **real actor** from the trust context via a new `currentActor()` helper (`TrustContextHolder.require().actorId()`, "system" fallback only when no context). Clinical audit now records who actually acted, not a placeholder. inpatient suite 8/0; compiles. | `inpatient-service/.../core/ProcedureEpisodeService.java` | hardcoded | fixed | I |
| G038 | ~~Handover `outgoingStaff`="outgoing-nurse"; history row "Impilo Facility"~~ **FIXED (I).** Handover `outgoingStaff` defaults to the **real actor** (`currentActor()`) when the body omits it (was the fake "outgoing-nurse"); the procedure-history row's facility is the **real facility id** from the trust context (`currentFacility()`) instead of the literal "Impilo Facility". inpatient suite 8/0. | `inpatient-service/.../core/{InpatientClinicalService,ProcedureEpisodeService}.java` | hardcoded | fix | I |
| G039 | ~~Conversational `ask()` confidence two-value constant; no NLP~~ **FIXED (I).** `GuidanceService.ask` now does grounded LLM synthesis over the retrieved knowledge-base articles via the governed `llm-orchestration-service` (`/internal/v1/llm/structured`): the model answers ONLY from the supplied sources (advisory, cites titles) and returns a calibrated confidence; response carries `answerSource=llm`. Opt-in `guidance.llm.enabled` (default false → retrieval answer preserved). **Honesty gate** (same as CKP): only a real provider (`anthropic`/`openai`/…) with non-empty `structuredOutput` is used; stub provider / empty / timeout / error → falls back to retrieval (`answerSource=retrieval`) — never fabricates. Reuses the CKP `LlmClinicalClient`/`ClinicalLlmConfig` pattern. 7 tests (real-provider use, non-real/empty fallback, confidence clamp, disabled/empty-sources no-call, upstream-error fail-closed). | `guidance-service/.../llm/{GuidanceLlmClient,GuidanceLlmConfig}.java`, `core/GuidanceService.java` | resolved | **built (LLM, fail-closed)** | I |
| G040 | ~~DAGS policy `conditions` persisted but no engine reads them~~ **FIXED + PROVEN (I).** New `PolicyEvaluator` reads the persisted `conditions` JSON and evaluates a request against them — a policy matches when every `key=value` condition is satisfied by the request context (`{}` = unconditional); decision is **deny-overrides → allow → default-deny**, and unparseable conditions never match (fail-closed). Exposed as `POST /internal/v1/policies/evaluate`. The conditions column is no longer write-only. Proven by `PolicyEvaluatorTest` (4: allow-on-match, deny-overrides, default-deny, unconditional); DAGS suite 44/0. | `data-access-governance-service/.../core/PolicyEvaluator.java`, `api/PolicyController.java` | dead | fix | B3/I |
| G041 | ~~Identity-assurance has no GET endpoint — stored records unreachable~~ **FIXED (C1).** Added `GET /internal/v1/risk` (lists the tenant's assessments; the service method already existed). Attestations already had a GET. | `identity-assurance-service/.../RiskAssessmentController.java` | dead | fixed | C1 |
| G042 | ~~hr-payroll attendance stored but never computes hours/overtime~~ **FIXED + PROVEN (H2).** New `AttendanceService.record` computes `hoursWorked` from clock-in/out (minutes→hours, HALF_UP) and `overtimeHours = max(0, worked − 8h standard shift)`; open shifts (no clock-out) are marked OPEN with null hours instead of trusting/blanking the body. Controller routes the POST through it. Proven by `HrPayrollDomainLogicTest` (08:00–18:00 → 10.00 worked / 2.00 OT / PRESENT; clock-in-only → OPEN). **SoR move (P2.2/P3.2):** attendance is a **Vashandi** workforce concern — Vashandi now derives worked-hours/overtime from its real check-in/out events (`AttendanceHoursService`, proven by `AttendanceHoursServiceTest`), and **payroll consumes Vashandi** (`PayrollService` → `VashandiAttendanceClient`, mapped by providerId) instead of a separately-entered table — **kills the double data-entry**. **DONE (P3.1/P3.3):** the ERP-HR UI reads attendance from Vashandi (by-provider events via the BFF; RTL-proven), and hr-payroll's `AttendanceService`/`AttendanceRecordEntity`(+repo)+endpoints were **removed**. hr-payroll keeps employees/contracts/deductions/payroll/payslips. | `vashandi-workforce-service/.../core/AttendanceHoursService.java`; `hr-payroll-service/.../core/{PayrollService,VashandiAttendanceClient}.java`; BFF/UI repoint | dead | fixed; moved to Vashandi | H2/P2.2/P3 |
| G043 | ~~No earnings model beyond `basicSalary` (no allowances/overtime/tax)~~ **FIXED + PROVEN (H2).** `PayrollService.calculate` now computes **gross = basic + allowances + overtime**: allowances summed from the contract's `allowancesJson`; overtime pay = period overtime hours (from attendance) × (basic ÷ 176 standard monthly hours) × 1.5; the auditable breakdown is stored on `payslip.allowancesJson`; deductions (tax/pension/other) still apply on the real gross. Proven by `HrPayrollDomainLogicTest` (basic 1760 + allowances 200 + OT 150 = gross 2110.00). | `hr-payroll-service/.../core/PayrollService.java` | hardcoded | fixed; proven | H2 |
| G044 | ~~live-service media defaults to in-memory `local-dev` (`DEV-TOKEN-NOT-FOR-PRODUCTION`)~~ **FIXED (I, honesty).** `MediaProviderConfig` now logs a **WARN** at bean creation when the local-dev provider is selected ("NON-PRODUCTION token; set live.media.provider=rtc-gateway"), so a dev media provider can't silently run in a real env. The real `rtc-gateway` path is unchanged. compiles. | ~~`live-service/.../MediaProviderConfig.java:13`~~ | placeholder | fix (config default) | I |
| G045 | ~~External PACS connector throws on get*~~ → **CLOSED**. `ExternalPacsClient` is now a real **DICOMweb** connector (QIDO-RS search + WADO-RS metadata): `findStudyIdsByStudyInstanceUid` → QIDO `/studies`; `getStudy`/`getSeries`/`getInstanceSimplifiedTags` drill down via `/studies/{s}/series` + `/instances` + `/instances/{sop}/metadata`, adapting DICOM JSON into the Orthanc-shaped JSON `ImagingHierarchySyncService` already consumes (UID-composite backend ids; new `dicomwebRestTemplate` handles `application/dicom+json`). **Fail-closed:** no `external-base-url`/DICOMweb-disabled → queries empty, retrievals throw, `supportsHierarchySync=false` (no fabrication). 6 tests against a simulated DICOMweb endpoint (JDK HttpServer) prove the round-trip + that the output parses through `OrthancTagParser`/`OrthancMainDicomHelper`. **Live remote PACS/VNA is still absent in this env** — connector is reachable-when-configured, fail-closed otherwise. | `pacs-adapter-service/.../integration/ExternalPacsClient.java`; `config/DicomWebClientConfig.java` | resolved | **built (DICOMweb)** | **DONE** |
| G046 | ~~`oauth2-enabled=false → permitAll` off-switch disables all auth~~ → **CLOSED (batch F)**. The `<svc>.security.oauth2-enabled` flag, when false, set `anyRequest().permitAll()` — a single property that disabled ALL auth for a service. **Removed from all 23 services** that still carried it (a deterministic codemod, `scripts/completeness/g046-remove-oauth-offswitch.mjs`, preserving each service's per-service request matchers). Production is now always secure (oauth2 JWT); the open chain is reachable only via the explicit test-only `impilo.security.disable-oauth-for-tests` conditional — there is **no production auth off-switch**. Also: dead `oauth2-enabled` keys purged from 29 config files; 3 test ymls (vashandi/simba/pct) repointed to the test flag; generator template (`emit-service-security-config.mjs`) updated so new services are safe-by-default. **Verified:** 32 services compile; 7 representative services across all structural variants + the 3 config-changed (inpatient, procurement, vashandi, simba, pct, madi, coverage — incl. MockMvc auth tests) test green. | 23× `*/config/SecurityConfig.java`; `scripts/completeness/g046-remove-oauth-offswitch.mjs` | resolved | **built (batch F)** | **DONE** |
| G055 | ~~Client demographics-UPDATE has no consumer above Vito~~ → **CLOSED (H5)**. Full UI→BFF→Vito thread built: `VitoServiceClient.rawPut` + `VitoClientRegistryBffController` `PUT /clients/{healthId}/demographics` (proxies Vito `PUT /v1/clients/{id}`) + `useUpdateClientDemographics` hook + `EditDemographicsForm` component (RTL-tested: asserts PUT to the BFF route with the edited body) + wired into the client master tab (`registry/clients/[id]/page.tsx`, seeded from master, refetches on save). `tsc` clean. | `experience-bff/.../client/VitoServiceClient.java`; `.../controller/VitoClientRegistryBffController.java`; `ui/one-ui-shell/src/components/registry/EditDemographicsForm.tsx` | resolved | **built (H5)** | **DONE** |

## LOW

| ID | Finding | file:line | Cat | Decision | Block |
|----|---------|-----------|-----|----------|-------|
| G047 | ~~Share-slip PDF prints token as text — no QR rendered~~ **FIXED (I).** Added ZXing (cached, Apache-2.0) + a `qrPng()` helper; the slip now embeds a **real scannable QR** of the share token via PDFBox `PDImageXObject`, with the raw token kept below for manual entry. share-slip compiles offline. | ~~`share-slip-service/.../ShareSlipPdfService.java:125`~~ | placeholder | fix | I |
| G048 | ~~BFF DisplaySettings GET defaults / PUT echoes — prefs don't persist~~ **FIXED + PROVEN (I).** Same disposition as [[G024]]: persist in data-governance (the per-user display-preference store). Extended `dgv_privacy_display_preference` (V007) + entity with theme/font/density; `PrivacyPreferenceService.upsertDisplaySettings` (clamped) + `GET/PUT /internal/v1/governance/display-settings`. `DisplaySettingsController` now **proxies** it (persists, reads back; unset → safe defaults flagged `persisted=false`) instead of echoing. Proven by `PrivacyPreferenceApiMockMvcTest` display case (PUT dark/large/compact → GET reads back); data-gov suite green; BFF compiles. | `data-governance-service/.../{PrivacyPreferenceService,PrivacyRightsController}.java` + V007; BFF `DisplaySettingsController.java`, `DataGovernanceServiceClient.java` | echo | fixed; proven | I | I |
| G049 | ~~BFF Vitals listing without ids returns empty — `TODO: wire to PctServiceClient`~~ **FIXED (I).** Vitals are per-patient and the PCT path is already wired when a cpid resolves; the no-identifier case now returns **400** ("patient_id or a resolvable encounter_id is required") instead of a misleading empty-list + TODO. compiles. | ~~`experience-bff/.../VitalsController.java:78`~~ | todo | fix | I |
| G050 | ~~Search relevance hardcoded 1.0 (no ranking)~~ **FIXED + PROVEN (I).** Replaced `score:1.0` with a real deterministic lexical `relevanceScore` (query-term overlap weighted title 3× / summary 1×, normalised to (0,1]); results are now **sorted by score**. No external engine needed. Proven by `RelevanceScoreTest` (3: title>summary>none, blank→0, partial-multi-term). compiles. | `guidance-service/.../GuidanceApiController.java` | hardcoded | fixed | I |
| G051 | ~~Empty `ifPresent` lambda + redundant re-query (dead block)~~ **FIXED (I).** Removed the no-op `findById(...).ifPresent(item -> { if (item.isCriticalFlag()) {} })` block — it did nothing and was immediately followed by the real `findById(...).orElse(null)` + critical-flag check. indawo compiles. | `indawo-service/.../core/SiteRegulatoryService.java` | dead | fixed | I |
| G052 | ~~PortalHealthReporting fixtures + fake submit — **orphaned/unmounted**~~ **RESOLVED — DELETED (I).** Confirmed not imported/mounted anywhere (grep) and entirely fixture-driven (fake submit) — removed the file rather than wire a dead component. `tsc` clean. | `ui/.../portal/PortalHealthReporting.tsx` (removed) | component-fixture | reject(delete) or wire | I |
| G053 | ~~PatientLocationBadge "Uses mock data"~~ → **CLOSED**. Now reads the patient's ward + bed LIVE from the inpatient-service SoR: new `AdmissionService.getCurrentLocation` (resolves the active admission's ward/bed UUIDs to labels) + `GET /internal/v1/admissions/current-location` (204 when not admitted; 4 unit tests) → BFF `InpatientServiceClient.getCurrentLocation` + `GET /internal/v1/inpatient/admissions/current-location` → `usePatientLocation` hook → badge prefers the live admission over URL hints (RTL-tested: live ward/bed renders + not-admitted falls back). `tsc` + `mvn` clean. | `services/inpatient-service/.../core/AdmissionService.java`; `.../api/controller/AdmissionController.java`; `ui/.../layout/PatientLocationBadge.tsx` | resolved | **built** | **DONE** |
| G054 | ~~Mobile Immunizations/CarePlans/Referrals unwired (`TODO`; honest empty no-op)~~ **FIXED (I).** The three citizen-app sections now fetch real data: new BFF `CitizenClinicalRecordsController` (`/internal/v1/mobile/citizen/{immunizations,care-plans,referrals}`) resolves the patient from `X-Actor-ID` and delegates to the existing sovereign clients (PCT immunizations/referrals, inpatient care-plans) — no new model/fixtures; mobile `clinicalRecordsService` + sections wired to it (TODO no-op removed). 4 BFF tests + 6 mobile service tests (endpoint paths + envelope unwrap shapes); tsc clean. | `experience-bff/.../mobile/citizen/CitizenClinicalRecordsController.java`, `apps/mobile/citizen-app/src/services/clinicalRecordsService.ts`, `screens/personal/{Immunizations,CarePlans,Referrals}Section.tsx` | resolved | **built** | I |
| G057 | ~~Capability-token verification is **not yet truly offline** (fetches JWKS online per verify) and **omits issuer/audience/scope claim checks**~~ **FIXED + UNIT-PROVEN (real crypto) (b).** New `JwksCache` serves a TTL-cached JWKS — verification stays offline within the window and falls back to the **last-known-good** key set if the keys-service is unreachable (rotation handled by a single force-refresh on unknown-kid). `CapabilityTokenJwsVerifier` now enforces **iss** + **aud** (config-shared with mint, so they can't drift) + non-empty **capability scope** after signature verify. Proven by `CapabilityTokenJwsVerifierTest` (real Ed25519 mint/verify, new WRONG_ISSUER/WRONG_AUDIENCE/MISSING_SCOPE cases) + `JwksCacheTest` (hit/refresh/offline-fallback/rotation). **Residual:** broad JWKS *distribution* to fully-disconnected devices is a separate offline-pack delivery concern (not this hot path); not runtime-proven against a live keys-service (cross-service). | `tshepo-offline-service/.../CapabilityTokenJwsVerifier.java`, `.../JwksCache.java` | incomplete → fixed | fixed; unit-proven (real crypto) | b / B2-followup |
| G058 | **PARTIAL (batch F): SUPERVISOR_APPROVAL endpoint built; external-dep seams remain honestly fail-closed.** The runtime-proven dual-control approval flow now has a controller endpoint — `POST /v1/step-up/{challengeId}/supervisor-approval`: supervisor identity (`x-actor-id`) + authority (JWT `realm_access.roles` ∩ configurable `tshepo.authz.step-up-supervisor-roles`) are derived **server-side**, never from the body; fail-closed (403) for non-supervisors, and the service still blocks self-approval. 4 controller tests + existing 14 `StepUpServiceTest` green. **SMS-OTP delivery wired to the comms SoR:** `NotificationOtpDeliveryAdapter` (opt-in `tshepo.authz.otp-delivery.notification-enabled=true`) hands the OTP to notification-service `/internal/v1/notify` with companion context, fail-closed if unreachable/rejected. 3 adapter tests. **TOTP enrolment store BUILT (in-platform):** authenticator-app enrolment works end-to-end — V016 `totp_enrolment` table; `TotpSecretCipher` (AES-256-GCM at rest, config key, refuse-weak-key); `TotpEnrolmentService` (enrol → `otpauth://` URI + base32 secret; confirm; revoke); `StoredTotpSecretProvider` replaces the fail-closed default when `tshepo.authz.totp.enrolment-enabled=true`; `TotpCodes` shared RFC 6238 math (verifier refactored to share it); `TotpEnrolmentController` (`/v1/step-up/totp/enrolment[/confirm|/revoke]`, server-side actor). 8 tests (cipher round-trip/tamper/weak-key + enrol→confirm→provider→verify). **Still genuinely external seams (correctly NOT_LIVE):** the live SMS gateway + recipient contact resolution, and the biometric matcher SDK. | `tshepo-authz-service/.../stepup/{TotpEnrolmentService,TotpSecretCipher,TotpCodes,StoredTotpSecretProvider,NotificationOtpDeliveryAdapter}.java`, `api/{StepUpController,TotpEnrolmentController}.java` | partial — supervisor endpoint + SMS-OTP wiring + TOTP enrolment store built; SMS gateway + biometric remain external | supervisor + SMS-OTP + TOTP enrolment DONE; SMS gateway/biometric remain NOT_LIVE | B1-followup |
| G059 | ~~**Signing client calls a non-existent keys-service endpoint — breaks TWO live issuance paths**~~ **DEFECT FIXED + RUNTIME-PROVEN (a+G059).** Was: tshepo-offline `KeysServiceClient` POSTed `/v1/sign/jws` `{payload,keyId,algorithm}` (no such endpoint; no tenantId) → 404, breaking **both** capability-token issuance and offline-pack generation. Fix: unified both ends on a **tenant+purpose** signing contract — keys-service `POST /v1/sign` now signs with `getActiveKeyForPurpose(tenantId, purpose)` (fail-closed) and returns the kid used; the client posts `{tenantId,payload,jwsCompact,purpose}` (OFFLINE_CAPABILITY / new OFFLINE_PACK). This also **threads fix-a** (the previously-dormant purpose-scoped key lock is now fitted to the signing path). Proven by `SigningRuntimeProofIT` (real `/v1/sign` → JWS verifies against live JWKS + fail-closed path, real Postgres) **and** `KeysServiceClientTest` (client posts the aligned contract) — both ends of the wire. | `tshepo-offline-service/.../client/KeysServiceClient.java`; `.../core/CapabilityTokenService.java`; `.../core/OfflinePackService.java`; `tshepo-keys-service/.../api/SigningController.java`, `.../core/Ed25519SigningService.java` | contract-mismatch / runtime-break | fixed; runtime-proven (both ends) | a / B-core hardening |
| G060 | ~~**B1 migration version collision** — `V002__step_up_verification.sql` shared version `2` with `V002__add_10_dimension_access_control_fields.sql`; Flyway throws *"Found more than one migration with version 002"* at startup → **tshepo-authz cannot boot**~~ **DEFECT FIXED + RUNTIME-PROVEN (fix-d):** renamed to `V010`; `StepUpVerificationIT` boots the full context and applies all 13 migrations against a real Postgres. Was invisible to the mocked unit suite (no Flyway). Found *by* standing up the runtime proof. | `tshepo-authz-service/.../db/migration/V010__step_up_verification.sql` | migration/boot-failure | fixed; runtime-proven | d / B1 |
| G061 | ~~**Step-up lockout & reject-audit silently defeated** — `verifyChallenge` is `@Transactional` and signals failure by throwing `SecurityException`, so Spring rolls back the `attemptCount++` **and** the `STEP_UP_REJECTED` outbox audit on every failed attempt → attempt-count lockout never fires (**unlimited guesses**) and rejections are never audited~~ **DEFECT FIXED + RUNTIME-PROVEN (fix-d):** `@Transactional(noRollbackFor=…)` so the counter + reject audit commit on throw; `StepUpVerificationIT.failedAttempts_persistAndLockOut` exhausts the cap and asserts lockout (`FAILED`) against a real Postgres. Mocks hid it (no real rollback). Found *by* the runtime proof. | `tshepo-authz-service/.../service/StepUpService.java:90` | auth-bypass/audit-gap | fixed; runtime-proven | d / B1 |
| G062 | ~~**VITO client-biometric match = SHA-256 template-hash equality; confidence hardcoded 1.0; "(vendor engine not configured)"** — same defect as G017 but in the *client* identity path. The default `BiometricMatchingEngine` bean (`TemplateDigestMatchingEngine`) returned `MATCH`/1.0 when `SHA-256(probe)` equalled the stored template hash, and `identify(...)` fabricated 1:N candidates the same way. Biometric captures never produce byte-identical templates, so this is a fabricated person-identity verification — dangerous on VITO's `BiometricOperationsService` verify/identify/dedup paths.~~ **DEFECT FIXED + PROVEN.** Removed `TemplateDigestMatchingEngine`; the default engine is now `FailClosedMatchingEngine` which **never fabricates a match** — `verify(...)` returns `UNAVAILABLE`/0.0 and `identify(...)` returns no candidates, so verification denies and stays auditable (honest decision summary) until a genuine vendor engine is registered as a `@Primary` `BiometricMatchingEngine` bean. The configured liveness gate (`vito.biometric.liveness-min-score`) is preserved and still evaluated first. Proven by `FailClosedMatchingEngineTest` (4/0: never MATCH even on digest equality, liveness gate fires, identify empty). | `vito-service/.../core/biometric/engine/FailClosedMatchingEngine.java` (was `TemplateDigestMatchingEngine.java`), `BiometricMatchingEngine.java` | crypto | fixed; proven | follow-up (mirrors H3/G017) |

## Wave B-foundations — GDHCN-ready primitives (landed progress)

Not gap closures — foundations that make Tshepo GDHCN-*ready* (later waves make GDHCN
operational). Honest scope: a Tshepo primitive, **not** a claim of GDHCN conformance.

- **B4 — `libs/tshepo-trust-crypto` (landed, `7dfd490a`).** JWS signature verification with
  kid resolution (Ed25519/RSA/EC) + algorithm allowlist + canonical `TrustError` model;
  pass/fail proven (11 cases, real Ed25519). Consumers (`OfflineEntitlementVerifier`,
  `CapabilityTokenJwsVerifier`) may later refactor onto it — not done in B4.
- **B5 — Tshepo Trust Authority registry (landed).** tshepo-authz V014: `trust_authority` +
  authorised `trust_issuer_system` + `trust_document_signer_cert` (metadata only, no private
  keys) with honest readiness enums (NOT_STARTED … PRODUCTION_NOT_READY) and a status
  lifecycle (DRAFT/PENDING/ACTIVE/SUSPENDED/RETIRED) with enforced transitions. Admin API
  `/v1/trust-authorities`; every mutation gated by a **dual gate** (admin role + recent
  step-up) and audited. Unit-proven (`TrustAuthorityServiceTest`, 6) + runtime-proven
  (`TrustAuthorityRegistryRuntimeProofIT`, real Postgres + V014).
- **B6 — GDHCN readiness cockpit (landed).** Full vertical slice: tshepo-authz readiness
  domain (V015, 10-domain code catalogue merged per-tenant, dual-gated updates, audit) →
  experience-bff proxy (`/internal/v1/admin-governance/gdhcn-readiness`) → one-ui-shell route
  (`/work/administration-governance/gdhcn-readiness`, real-data board with the honesty
  contract stated inline). Backend unit (5) + runtime (2, real Postgres + V015); BFF unit (2);
  UI `tsc --noEmit` clean. UI domain-editing deferred (backend ready, admin+step-up gated).
- **Design note** `docs/architecture/tshepo-gdhcn-trust-and-certification.md`: landed.

**Wave B (B-core + B-foundations) is complete.** The three trust-plane criticals (G001/G002/
G003) plus G056/G057/G059/G060/G061 are closed; B4/B5/B6 + design note make Tshepo
GDHCN-*ready* (not operational — see the deferred list in the design note).

## Committed deferred feature blocks (will be built, not dropped)

These are not "deferred indefinitely" — they are scheduled blocks that close a
register row by building a missing surface/feature:

- **H5 — Client demographics-edit flow (closes G055).** Build the UI edit-demographics
  screen + experience-bff update endpoint + `VitoServiceClient` PUT to
  `/v1/clients/{healthId}`, completing the UI→BFF→Vito→read-back thread for the
  already-correct Vito backend (3A). Hard commitment for Wave H.

## ACCEPTABLE / NOT GAPS (verified — do not re-flag)

- `*V11ProbeController` — intentional tech-companion enforcement probes; `/test-command` echo is a documented idempotency proof.
- `rtc-gateway` `InMemoryRtcSessionPersistence` — superseded; `JpaRtcSessionPersistence` is `@Primary`.
- `ndila` `MockProviderAdapter`, `nhume` `Simulated*Adapter` — clearly-labelled config-selectable safe fallbacks.
- `fhir-gateway` `ConsentCacheService` — Kafka-fed TTL revocation cache; enforcement makes a real call, fail-closed DENY.
- BFF Wallet/Queue/Facility fallback controllers — try downstream first, gate fabrication behind `allow-local-fallback=false`/`mode=live`, fail-closed by default. (Contrast G009–G012, which inject **no** client.)
- BFF telemetry history ring-buffers (Telemedicine/AppointmentComms) — bounded ephemeral telemetry by design. NB: the widened detector flags these `in-memory-store`/`in-memory-backing`; treat as low/by-design unless durability is required.
- BFF `HealthIntelligenceService aggregateOnlyPlaceholder` — deliberate visibility withholding.
- `document-service` MINIO storage + ClamAV — real; NOOP variants are `@ConditionalOnProperty`-gated (the *default selection* is the G019/G020 gap).
- `costing-engine` `ZW-PLACEHOLDER-POC` / `DEFAULT_DEMO_TARIFF` — labelled demo seed ("NOT an official national tariff").
- general-ledger, forms-service, developer-portal — in-memory suspicions refuted via full reads.
- Static UI config arrays (nav menus, enum/option pickers, badge/theme maps, structured clinical checklists) — legitimate configuration.
- All honest `501 NOT_IMPLEMENTED` responses — intentional fail-closed contract behaviour.

## Coverage & blind-spot notes

- **Gate vs register:** the gate detects `mockData`/`Placeholder`-marker/in-memory-`*Store`/in-memory-backing/`TODO:wire`/hardcoded-data-collections. It **cannot** detect semantic defects (G001–G008, G013–G017): code that compiles, persists, returns success while defeating auth/tenancy/forwarding. Those live only here.
- **Reachability matters:** G052 is orphaned/unmounted today (no live impact) — flagged so it is not wired up as-is. (G053 is mounted via `UtilityStrip` and has since been **wired to the inpatient SoR**; G029 was orphaned but has since been **rebuilt real** and mounted; G029b was a *live* fabrication and has been de-fabricated.)
- **REAL_PROVEN — first artifact landed (fix-d):** the tshepo-authz step-up **supervisor-approval path** is now runtime-proven by `StepUpVerificationIT` (full Spring context + all Flyway migrations + fail-closed/lockout/replay invariants against a real Postgres). Everything else remains `REAL_CODE_NOT_PROBED` until its own runtime/test probe exists. The proof is harness-driven against a CLI-/CI-provided Postgres (`-Dit.pg.url=…`); Testcontainers is intentionally unused here because this environment's docker-java client cannot negotiate the engine's minimum API version.
- Not line-re-verified by the sweep: `analytics-pipeline`, `booking` (no markers surfaced).

## CITIZEN ZERO-TO-ONE TRUST JOURNEY (audit 2026-06-25, branch `intake/citizen-zero-to-one`)

Semantic gaps in the ordinary-person path (discover → signup-without-Health-ID → request → temporary →
verified → high-assurance). Full detail + cross-walks: `docs/audits/citizen-zero-to-one/`. None of these
are gate-detectable (absent surfaces / unenforced trust boundaries that compile and return success).

| ID | Finding | file:line | Cat | Decision | Block |
|----|---------|-----------|-----|----------|-------|
| G-CZO-01 | ~~**LOA upgrade never reaches policy.**~~ **FIXED + UNIT-PROVEN (Slice 1).** PolicyEngine now keys `min_loa` (and `account_assurance_required`) on `effectiveLoa = max(ACR loaLevel, parsed X-Assurance-Level)`; the BFF `AssuranceLevelResolutionInterceptor` resolves a CITIZEN caller's current identity-assurance level once per request and `ServiceClientConfig` sets `X-Assurance-Level` authoritatively (overwrite — unspoofable). A self-service upgrade now reaches the trust plane on the next request. Proven: `PolicyEngineTest` (DENY@LOA1 → ALLOW@LOA3 with ACR unchanged; bare-numeric parse) + `AssuranceLevelResolutionInterceptorTest` (citizen-only, fail-safe). Fail-safe: IA unreachable → header omitted → ACR fallback. **Residual:** Envoy must strip inbound `X-Assurance-Level` from external callers (defence-in-depth; BFF already overwrites). Not yet full-stack runtime-proven. | `tshepo-authz/.../core/PolicyEngine.java` (effectiveLoa/accountVerified), `experience-bff/.../config/{AssuranceLevelResolutionInterceptor,WebMvcConfig,ServiceClientConfig}.java` | trust/unenforced | **fixed; unit-proven** | CZO-1 |
| G-CZO-02 | ~~**No public L0 entry.**~~ **FIXED + UNIT-PROVEN (Slice 2).** New public surface `/welcome` (+ `/find-care`, `/emergency`, `/accessibility`) reachable without login: what Impilo is, get-started CTAs (sign in / create account / request Health ID), trust-ladder explainer, major-referral-hospital orientation, emergency + public-health info, a11y/language entry. Static server components, **no authenticated calls, no PII**. Root `/` now redirects by session (`exp_has_session`): authed→`/home`, else→`/welcome`. `middleware.ts` adds `/welcome` to the public set + root passthrough. Proven: `middleware.test.ts` (welcome public, `/home`/`/citizen` still gated, root passes) + `tsc --noEmit` clean. Facility finder is honest orientation (public hospitals are public knowledge) — full searchable directory deliberately gated behind an account (no dev-seed presented as a live directory). | `one-ui-shell/src/app/welcome/*`, `components/public/PublicShell.tsx`, `src/app/page.tsx`, `src/middleware.ts` | missing-surface | **fixed; unit-proven** | CZO-2 |
| G-CZO-03 | **L5 delegated/caregiver access not built** — role constants + non-persisting `stubDelegatedPickup` only (no act-on-behalf authz/audit/scope/expiry/banner). **Scoped design produced; STOP for PO** — registry confirms NO current owner for act-on-behalf relationships; SoR ruling needed (Vito vs tshepo-consent vs new service). See `docs/audits/citizen-zero-to-one/DELEGATION-DESIGN.md`. | `experience-bff/.../service/citizen/CitizenLongtailService.java`; design: `DELEGATION-DESIGN.md` | missing-capability | **design done; STOP for PO** | CZO-3 |
| G-CZO-04 | ~~**No step-up UI on citizen sensitive actions.**~~ **FIXED + UNIT-PROVEN (Slice 4).** Reusable `StepUpPrompt` + `useStepUp` drive the real BFF→tshepo-authz step-up engine (challenge→verify); a sensitive action that returns 401 `STEP_UP_REQUIRED` now shows a clear "verify your identity" prompt and retries on success (wired into record-sharing). `api-client` no longer swallows a step-up 401 as session-expiry (`stepUp.readStepUp`). Proven: `stepUp.test.ts`, `StepUpPrompt.test.tsx`, `CitizenStepUpControllerTest` (identity server-authoritative). | `one-ui-shell` `components/citizen/StepUpPrompt.tsx`, `hooks/useStepUp.ts`, `lib/stepUp.ts`, `lib/api-client.ts`, `app/citizen/record-sharing/page.tsx`; `experience-bff/.../controller/CitizenStepUpController.java` | missing-ux | **fixed; unit-proven** | CZO-4 |
| G-CZO-05 | ~~**Mobile dashboard has no assurance banner**~~ **FIXED + UNIT-PROVEN (Slice 5).** `TrustBanner` on `HomeScreen` reads the citizen's real current level via new `assuranceService.fetchAssuranceStatus()` (BFF `/internal/v1/identity/assurance/status`) and shows account-only (LOA1) / temporary (LOA2) guidance with a continue-verification CTA; hidden at verified LOA3+ (mirrors web `FULLY_VERIFIED`); fail-safe to nothing on error. Clinical sections were already wired — confirmed, no change needed. Proven: `assuranceService.test.ts` + `TrustBanner.test.tsx`; mobile suite 133/133 + tsc clean. | `apps/mobile/citizen-app/src/components/TrustBanner.tsx`, `src/services/assuranceService.ts`, `src/screens/HomeScreen.tsx` | missing-ux | **fixed; unit-proven** | CZO-5 |
| G-CZO-06 | ~~**Policy-consent capture not persisted**~~ **FIXED + UNIT-PROVEN (Phase 1).** SoR = **`mvumo-service`** (consent orchestration). New Mvumo `LegalAgreementEntity` (V005, DISTINCT from clinical FHIR Consent — subject is an actor, never materialised to tshepo-consent) + `LegalAgreementService` (accept/list/withdraw/evaluate, idempotent+supersede, reuses ConsentEvent audit + outbox) + `/internal/v1/mvumo/legal-agreements/**` (auto-proxied). BFF `PolicyConsentController` accept now persists per policy bound to the authenticated actor — **fail-safe** (Mvumo down never blocks the login interstitial). Proven: `LegalAgreementServiceTest` 7/7 + `ConsentControllerTest` 5/5. | `mvumo-service/.../{persistence/LegalAgreement*,service/LegalAgreementService,api/LegalAgreementController}.java`, V005; `experience-bff/.../controller/PolicyConsentController.java`, `client/MvumoServiceClient.java` | write-no-persist | **fixed; unit-proven** | CZO-7 |
| G-CZO-07 | ~~No real citizen consent history/revoke feedback~~ **FIXED (Phase 1).** BFF `/status` (GRANTED-only) + `/history` (all states) now source the actor's real agreements from Mvumo in the JSON:API shape the UI already parses (`policyType/policyVersion/accepted/acceptedAt/revokedAt`); `/revoke` withdraws the matching GRANTED agreement. `/settings/privacy` renders real state with **no UI change**. Fail-safe to empty on Mvumo error. | `experience-bff/.../controller/PolicyConsentController.java`; `one-ui-shell/src/app/settings/privacy/page.tsx`, `hooks/queries/usePolicyConsent.ts` | stub-backed-ux | **fixed** | CZO-7 |
| G-CZO-08 | ~~High-contrast/text-scale/language a11y exist in code but **not user-exposed**.~~ **CORRECTED + FIXED (Slice 6).** Probe disproved the heuristic claim: a11y toggles (high-contrast, large-text, low-bandwidth, simple-language; CSS classes + SR announcements via `useAccessibilityPreferences`) ARE exposed for authenticated users — `ShellAccessibilityMenu` is mounted in `ShellTaskbar:283`. The real gap was the **public L0 surface** (a guest/Persona E couldn't adjust before login). Fixed: `PublicAccessibilityMenu` reuses the same hook, mounted in `PublicShell`; choice persists into the authed experience. Proven: `PublicAccessibilityMenu.test.tsx`. | `one-ui-shell/src/components/public/PublicAccessibilityMenu.tsx`, `components/public/PublicShell.tsx`, `hooks/useAccessibilityPreferences.ts` | a11y-not-exposed | **corrected; fixed** | CZO-6 |
| G-CZO-09 | ~~No resumable/offline Health-ID request continuation.~~ **FIXED (Slice 6).** The request form now persists a localStorage draft as the citizen types and restores it on return (with a "restored your saved progress" banner + Start over), cleared on submit — a dropped connection no longer restarts the journey (Persona D). Proven: `request/page.test.tsx`. | `one-ui-shell/src/app/citizen/health-id/request/page.tsx` | resilience | **fixed** | CZO-6 |
| G-CZO-10 | No low-data mode. | one-ui-shell (absent) | a11y/inclusion | fix | CZO-6 |
| G-CZO-11 | No SMS fallback for **primary** auth (SMS-OTP is step-up only). | `tshepo-authz/.../stepup/NotificationOtpDeliveryAdapter.java` | inclusion | defer (documented) | CZO-6 |
| G-CZO-13 | TEMPORARY assurance tier collects DOB + national-ID with **no verification/step-up**. | `one-ui-shell/src/app/auth/register/assurance/page.tsx:172-208` | privacy/proofing | fix | CZO-4 |
| G-CZO-14 | Web biometric login is **simulated** (hardcoded creds). | `one-ui-shell/src/app/auth/login/biometric/page.tsx:42-81` | mock-in-path | fix or hide | CZO-4 |
| G-CZO-12 | No LOA4/high-assurance banner state (tops out at `FULLY_VERIFIED`→null). | `one-ui-shell/src/components/citizen/IdentityAssuranceBanner.tsx:17-24` | cosmetic | low | CZO-5 |
| G-CZO-15 | Vito↔identity-assurance level not synced (Vito int vs canonical `AssuranceLevel`; no raise-hook). | `vito-service ClientEntity`, `identity-assurance AssuranceService` | consistency | future | — |
| G-CZO-16 | Citizen routes untested (6 `/citizen/*` + `/home`); no LOA-propagation e2e. | `one-ui-shell` (absent tests) | test-gap | fix per slice | CZO-* |
| G-CZO-17 | **Citizen step-up completion loop not exposed via BFF.** tshepo-authz has a full `/v1/step-up/{challenge,verify,status}` engine (B1/G058, runtime-proven supervisor path) but it was **not reachable citizen-facing through the BFF**. Enforcement is **gateway-model**, not token-based: `PolicyEngine` already calls `StepUpService.hasRecentStepUp(tenant, actor)`, and `verifyChallenge` marks a challenge COMPLETED within the window — so completing a challenge and retrying the action is sufficient (no cross-service token contract required; the opaque `X-Step-Up-Token` in vito/mushex is a separate, secondary seam per [[G016]]). Fix = expose the engine through the BFF (citizen-authenticated) + a reusable UI prompt. **FIXED (Slice 4):** `CitizenStepUpController` proxies `/internal/v1/step-up/{challenge,verify,status}` → tshepo-authz, binding tenant/actor **server-side from trust headers** (never the body); `TshepoAuthzServiceClient` extended; SecurityConfig authenticated lane. The opaque-`X-Step-Up-Token` vito/mushex seam is untouched (separate, secondary — gateway model is authoritative for citizen actions). | `tshepo-authz/.../service/StepUpService.java` (`hasRecentStepUp`/`verifyChallenge`), `experience-bff/.../controller/CitizenStepUpController.java` | trust/missing-exposure | **fixed (reuse engine)** | CZO-4 |
