# Adult Medicine — completion register against the brief

**Source: [`brief.md`](brief.md).** One row per section, with the status and — where a section is
not complete — exactly what is missing and who owns it.

This file exists because the pack has twice been reported as more complete than it was. A summary
that says "the clinical decision-support backbone is complete" is true and was read as "the pack is
complete"; the backbone is a minority of the brief. The register is per-section so that reading is
not available.

**Convention: PARTIAL is never counted as delivered.** A section is DONE only when nothing in it is
outstanding.

| § | Section | Status | What is outstanding |
|---|---|---|---|
| 1 | Standards baseline + 8-part DAK decomposition | **PARTIAL** | 3 of the 8 DAK parts exist (recommendations, data elements, decision support). Personas, user scenarios, business workflows, indicators and functional/non-functional requirements were never produced. Content missing for stroke, chronic respiratory and neuro. |
| 2 | Domain boundary | **DONE** | — |
| 3 | Principal care settings | **PARTIAL** | One record across settings holds. Day-treatment, infusion, dialysis and anticoagulation clinic are not modelled as settings. |
| 4 | Canonical episode and problem model | **DONE** | — |
| 5 | 17-step adult journey | **PARTIAL** | **Wave 5.6:** PCT `V119` `adult_journey_positions` + BFF `/internal/v1/medicine/journey-position` + `JourneyPositionIndicator` on medicine workspace (read + advance). Outstanding: wire real encounter/journey anchors from active visit context (workspace currently may pass a placeholder); prove position across the ten clinician journeys. |
| 6 | Full medical clerking | **PARTIAL** | V106 covers social, family, functional, procedures and advance directives with UI write paths. **§6 INTEGRATE hub:** `/ehr/[patientId]/history` mounts `ClerkingContinuityShell` composing PMH (problems), medical episodes (V101 + BFF), meds + V107 reconciliation adherence, allergies, immunisations, past procedures, functional, family, goals/care-plans/directives, maternity summary, admissions, and ADR deep-link to patient-safety — failed reads named, never empty. **Governed social categories** (occupation, exposures, tobacco/alcohol/substance, diet/activity/sleep, support/housing/food) selectable on social-history; unknown still appendable; Simba OUT_OF_PACK. **NEW_PCT V117 built:** presenting concern + timeline, systems review, prior ICU, infection exposure, cognition/mood screens (score-or-reason), genetic risk, safeguarding (confidential lane, not social-history) — PCT + BFF + history hub panel. **Conditional clerking (V118):** visit attestations STILL_TRUE / CHANGED / NOT_ASSESSED — stance required; silence ≠ confirmation; sections collapse only after still-true attestation. |
| 7 | Examination framework | **PARTIAL** | 22 regions, 6 states and the coverage read are DONE (V113). **The eleven graphics now draw** — ExaminationShell mounts body-map instruments whose ids are the V113 graphic codes (`CHEST_AUSCULTATION` … `PRESSURE_INJURIES`), reusing existing maps where they fit and adding cardiac / neuro / dermatome / oedema maps for the rest. Submit no longer drops `graphic`/`site`/`laterality`; marks collapse to those three columns (last pin wins). History redraws a stored site read-only. Outstanding against the brief: not every one of the 22 regions has a diagram (only the nine that clinically site findings), and the new maps are engineering-authored pending clinical verification of SNOMED bindings. |
| 8 | Thirteen specialty workspaces | **PARTIAL** | All thirteen exist as governed configuration over the shared spine, each stating its own `notBuilt` list on screen (a test fails the build if any list is empty). **Specialty spine links** deep-link endoscopy, biopsy, dialysis HD/PD/AVF, ECG/echo/spirometry, volume/fluid balance, **home respiratory monitoring → telemonitoring**, **tobacco → Simba coaching**, **glucose → telemonitoring**, **transfusion → Madi**. **Wave 6:** legacy `/ehr/.../workspace/[specialty]` quarantined (honesty banner + CTA to medicine specialty routes). Design notes for dialysis adequacy and derm morphology — notBuilt kept honest until built. Specialty tooling depth (~30 absent items) remains. |
| 9 | Multimorbidity engine | **PARTIAL** | 11 panels and 7 detections are built and honest about their own inputs. Appointments (booking-service), investigations (OROS, LAB/IMAGING only) and functional status (pct functional assessments) are **now composed by the BFF** — `excessive_visit_schedule`, `repeated_investigations` and `high_treatment_burden` move off UNDETERMINED, and `appointment_burden`, `functional_impact` and `monitoring_plan` move off unknown, all proven in `MultimorbidityControllerTest`. Patient priorities and the care team remain uncomposed **because no service in the estate owns either concept yet** (an advance directive is not "what matters to the patient day to day"; an MDT roster is not a standing care team) — those two panels correctly stay unknown until a first home for the data is built. |
| 10 | Medicines and clinical pharmacology | **PARTIAL** | Reconciliation (V107) and deprescribing content exist; medication facts are now derived rather than asserted. **Wave 5.5:** `RenalDosingAdvisor` (medicine-domain) + CKP/BFF `/internal/v1/medicine/renal-dosing/advise` + `RenalDosingPanel` on medications and medicine workspace — missing eGFR → UNKNOWN, never invented. Outstanding: interaction checking, hepatic dosing, formulary/refill/possession. |
| 11 | Diagnostic orchestration | **PARTIAL** | **Result action is built** (V115). **Ambulatory order sets are built in OROS** (`V018`, batch place, BFF `/internal/v1/order-sets`, `AMBULATORY_ORDER_SETS_BUILT=true`). **Wave 5.1:** patient-level duplicate guard (`DUPLICATE_PATIENT_ORDER`) for LAB/IMAGING with multimorbidity repeat intervals + V019 index — teleconsult `sourceRef` guard retained. Outstanding: deepen ZIBO↔investigation mapping coverage; clinic cart UX beyond OROS place. |
| 12 | Procedures | **PARTIAL** | Indication/appropriateness/interpretation in `procedure-indication-rules.json` now cover LP, paracentesis (+ SBP interpretation), pleural aspiration, cross-procedure anticoag review, **arthrocentesis (hot joint)**, and **bone marrow for unexplained cytopenia** (Wave 5.10). Execution remains procedures-service. Remaining named procedures from the brief still lack dedicated indication depth. |
| 13 | Inpatient medicine | **PARTIAL** | **Wave 5.3:** `MedicalWardRoundShell` shows all 18 brief concerns in a panel grid — composed: problems, severity, allergies, medicines, fluid balance, consultations; twelve others honest `notComposed` (never empty-as-none). Structured round types: MORNING through DISCHARGE. Outstanding: SoR/read paths for oxygen, devices, nutrition, mobility, VTE, infection, pending results, goals, barriers, escalation, ceiling-of-care; problem-linked round entries (needs inpatient column). |
| 14 | Consultation and MDT | **PARTIAL** | Record, service, API, BFF and UI are built; ownership never moves on an answer. **Transfer of care (V116).** MDT decisions from consultations UI. **Wave 5.4:** nullable `case_item_id` on `pct_mdt_decisions` (V120); V051/V114 ownership callouts on both UIs; **V051 untouched**. Full consolidation still needs telemedicine-lane agreement to wire session→decision in product flows — see [`mdt-consolidation-proposal.md`](mdt-consolidation-proposal.md). |
| 15 | Longitudinal monitoring and home care | **ROUTED** to telemonitoring-service | **Claim VERIFIED.** Do not build a rival home-vitals SoR (V004 sole SHR writer). **Wave 5.2 pack-owned item BUILT:** `V006__pct_problem_anchor` + `PctProblemContributionClient` on plan approve (fail-open). Outstanding inside telemonitoring: measurement domains, enrolment UI — see [`routed-blockers.md`](routed-blockers.md). |
| 16 | Clinical decision support | **DONE** | 12 governed packs, 383 fixtures, full provenance and override metadata. The pack's strongest asset. **A severe defect was found and closed here:** `/clinical-tools` rendered sixteen calculators (MELD, Child-Pugh, eGFR CKD-EPI, SOFA, GCS, CHA₂DS₂-VASc, CURB-65, Wells DVT/PE, BSA, creatinine clearance, anion gap, corrected calcium/sodium and more) that computed nothing and asserted a **"Validated"** badge — the worst instance of the defect class this programme exists to remove, because it claimed validation rather than merely omitting a feature. Fix: `libs/medicine-domain` — previously a standalone module not wired into `services/pom.xml`, so its own tests never ran in the reactor build — is now a reactor module, gained the missing calculators with the same `RefusalReason` discipline as the ones already present, and is served through a CKP calculator endpoint and a BFF route/`useCalculator` hook. `CalculatorDetail` is rewritten as catalogue-driven forms with genuine result/refusal/failure states; the false "Validated" badge and CKP-connection line are gone. eGFR CKD-EPI 2021 is now derived (not merely displayed) into all four consumers that need it: `ClinicalRulesEngine`, the §9 renal panel, deprescribing's renal gates, and pct's `PatientFacts`. A guard test fails the build if a calculator renders without either a computation or an explicit not-built state. |
| 17 | African and Zimbabwean context | **PARTIAL** | HIV, TB and EDLIZ are deep. **Malaria, sickle-cell and RHD ENGINEERING_SEED packs** (`malaria-adult.json`, `sickle-cell-adult.json`, `rheumatic-heart-disease.json`) registered in CKP and linked from infectious-diseases / haematology / cardiology CDS topics — evaluate path wired; not ratified. Stockouts, distance and connectivity are not modelled. |
| 18 | Integration | **PARTIAL** | PCT, CKP, zibo, OROS, BUTANO (Condition), procedures. Dura, Madi, TUSO, VARAPI, Khuluma, Nompilo, Ndila, Rito, Ruvimbo/COSTA, Fundo and Simba are not wired from this pack. |
| 19 | Data model and interoperability | **PARTIAL** | Three producers now: `Condition`, `ClinicalImpression` (the examination) and `DetectedIssue` (the §9 findings). PII excluded, unknown values mapped to null rather than guessed, and the six examination states kept distinct at the FHIR boundary. **CKP Kafka relay** still defaults to OFF (`impilo.clinical.kafka.relay-enabled`) — **Wave 5.9:** `CLINICAL_KAFKA_RELAY_ENABLED: "true"` is already set for `clinical-knowledge-platform-service` in `values-full-preview.yaml` (not the four-service `impilo-preview` slice, which does not include CKP). Unit proof: `ClinicalKafkaOutboxRelayIT`. **Post-deploy runtime proof:** `bash scripts/runtime-proof/ckp-kafka-outbox-relay.sh` (checks pod env + drains a fresh `clinical.event_outbox` row after multimorbidity assess). **Not yet proven on live preview** until HEAD is deployed and the script passes. **DetectedIssue retirement is built:** V071 `multimorbidity_detection_snapshot` records OPEN findings; a subsequent assess that answers the owning detection as no longer DETECTED emits `MULTIMORBIDITY_ISSUE_RESOLVED` → topic `impilo.clinical.multimorbidity.issue.resolved` → BUTANO moves status to `cancelled`. Silence never retires; UNDETERMINED never retires (`MultimorbidityIssueRetirementTest`, `DetectedIssueShrBoundaryTest`). ~23 resources still have no producer. |
| 20 | Offline operation | **NOT BUILT** | Two verified blockers, **both in services this pack does not own**. (a) `OfflineRulesEngine.READ_ACTIONS:51` in **`tshepo-offline-service`** — *corrected: this register previously said offline-edge-service* — holds only READ_PATIENT/MEDICATION/ENCOUNTER/OBSERVATION, and the gate is two-layered (the set, and the capability token's `allowed-offline-actions` filter at mint time). Adding the three reads to the set **and** the READ_PATIENT-covers branch at `:124` needs no token change. (b) `offline-edge-service` — the client is not the constraint; `OfflineEdgeService.replayActions:143` only dispatches to FHIR for two vitals action types. **Corrected:** `useOfflineStore` is live in **one** screen (`HouseholdListScreen.tsx:27`), not several — `ScreeningScreen.tsx:21` imports it and never calls it. Medicine would be *consuming* an existing seam. Routed, not absorbed. |
| 21 | Analytics | **PARTIAL** | 6 implemented, 6 partial, 9 not computable — see [`analytics-coverage.md`](analytics-coverage.md). Equity is **structurally** impossible here: PCT holds CPID and no PII by design. |
| 22 | Testing | **PARTIAL** | Unit, content, migration and mutation proofs are strong. **Wave 5.12:** `AdultMedicineScenarioTest` encodes 5 named scenarios (frailty+diabetes conflict, duplicate HbA1c, CKD/diabetes diet, metformin eGFR 25, missing eGFR UNKNOWN). Remaining brief scenarios not yet encoded. |
| 23 | Required demonstrations | **PARTIAL** | Record layer 35/35; service layer 30/30. **Clinician walkthrough** still **INCOMPLETE** — see `reports/journeys/medicine-clinician-walkthrough-20260731/summary.txt` and Wave 4 checkpoint `reports/journeys/medicine-wave4-deploy-checkpoint.md`. Requires product owner `AUTHORIZE DEPLOY WITH VM GATES`, HEAD-matched `/health/version`, then re-run `clinician-walkthrough.md`. |
| 24 | Expected outputs | **PARTIAL** | Delivered: repository audit, medicine architecture, specialty map, HIV/TB DAK traceability, canonical problem model, decision-support catalogue, procedures integration, analytics, tests, completion report. Missing: DAK-style traceability for other specialties, full medical clerking, demonstration data. |
| 25 | Definition of done | **NOT MET** | Checklist in [`routed-blockers.md`](routed-blockers.md). Still outstanding: clinician walkthrough on HEAD preview, full product reconciliation, emergency/surgical handoffs in product, §20 offline (other owners). Pack Waves 5–7 advanced PARTIAL sections but PARTIAL ≠ DONE. |

## Totals

**3 DONE · 18 PARTIAL · 1 ROUTED · 2 NOT BUILT/NOT MET** (§20 NOT BUILT, §25 NOT MET).
§15 remains ROUTED (pack-owned PCT anchor now built). §20 still blocked in other owners — see
[`routed-blockers.md`](routed-blockers.md).

Mobile first mirrors (Wave 7): provider medicine workspace, CDS evaluate, clerking continuity
read, chronic registers — see [`docs/mobile/adult-medicine-parity.md`](../../mobile/adult-medicine-parity.md).
54 AdultMedicine specialty tools remain `IN_DEVELOPMENT`.

## Highest-value next pieces

1. **Wave 4 unlock:** product owner types `AUTHORIZE DEPLOY WITH VM GATES` → deploy HEAD →
   clinician walkthrough COMPLETE → update §23/§25.
2. **Wire real visit anchors** into `JourneyPositionIndicator` (drop patientId placeholder).
3. **Telemedicine lane:** finish V051↔V114 product consolidation using V120 `case_item_id`.
4. **§20 owners:** offline READ + replay (tracked in routed-blockers).
5. **Specialty tooling depth** and remaining §22 scenarios; exam map clinical SNOMED verify (§7).
