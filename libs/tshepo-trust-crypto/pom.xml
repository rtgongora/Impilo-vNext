<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>zw.gov.mohcc.impilo</groupId>
    <artifactId>tshepo-trust-crypto</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <packaging>jar</packaging>
    <name>TSHEPO — Trust Crypto Primitive</name>
    <description>
        Pure-Java Tshepo trust-plane primitive: JWS signature verification with kid
        resolution, an algorithm allowlist, and a canonical trust error model
        (malformed / unsupported-alg / unknown-key / invalid-signature / expired /
        not-yet-valid / unknown-issuer / wrong-audience / wrong-purpose / revoked /
        trust-boundary). The building block later Tshepo trust work (offline capability
        tokens, data-access permits, GDHCN document-signer verification) verifies against.
        Named as a Tshepo primitive — NOT a claim of GDHCN conformance. No Spring.
    </description>

    <properties>
        <java.version>21</java.version>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <nimbus-jose.version>9.40</nimbus-jose.version>
        <junit.version>5.10.3</junit.version>
    </properties>

    <dependencies>
        <!-- Nimbus JOSE+JWT for JWS verification (Ed25519/RSA/EC) -->
        <dependency>
            <groupId>com.nimbusds</groupId>
            <artifactId>nimbus-jose-jwt</artifactId>
            <version>${nimbus-jose.version}</version>
        </dependency>

        <!-- Google Tink — required by Nimbus for Ed25519 OKP verification at runtime -->
        <dependency>
            <groupId>com.google.crypto.tink</groupId>
            <artifactId>tink</artifactId>
            <version>1.14.1</version>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <version>3.26.3</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <release>21</release>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.3.1</version>
            </plugin>
        </plugins>
    </build>
</project>
