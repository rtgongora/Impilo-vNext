{
  "name": "@impilo/mobile-api-client",
  "version": "0.1.0",
  "private": true,
  "description": "HTTP client with v1.1 trust header injection, ApiEnvelope parsing, idempotency, retry, and error handling for Impilo mobile apps.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@impilo/mobile-trust": "workspace:*",
    "@impilo/mobile-auth": "workspace:*"
  },
  "peerDependencies": {
    "react": "^18.2.0"
  },
  "devDependencies": {
    "typescript": "~5.5.4",
    "vitest": "^2.1.0",
    "react": "^18.3.1",
    "@types/react": "^18.3.11"
  },
  "engines": {
    "node": ">=20"
  }
}
