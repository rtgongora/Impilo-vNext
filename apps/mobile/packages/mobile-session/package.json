{
  "name": "@impilo/mobile-session",
  "version": "0.1.0",
  "private": true,
  "description": "Shared adaptive real-time session components for Impilo mobile apps — governed LiveKit room rendering (consult/grid/stage layouts), pre-join device checks, and RTC server URL normalization over the canonical self-hosted LiveKit (rtc-gateway).",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "peerDependencies": {
    "@livekit/react-native": ">=2.0.0",
    "livekit-client": ">=2.0.0",
    "react": ">=18.2.0",
    "react-native": ">=0.73.0"
  },
  "devDependencies": {
    "@livekit/react-native": "^2.11.0",
    "@livekit/react-native-webrtc": "^144.1.0",
    "@types/react": "~19.1.10",
    "livekit-client": "^2.19.0",
    "react": "19.1.0",
    "react-dom": "19.1.0",
    "react-native": "0.81.5",
    "typescript": "~5.9.2",
    "vitest": "^2.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
