{
  "name": "@impilo/mobile-integration",
  "version": "0.1.0",
  "private": true,
  "description": "Integration Service mobile SDK — typed status enum, friendly mobile copy, route/dispatch deep-link helpers, and a tiny client for read-only status polling. Mobile apps must NOT integrate directly with PACS/LIMS/eLMIS/telemedicine vendors; they must consume Integration Service contracts via this SDK.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@impilo/mobile-api-client": "workspace:*"
  },
  "peerDependencies": {
    "react": ">=18.2.0"
  },
  "devDependencies": {
    "typescript": "~5.9.2",
    "vitest": "^2.1.0",
    "react": "19.1.0",
    "@types/react": "~19.1.10"
  },
  "engines": {
    "node": ">=20"
  }
}
