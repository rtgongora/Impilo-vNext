{
  "name": "@impilo/mobile-registry",
  "version": "0.1.0",
  "private": true,
  "description": "Canonical sovereign service registry and wiring metadata for Impilo mobile apps.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "~5.5.4",
    "vitest": "^2.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
