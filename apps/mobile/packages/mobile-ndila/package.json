{
  "name": "@impilo/mobile-ndila",
  "version": "0.1.0",
  "private": true,
  "description": "Ndila SDK for Impilo mobile apps — GPS capture, offline-queued location submissions, nearby services, route preview, geofence checks, and Ndila-driven map config.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@impilo/mobile-api-client": "workspace:*",
    "@impilo/mobile-trust": "workspace:*",
    "@impilo/mobile-offline": "workspace:*"
  },
  "peerDependencies": {
    "react": ">=18.2.0"
  },
  "devDependencies": {
    "typescript": "~5.5.4",
    "vitest": "^2.1.0",
    "react": "19.1.0",
    "@types/react": "~19.1.10"
  },
  "engines": {
    "node": ">=20"
  }
}
