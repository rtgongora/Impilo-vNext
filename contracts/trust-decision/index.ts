/**
 * Canonical package entry point for Tshepo trust-decision contracts.
 *
 * Consumers must import from `contracts/trust-decision` (this file) or
 * `contracts/trust-decision/v1` — never re-declare these types locally.
 * See VERSIONING.md for compatibility and deprecation rules.
 */
export * from "./v1";
export * from "./validate";
